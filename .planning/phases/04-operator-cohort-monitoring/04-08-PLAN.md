---
phase: 04-operator-cohort-monitoring
plan: 08
type: execute
wave: 7
depends_on: ["04-06"]
files_modified:
  - e2e/operator-cohort.ts
  - e2e/browser-operator.ts
  - e2e/live-uat.ts
  - package.json
  - docs/adr/0016-polled-monitoring-read-model.md
  - docs/adr/0015-operator-authentication.md
  - docs/DEPLOY.md
  - .planning/scoping/participant-esplora-override.md
  - .planning/scoping/external-signers.md
  - .planning/scoping/tos-payments-notifications.md
  - .planning/phases/04-operator-cohort-monitoring/04-LIVE-UAT-CHECKLIST.md
autonomous: false
requirements: [SVC-03, LIVE-01]
must_haves:
  truths:
    - "A hermetic CI-facing monitoring e2e over the FIXTURE path asserts the operator monitoring reads reflect real cohort activity (members joined, submissions) end to end (D-47 fixture leg)."
    - "A local browser-level operator capstone (playwright-core: login, create, advertise, watch member/submission/co-sign/anchor states appear) mirrors e2e:browser:participant; CI wiring stays Phase 6 debt (D-49)."
    - "e2e/live-uat.ts is formalized into the repo as the repeatable opt-in live check (`pnpm uat:live` against Polar/regtest), RESHAPED to boot through the env-passthrough demo-server path and drop the getUtxos monkey-patch now that the native funding stage exists, keeping the runner-event tap + funding prompts (D-48)."
    - "A new ADR (0016) documents the polled monitoring read model + dashboard-SSE retirement, explicitly superseding ADR 0004, and patches ADR 0015's rationale/route inventory (it cited /dashboard/events + EventSource header limits; the cookie-session scheme survives on its independent merits) (D-53)."
    - "docs/DEPLOY.md gains an honest going-live section: env vars (incl. BROADCAST / RECOVERY_KEY / FUNDING_WINDOW_MS / LIVE_CHANGE_ADDRESS / SERVICE_NAME), the funding walkthrough, network choices, and guard rails; the flagship live story is MUTINYNET, regtest/Polar is local development, hermetic remains the boot default (D-52)."
    - "Three scoping one-pagers land under .planning/scoping/ (problem, options, recommended scope, dependencies incl. upstream needs) for participant esplora override, external signers (incl. the PSBT registration-leg option; full MuSig2 needs an upstream signer interface), and ToS/contracts/payments/participant notifications - SCOPING ONLY, no build this phase (D-54)."
    - "A human live-UAT walkthrough checklist (boot live, create, advertise, fund via the new stage, watch anchor confirm, resolve) is authored for the owner to run against Polar (D-50)."
    - "SVC-03 monitoring AND live-path operability (LIVE-01) are jointly the non-negotiable core; the trimmings (export, SERVICE_NAME, the three one-pagers) were the slip-first items and are all delivered (D-55)."
  artifacts:
    - path: e2e/browser-operator.ts
      provides: "local playwright-core operator capstone (login -> create -> advertise -> watch monitored states)"
      contains: "browser"
    - path: docs/adr/0016-polled-monitoring-read-model.md
      provides: "ADR recording the polled read model + SSE retirement, superseding ADR 0004"
      contains: "Supersedes"
    - path: .planning/phases/04-operator-cohort-monitoring/04-LIVE-UAT-CHECKLIST.md
      provides: "owner-run live-UAT walkthrough checklist against Polar/regtest"
      contains: "Fund"
  key_links:
    - from: e2e/live-uat.ts
      to: packages/service/src/demo-server.ts
      via: "reshaped to boot through the env-passthrough (BROADCAST=1) demo-server path, no getUtxos monkey-patch"
      pattern: "BROADCAST"
    - from: docs/adr/0016-polled-monitoring-read-model.md
      to: docs/adr/0004-dashboard-sse-telemetry-channel.md
      via: "Supersedes header pointing at 0004"
      pattern: "0004"
---

## Phase Goal

See ROADMAP.md Phase 4 + D-55. This plan proves both cores and documents the phase: the hermetic monitoring e2e + browser operator capstone (D-47/D-49), the formalized live-UAT harness + owner checklist (D-48/D-50), the new ADR superseding 0004 + the DEPLOY going-live section (D-52/D-53), and the three scoping one-pagers (D-54). It is the phase's final gate, ending in a blocking human live-UAT walkthrough (the owner's "actual real server working out-of-the-box").

<objective>
Land the proof and documentation deliverables and the phase's final human gate: a hermetic fixture-path monitoring e2e (D-47) and a local browser operator capstone (D-49); the formalized `pnpm uat:live` harness reshaped onto the env-passthrough boot (D-48); the new ADR 0016 (polled read model, supersedes 0004, patches 0015) and the DEPLOY going-live section (D-52/D-53); three scoping one-pagers (D-54); and the owner-run live-UAT checklist behind a blocking human-verify checkpoint (D-50/D-55).

Purpose: prove SVC-03 monitoring criterion 4 (browser capstone) and the live path end to end (owner walkthrough), and leave the operator an honest going-live story.
Output: e2e capstone + fixture monitoring e2e + formalized live harness + ADR + DEPLOY + one-pagers + checklist + the live-UAT checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-operator-cohort-monitoring/04-CONTEXT.md
@.planning/phases/04-operator-cohort-monitoring/04-RESEARCH.md
@docs/adr/0004-dashboard-sse-telemetry-channel.md
@docs/adr/0015-operator-authentication.md
@docs/DEPLOY.md
@e2e/operator-cohort.ts
@e2e/browser-participant-cohort.ts
@e2e/live-uat.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Proof harnesses - fixture monitoring e2e + browser operator capstone + formalized uat:live</name>
  <files>e2e/operator-cohort.ts, e2e/browser-operator.ts, e2e/live-uat.ts, package.json</files>
  <read_first>
    - e2e/operator-cohort.ts (the existing operator e2e to extend with monitoring-read assertions over the fixture path; its negative-auth pin was migrated in 04-02)
    - e2e/browser-participant-cohort.ts (the playwright-core browser capstone pattern to mirror for the operator: one real Chromium page + headless peers)
    - e2e/live-uat.ts (uncommitted-on-disk harness: the getUtxos monkey-patch funding-wait, the keygen-complete FUND-THIS-ADDRESS prompt, the runner-event tap, human-paced timeouts - reshape to env-passthrough boot + native funding stage, keep the tap + prompts)
    - packages/service/src/demo-server.ts (the env-passthrough boot path uat:live now uses; BROADCAST=1)
    - .planning/phases/04-operator-cohort-monitoring/04-RESEARCH.md (Validation Architecture test map: e2e:monitor, e2e:browser:operator, uat:live)
  </read_first>
  <action>
Extend `e2e/operator-cohort.ts` (or add a focused `e2e/monitor-cohort.ts` wired as `e2e:monitor`) to assert the operator monitoring reads over the FIXTURE path: boot `createService` with an operator password + operatorCohorts + monitor, advertise a cohort, run headless participants that join and submit, then assert `GET /v1/operator/cohorts/:id` (within a session) reflects the members joined, the submissions, and the ended taxonomy - proving the monitoring read model end to end hermetically (D-47 fixture leg). Create `e2e/browser-operator.ts` (playwright-core, headless Chromium, local-only) mirroring `e2e/browser-participant-cohort.ts`: drive one real page through operator login -> create cohort -> advertise -> open the drill-down and assert the member/submission/co-sign/anchor states appear as headless peers drive the cohort (compose with the live-mock service pattern from 04-06 when it needs anchor states) (D-49). Formalize `e2e/live-uat.ts` into a committed, RESHAPED opt-in check: boot through the env-passthrough demo-server path (`LIVE=1 BROADCAST=1`, real esplora), DROP the getUtxos monkey-patch now that the native funding stage exists, and KEEP the runner-event tap + the keygen-complete funding prompts + the human-paced timeouts (D-48). Add `package.json` scripts `e2e:monitor`, `e2e:browser:operator`, and `uat:live`. CI wiring of the browser capstones stays Phase 6 debt (do NOT add them to the CI workflow).
  </action>
  <verify>
    <automated>pnpm exec tsx e2e/operator-cohort.ts && pnpm --filter @btcr2-aggregation/service exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - The fixture monitoring e2e (`e2e:monitor` or extended `e2e:operator`) asserts `GET /v1/operator/cohorts/:id` reflects members joined + submissions + ended taxonomy over the hermetic fixture path.
    - `e2e/browser-operator.ts` exists and drives login -> create -> advertise -> drill-down state assertions with one real Chromium page (local-only; not wired into CI).
    - `e2e/live-uat.ts` is committed, boots through the env-passthrough (`LIVE=1 BROADCAST=1`) demo-server path with NO getUtxos monkey-patch, and keeps the runner-event tap + funding prompts; `pnpm uat:live` runs it.
    - `package.json` has `e2e:monitor`, `e2e:browser:operator`, `uat:live` scripts; CI workflow is unchanged (Phase 6 debt).
  </acceptance_criteria>
  <done>The monitoring read model is proven hermetically, the browser operator capstone exists locally, and the live-UAT harness is formalized on the env-passthrough boot.</done>
</task>

<task type="auto">
  <name>Task 2: ADR 0016 (supersedes 0004, patches 0015) + DEPLOY going-live section + three scoping one-pagers</name>
  <files>docs/adr/0016-polled-monitoring-read-model.md, docs/adr/0015-operator-authentication.md, docs/DEPLOY.md, .planning/scoping/participant-esplora-override.md, .planning/scoping/external-signers.md, .planning/scoping/tos-payments-notifications.md</files>
  <read_first>
    - docs/adr/0004-dashboard-sse-telemetry-channel.md (the channel being superseded; mirror the ADR format + Supersedes-header convention)
    - docs/adr/0015-operator-authentication.md (its SSE rationale + route inventory citing /dashboard/events + EventSource header limits - patch those; the cookie-session scheme survives on its independent merits)
    - docs/adr/0010-mainnet-guard-rails.md (the guard-rail + going-live tone to mirror in DEPLOY)
    - docs/DEPLOY.md (the existing deploy doc to extend with the going-live section)
    - .planning/todos/pending/2026-07-21-let-participants-supply-their-own-esplora-endpoint.md, .planning/todos/pending/2026-07-21-support-external-signers-instead-of-pasted-private-keys.md, .planning/todos/pending/2026-07-21-scope-tos-contracts-payments-and-participant-notifications.md (the three folded todos the one-pagers trace back to)
  </read_first>
  <action>
Author `docs/adr/0016-polled-monitoring-read-model.md` following the `docs/adr/000N-*.md` sequence + Supersedes-header convention: document the polled monitoring read model (the per-service fold + gated summary/detail reads + bounded activity ring + ended-record taxonomy), why it replaces the dashboard-SSE telemetry channel (fresh-load truth, one data path, EventSource cannot carry the operator session header cleanly), and add a `Supersedes: ADR 0004` header (D-53). Patch `docs/adr/0015-operator-authentication.md`: update its rationale/route inventory where it cites `/dashboard/events` + the EventSource header limits (the SSE feed is retired), noting the cookie-session scheme survives on its independent merits (no auth change). Extend `docs/DEPLOY.md` with an honest going-live section: the env vars (NETWORK, LIVE, BROADCAST, ALLOW_MAINNET, RECOVERY_KEY, FUNDING_WINDOW_MS, LIVE_CHANGE_ADDRESS, SERVICE_NAME, PHASE_TIMEOUT_MS/COHORT_TTL_MS), the cohort-beacon funding walkthrough (fund the beacon address from your own wallet, the suggested minimum, the dead-end, the window), network choices + guard rails, with MUTINYNET as the flagship live story (real public chain, free coins), regtest/Polar as the local-development story, and hermetic as the boot default (D-52). Author three scoping one-pagers under `.planning/scoping/` (each: problem, options, recommended scope, dependencies incl. upstream needs; SCOPING ONLY, no build this phase, D-54): `participant-esplora-override.md` (let participants supply their own esplora endpoint; proxy stays the default), `external-signers.md` (external signers instead of pasted private keys; the PSBT registration-leg option sooner, full MuSig2 needs an upstream signer interface), `tos-payments-notifications.md` (ToS/contracts/payments/participant notifications - new milestone-level scope). All prose em-dash-free.
  </action>
  <verify>
    <automated>test -f docs/adr/0016-polled-monitoring-read-model.md && grep -qi "supersed" docs/adr/0016-polled-monitoring-read-model.md && test -f .planning/scoping/participant-esplora-override.md && test -f .planning/scoping/external-signers.md && test -f .planning/scoping/tos-payments-notifications.md && grep -qi "BROADCAST" docs/DEPLOY.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/adr/0016-polled-monitoring-read-model.md` exists, documents the polled read model + SSE retirement, and carries a `Supersedes: ADR 0004` header.
    - `docs/adr/0015-operator-authentication.md` no longer presents `/dashboard/events` + EventSource header limits as live rationale; the cookie-session scheme is noted as surviving on its own merits.
    - `docs/DEPLOY.md` has a going-live section covering BROADCAST/RECOVERY_KEY/FUNDING_WINDOW_MS/LIVE_CHANGE_ADDRESS/SERVICE_NAME, the funding walkthrough, and MUTINYNET-flagship / regtest-local / hermetic-default framing.
    - The three `.planning/scoping/*.md` one-pagers exist, each with problem/options/recommended-scope/dependencies, scoping-only (no build).
    - No em-dash character in any authored doc.
  </acceptance_criteria>
  <done>The ADR supersession + patched 0015 + DEPLOY going-live + three scoping one-pagers are authored, honest, and em-dash-free.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner live-UAT walkthrough against Polar/regtest (the real server working out-of-the-box)</name>
  <what-built>
    The live-path operability core (LIVE-01): BROADCAST=1 boot enablement, the cohort-beacon funding stage (3 states + dead-end + recovery-key/mainnet disclosure), the clamped funding wait, bounded broadcast send retry, and the participant awaiting-funding/resolve-guard fixes - plus the formalized `pnpm uat:live` harness and the authored checklist. The 04-LIVE-UAT-CHECKLIST.md walkthrough is authored in this task and captures the exact steps.
  </what-built>
  <how-to-verify>
    First author `.planning/phases/04-operator-cohort-monitoring/04-LIVE-UAT-CHECKLIST.md`: a step-by-step owner walkthrough (boot `LIVE=1 BROADCAST=1` against Polar/regtest with a funded wallet; operator login; create + advertise a live cohort; observe the funding stage surface at keygen and the `Needs funding` chip; fund the beacon address from the external wallet; watch the funding stage advance waiting -> seen -> funded; watch co-sign complete, the beacon broadcast, and the anchor confirm; resolve the updated DID). Then the OWNER runs it (real funds, real chain - cannot be automated in CI; the hermetic mocked-chain leg from 04-06 covers CI). Confirm: the funding stage advances honestly, the anchor confirms, the resolve reflects the update, and the stall/resolve copy reads honestly throughout.
  </how-to-verify>
  <resume-signal>Type "approved" once the live walkthrough passes end to end, or describe the defects observed (funding-stage state, anchor, resolve, or copy) for gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> operator practice | The DEPLOY going-live doc must state the real-money guard rails honestly so a self-hoster does not fund a mainnet cohort unaware. |
| live-UAT harness -> real chain | uat:live boots against a real chain with real funds; it is opt-in, never a CI dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08-01 | Information Disclosure | DEPLOY / one-pagers | low | mitigate | Docs describe env-var names + guard rails, never secret values; RECOVERY_KEY/OPERATOR_PASSWORD are described as env-only, never logged/baked (M4 lesson). |
| T-04-08-02 | Elevation of consequence | going-live guidance | high | mitigate | The going-live section leads with the mainnet REAL FUNDS + throwaway-recovery-key rails (ADR 0010 tone), MUTINYNET flagship, hermetic default; the owner live-UAT is a blocking human gate before phase sign-off. |
| T-04-08-03 | Repudiation | live-UAT evidence | medium | accept | The owner-run walkthrough is manual (real funds); the hermetic mocked-chain leg (04-06) + fixture monitoring e2e (this plan) are the automated evidence of record. |
</threat_model>

<verification>
- `pnpm exec tsx e2e/operator-cohort.ts` (fixture monitoring e2e) green; `pnpm --filter @btcr2-aggregation/service exec tsc -b` green.
- ADR 0016 exists with a Supersedes header; DEPLOY has the going-live section; three one-pagers exist.
- Owner completes the live-UAT walkthrough against Polar and approves (blocking).
- No em-dash in any authored doc.
</verification>

<success_criteria>
Both cores are proven (hermetic monitoring e2e + browser capstone + owner live-UAT) and documented (ADR 0016 supersedes 0004, patched 0015, DEPLOY going-live, three scoping one-pagers); the trimmings (export, SERVICE_NAME, one-pagers) are all delivered per D-55.
</success_criteria>

<output>
Create `.planning/phases/04-operator-cohort-monitoring/04-08-SUMMARY.md` when done.
</output>

