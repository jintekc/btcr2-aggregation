---
phase: 04-operator-cohort-monitoring
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - packages/service/src/monitor.ts
  - packages/service/src/hono-adapter.ts
  - packages/service/tests/monitor.spec.ts
  - packages/web/src/lib/operator.ts
  - packages/web/src/components/operator/CohortDetail.tsx
  - packages/web/src/components/operator/OperatorStageTimeline.tsx
autonomous: true
requirements: [SVC-03]
must_haves:
  truths:
    - "The drill-down leads with a cohort-lifecycle stage timeline (advertise -> filling -> submissions -> co-signing -> anchor; funding stage added by 04-06 for live) mirroring the participant StageTimeline treatment (completed good dot, active pulsing accent dot, pending dim neutral) (D-05)."
    - "Members show per-member round state on the row (Seated / Submitted / Validated / Nonce sent / Rejected), so one place answers who is holding the cohort up; rejections also land in the activity log (D-31)."
    - "Submissions show who has and has not submitted with server wall-clock timestamps; the raw signed-update JSON sits behind a `Raw signed update` technical expander (D-30/D-12)."
    - "Co-sign progress is per-participant where events carry identity (`{k} of {n} nonces received` with per-member Nonce sent chips); the partial-signature leg shows the honest line `All {n} nonces received. Awaiting partial signatures.` with NO per-member or k-of-n count for that leg (D-32)."
    - "Operator anchor detail mirrors participant D-22: Signed -> Broadcast (txid + explorer link) -> Confirmed, freezing at first confirmation, with `Anchored` narration reserved for a confirmed tx only; a hermetic no-broadcast service shows the no-on-chain-anchor explanation; a fallback anchor states plainly it anchored via the k-of-n fallback with {k} of {n} signatures (D-18/D-33)."
    - "The per-cohort activity log is a server-side bounded ring buffer delivered in the detail DTO (surviving fresh page loads), carrying ALL runner events (incl. rejects, nonce progress, non-fatal errors) plus broadcaster frames, each server wall-clock stamped, tone by level (D-21/D-22)."
    - "The pubkeys (participantPk/communicationPk) sit behind the member row's `Technical detail` expander only, never on the plain row (D-28)."
    - "Expired records sit in the Ended group with an expiry reason and the shipped Re-advertise action; expired records have no drill-down (D-10)."
    - "A gated `GET /v1/operator/cohorts/:id/export` downloads exactly the cohort's monitoring DTO + activity log as JSON, off-chain artifacts referenced by hash at /cas/, with a safe Content-Disposition and no new auth surface (D-34)."
    - { statement: "The activity log renders a full bounded ring inside a fixed-height internal scroll without growing the page (LogPanel auto-follow).", verification: backstop }
  prohibitions:
    - statement: "MUST NOT present an unobserved outcome as an established fact: `Anchored` only when a confirmation is observed, co-sign progress only for legs that emit an identified event, and NEVER an invented per-member or k-of-n partial-signature count for the leg that emits no event (D-18/D-32/D-33)."
  artifacts:
    - path: packages/service/src/monitor.ts
      provides: "detail DTO depth: submissions (who/when), per-member round state, honest co-sign, anchor detail compose, bounded activity ring; export serializer"
      contains: "activity"
    - path: packages/web/src/components/operator/CohortDetail.tsx
      provides: "drill-down depth render: stage timeline, member round-state chips + technical expander, submissions, co-sign lines, anchor sub-steps, activity log, export button"
      contains: "Activity"
    - path: packages/web/src/components/operator/OperatorStageTimeline.tsx
      provides: "operator cohort-lifecycle stage timeline (own stage set, not the participant component)"
      contains: "StageTimeline"
  key_links:
    - from: packages/web/src/components/operator/CohortDetail.tsx
      to: packages/service/src/monitor.ts
      via: "detail DTO carries members round-state, submissions, coSign honest fields, anchor, activity[]"
      pattern: "activity"
    - from: packages/service/src/monitor.ts
      to: packages/service/src/anchor-state.ts
      via: "anchor detail composed from the existing anchor-state read (public read byte-untouched)"
      pattern: "anchor"
---

## Phase Goal

See ROADMAP.md Phase 4 (criteria 1-3). This plan fills the per-cohort drill-down (D-01/D-05) with the full monitored depth: submissions + who's-holding-it-up round state, honest co-sign progress (with the honest partial-sig limit, D-32), operator anchor detail (D-18/D-33), the per-cohort activity log (D-21/D-22), and the JSON export (D-34).

<objective>
Deepen the monitor detail DTO and the drill-down render to the full SVC-03 scope: per-member round state and submissions with wall-clock timestamps, honest per-participant co-sign progress that refuses to invent the unobservable partial-signature leg (D-32), operator-side anchor detail mirroring the participant D-22 honest progression, a server-side bounded activity ring surviving fresh loads (D-21/D-22), and a gated per-cohort JSON export (D-34). All copy is plain-first with raw protocol detail behind the technical expander (D-12) and mode-honest (positive terminal claims only from observed facts).

Purpose: complete SVC-03 criteria 2 (submissions + co-sign progress) and 3 (anchor status) on the drill-down.
Output: monitor detail depth + operator stage timeline + members/submissions/co-sign/anchor/activity render + export.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-operator-cohort-monitoring/04-CONTEXT.md
@.planning/phases/04-operator-cohort-monitoring/04-RESEARCH.md
@.planning/phases/04-operator-cohort-monitoring/04-UI-SPEC.md
@packages/service/src/monitor.ts
@packages/service/src/anchor-state.ts
@packages/service/src/hono-adapter.ts
@packages/web/src/components/cohort/StageTimeline.tsx
@packages/web/src/components/operator/CohortDetail.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Monitor detail depth (submissions, round state, honest co-sign, anchor detail, activity ring)</name>
  <files>packages/service/src/monitor.ts, packages/service/tests/monitor.spec.ts</files>
  <read_first>
    - packages/service/src/monitor.ts (the fold + detail/summary from 04-01/04-02 to extend)
    - .planning/phases/04-operator-cohort-monitoring/04-RESEARCH.md (Runner event set: update-received/message-rejected/validation-received/nonce-received/signing-complete payloads; Session accessors getCohort pendingUpdates/collectedUpdates/validationProgress; Partial-sig gap CONFIRMED - no event/accessor; getResult path key-path vs script-path for fallback)
    - packages/service/src/anchor-state.ts (the AnchorReadDTO shape to compose into the operator detail; the public anchor read stays byte-untouched, D-26)
    - .planning/phases/04-operator-cohort-monitoring/04-UI-SPEC.md (Members/Submissions/Co-sign/Anchor/Activity copy; LogEntry shape id/t/level/text; D-32 honest partial-sig line)
  </read_first>
  <action>
Extend the monitor fold with the remaining identity-carrying events: `update-received` ({cohortId, participantDid} -> mark the member `submitted` + wall-clock stamp a submission time; the payload carries NO timestamp so stamp at receipt, D-22), `validation-received` ({cohortId, participantDid, approved} -> mark `validated` when approved:true, and when approved:false mark that member rejected + append to the activity ring), `nonce-received` ({cohortId, participantDid} -> mark `nonce-sent` and count nonces), `message-rejected` (`{from, code, reason, cohortId}` -> mark that member `rejected` and append the reject to the activity ring), plus `signing-started`/`fallback-started`/`signing-complete`. Enrich the LIVE detail from the session: `collectedUpdates(id)`/`getCohort(id).pendingUpdates` for who submitted vs not, and `validationProgress(id)` for validated state. Add a bounded per-cohort activity ring (Map or array capped at a fixed `ACTIVITY_RING_SIZE` constant, e.g. 200, per cohort; reuse the evict idiom) delivered in the detail DTO as `activity: { id, t, level, text }[]` where `t` is the server wall-clock stamp and `level` is info/good/warn/bad by event kind (rejects/failures -> bad/warn). Extend the detail DTO with: `submissions: { did, submitted: boolean, at?: number }[]`, per-member `round: 'seated'|'submitted'|'validated'|'nonce-sent'|'rejected'`, `coSign: { noncesReceived: number; total: number; awaitingPartialSigs: boolean }` (set `awaitingPartialSigs:true` after the last nonce with signing not complete; NEVER a partial-sig count - honest limit D-32), `anchor: AnchorReadDTO`-shaped view composed from the injected anchor-state read for a broadcasting service (or `{ enabled:false, state:'none' }` for hermetic), and `fallback: { used: boolean; k?: number; n?: number }` from `getResult(id).path === 'script-path'` (D-33). Add an `exportRecord(cohortId)` returning the full detail DTO + activity log as a plain JSON-serializable object (off-chain artifacts referenced by hash, not inlined). Extend `monitor.spec.ts`: assert round-state transitions, that a rejected member and its reason land in the activity ring, that co-sign reports nonces k/n and flips `awaitingPartialSigs` with NO partial-sig number, that the activity ring is bounded, and that the anchor view for a hermetic service is `{ enabled:false, state:'none' }` (public anchor read untouched).
  </action>
  <verify>
    <automated>pnpm vitest run packages/service/tests/monitor.spec.ts && pnpm --filter @btcr2-aggregation/service exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `detail(id)` carries submissions (who/when), per-member `round` state, `coSign` with `noncesReceived`/`total`/`awaitingPartialSigs` (never a partial-sig count), an anchor view, a fallback flag, and a bounded `activity[]` of `{id,t,level,text}` with server wall-clock `t`.
    - A `message-rejected`/`validation-received{approved:false}` marks the member `rejected` AND appends a bad/warn entry to the activity ring.
    - After the last `nonce-received` with signing not yet complete, `coSign.awaitingPartialSigs === true` and no partial-sig count is present anywhere in the DTO.
    - The activity ring is bounded (oldest evicted past ACTIVITY_RING_SIZE); ended cohorts still project their captured members/round/anchor/activity (session GC survived).
    - The hermetic anchor view is `{ enabled:false, state:'none' }`; the public `GET /v1/anchor/:cohortId` shape is byte-untouched (freeze pin holds).
  </acceptance_criteria>
  <done>The detail DTO carries the full honest monitored depth (submissions, round state, honest co-sign, anchor, activity ring) plus an export record.</done>
</task>

<task type="auto">
  <name>Task 2: Drill-down depth render (stage timeline, member round-state + expander, submissions, co-sign, anchor sub-steps, activity log)</name>
  <files>packages/web/src/components/operator/CohortDetail.tsx, packages/web/src/components/operator/OperatorStageTimeline.tsx, packages/web/src/lib/operator.ts</files>
  <read_first>
    - packages/web/src/components/cohort/StageTimeline.tsx (the participant timeline visual treatment to mirror: completed = good dot, active = pulsing accent dot, pending = dim neutral; and the AnchorSubSteps pattern for Signed/Broadcast/Confirmed)
    - packages/web/src/components/operator/CohortDetail.tsx (the members/seats render from 04-01 to deepen)
    - packages/web/src/ui/primitives.tsx (the shared Expander promoted in 04-03; Mono/CopyField/Badge/StatusDot; LogPanel for the activity log)
    - .planning/phases/04-operator-cohort-monitoring/04-UI-SPEC.md (all drill-down copy: Members round-state chips, Submissions rows, Co-sign honest lines, Anchor sub-steps + hermetic + fallback lines, Activity log; the LogPanel timestamp renders the server wall-clock stamp not the participant elapsed fmtElapsed)
  </read_first>
  <action>
Widen the `CohortDetailDTO` type in `lib/operator.ts` to the new server shape (submissions, per-member round, coSign, anchor, fallback, activity). Create `components/operator/OperatorStageTimeline.tsx` mirroring the participant StageTimeline visual scaffolding but with the operator stage set advertise -> filling -> submissions -> co-signing -> anchor (funding stage is inserted for live cohorts by 04-06); do NOT import the participant component (different stage set). In `CohortDetail.tsx`, render top-to-bottom (D-05): the stage timeline, then Members (each row: shortened DID + copy-full, onboarding-model badge when known, per-member round-state chip Seated/Submitted/Validated/Nonce sent/Rejected with the fixed tones, and a `Technical detail` expander holding participantPk/communicationPk via Mono/CopyField), then Submissions (`{shortDid} submitted at {time}` / `{shortDid}: not yet submitted`, raw signed-update JSON behind a `Raw signed update` expander), then Co-sign (`{k} of {n} nonces received` + per-member Nonce sent chips; when `awaitingPartialSigs` render exactly `All {n} nonces received. Awaiting partial signatures.` with NO count), then Anchor (reuse the AnchorSubSteps pattern Signed -> Broadcast (txid Mono + `View on explorer` accent link) -> Confirmed, `Anchored` only at confirmed; hermetic services render `This no-broadcast service does not publish to Bitcoin, so there is no on-chain anchor to show.`; fallback renders `This cohort anchored via the k-of-n fallback path with {k} of {n} signatures.`), then Activity (reuse `LogPanel` at a fixed height with internal scroll; the timestamp column renders the server wall-clock `t`, not the participant elapsed `fmtElapsed`; empty renders `No activity yet.`). All copy verbatim from 04-UI-SPEC.md, em-dash-free.
  </action>
  <verify>
    <automated>pnpm --filter @btcr2-aggregation/web exec tsc --noEmit && pnpm --filter @btcr2-aggregation/web build</automated>
  </verify>
  <acceptance_criteria>
    - The drill-down renders the stage timeline at top, then Members (round-state chips + pubkeys behind the `Technical detail` expander), Submissions (who/when + `Raw signed update` expander), Co-sign, Anchor sub-steps, and the Activity log, top-to-bottom per D-05.
    - The co-sign section renders `{k} of {n} nonces received` and, in the partial-sig window, exactly `All {n} nonces received. Awaiting partial signatures.` with no invented count.
    - Anchor sub-steps show Signed/Broadcast/Confirmed with `Anchored` only at confirmed; a hermetic service renders the no-on-chain-anchor line; a fallback anchor renders the k-of-n fallback line.
    - The activity log renders server wall-clock timestamps + tone by level inside a fixed-height internal scroll; empty renders `No activity yet.`
    - No em-dash in any new copy string; web build green.
  </acceptance_criteria>
  <done>The drill-down renders the full honest monitored depth for one cohort.</done>
</task>

<task type="auto">
  <name>Task 3: Gated per-cohort JSON export (route + download)</name>
  <files>packages/service/src/hono-adapter.ts, packages/service/src/monitor.ts, packages/web/src/lib/operator.ts, packages/web/src/components/operator/CohortDetail.tsx, packages/service/tests/monitor.spec.ts</files>
  <read_first>
    - packages/service/src/hono-adapter.ts (the gated `if (operatorAuth) { if (operatorCohorts) { ... } }` block where the detail read was registered; register the export sibling there so it inherits requireSameOrigin + requireOperator)
    - packages/service/src/monitor.ts (the exportRecord(cohortId) added in Task 1)
    - .planning/phases/04-operator-cohort-monitoring/04-UI-SPEC.md (Export card copy: button `Download monitoring record (JSON)`, help line)
  </read_first>
  <action>
In `hono-adapter.ts`, inside the gated cohort block, register `app.get('/v1/operator/cohorts/:id/export', ...)`: validate the `:id` shape (same `/^[0-9a-zA-Z-]{1,64}$/` guard, 400 on bad shape), then return the monitor's `exportRecord(id)` as JSON with `Content-Disposition: attachment; filename="cohort-<id>.json"` where the filename is built ONLY from the already-shape-validated id (no user-controlled header content). Register it AFTER the requireOperator prefix guard so anonymous callers get 401 first (V5, no existence oracle). It is a plain gated GET, no new auth surface (D-34). In `lib/operator.ts`, add a `fetchExport(baseUrl, id)` (or reuse the download by opening the gated URL) - prefer a fetch that returns the JSON blob and triggers a client download so the session cookie rides same-origin. In `CohortDetail.tsx`, add the Export section: a ghost `Download monitoring record (JSON)` button plus the help line `Downloads exactly what this page shows, plus the activity log. Off-chain artifacts stay referenced by hash at /cas/.` Extend `monitor.spec.ts` with an app.request assertion that the export route 401s anonymously and returns the export JSON within a session.
  </action>
  <verify>
    <automated>pnpm vitest run packages/service/tests/monitor.spec.ts && pnpm --filter @btcr2-aggregation/web build && pnpm --filter @btcr2-aggregation/service exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `GET /v1/operator/cohorts/:id/export` returns 401 anonymously, 400 on a bad id shape, and within a session returns the monitoring DTO + activity log JSON with a `Content-Disposition` filename built only from the validated id.
    - The Export section renders the `Download monitoring record (JSON)` button + the exact help line; clicking downloads the record.
    - Off-chain artifacts are referenced by hash (not inlined) in the export.
    - No em-dash in any new copy; gate + web build green.
  </acceptance_criteria>
  <done>A gated per-cohort JSON export downloads exactly what the drill-down shows plus the activity log.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detail/export DTO -> operator | Member DIDs, pubkeys, raw signed-update JSON, and the activity log cross to the operator only; no recovery-key value is ever serialized. |
| honesty boundary | The unobservable partial-signature leg must never be rendered as a count; positive terminal claims only from observed facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04-01 | Information Disclosure | export/detail DTO | high | mitigate | Detail + export serialize member DIDs/pubkeys/raw updates + activity, all operator-gated (requireOperator); the recovery-key value is never included (only its state, disclosed by 04-06); off-chain artifacts referenced by hash. |
| T-04-04-02 | Information Disclosure | Content-Disposition filename | medium | mitigate | Filename built only from the shape-validated cohort id (`/^[0-9a-zA-Z-]{1,64}$/`), no user-controlled header content. |
| T-04-04-03 | Denial of Service | activity ring | medium | mitigate | Per-cohort ring bounded at a fixed ACTIVITY_RING_SIZE with oldest-first eviction; reads are pure projections. |
| T-04-04-04 | Repudiation | co-sign / anchor claims | high | mitigate | Honesty rule (prohibition): no invented partial-sig count, `Anchored` only at observed confirmation, fallback disclosed plainly (D-18/D-32/D-33). |
</threat_model>

<verification>
- `pnpm vitest run packages/service/tests/monitor.spec.ts` green (round state, activity ring bound, honest co-sign, hermetic anchor, export gating).
- `pnpm --filter @btcr2-aggregation/web build` + `pnpm --filter @btcr2-aggregation/service exec tsc -b` green.
- Manual: a live drill-down shows submissions/round state/co-sign/anchor/activity; the partial-sig window shows the honest line with no count; export downloads the record.
- No em-dash in authored copy.
</verification>

<success_criteria>
SVC-03 criteria 2 and 3 hold on the drill-down: the operator sees submissions + honest co-sign progress (with the honest partial-sig limit) and honest anchor status per cohort, plus the per-cohort activity log and a JSON export.
</success_criteria>

<output>
Create `.planning/phases/04-operator-cohort-monitoring/04-04-SUMMARY.md` when done.
</output>

