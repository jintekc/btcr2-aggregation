---
phase: 05-operator-cohort-lifecycle-control
plan: 27
type: execute
wave: 7
gap_closure: true
depends_on: ["05-21", "05-22", "05-23", "05-24", "05-25", "05-26"]
files_modified:
  - e2e/browser-operator.ts
  - packages/web/src/components/cohort/CompletionSummary.tsx
  - packages/web/tests/completion-summary.spec.ts
  - packages/service/tests/operator-auth-secure.spec.ts
  - .planning/todos/pending/2026-07-30-live-regtest-browser-leg-for-the-reflected-outcome.md
  - .planning/phases/05-operator-cohort-lifecycle-control/deferred-items.md
  - .planning/phases/05-operator-cohort-lifecycle-control/05-UAT-PROCEDURES.md
  - .planning/phases/05-operator-cohort-lifecycle-control/05-UAT.md
autonomous: true
requirements: [SVC-04, SVC-05, PART-05, PART-06]
must_haves:
  truths:
    - "A real Chromium page proves the one-way broadcast control is absent on a hermetic boot, so the unit render matrix from 05-22 has an independent witness on the shipped bundle (audit #27)."
    - "The operator console heading is pinned, and the fact that it CANNOT discriminate signed-in from signed-out is recorded rather than left implied, with the UAT procedure's citation corrected to name the control that actually proves sign-in (audit #3)."
    - "The honest-success sentence on the participant's completion summary has a positive hermetic pin, so renaming it or deleting its arm can no longer make a hermetic browser leg MORE green (audit #28)."
    - "The live regtest browser leg that #28 genuinely needs is FILED as a todo with its exact shape, rather than described as done."
    - "The operator session cookie is exercised with its Secure attribute on, which no existing call site does."
    - "`05-UAT.md` is reconciled once, from the ledger: every clause covered by automation is cited, and what remains is the honest residue of owner judgment, one batched copy read, one wallet pass, and the legs that need a real chain."
  prohibitions:
    - statement: "MUST NOT delete a UAT test without a ledger row citing the automation that replaced each of its clauses. A test removed with an uncited clause is a claim the round did not earn."
      status: pending
      verification: test
    - statement: "MUST NOT change the operator console heading or the login heading to make them discriminate. Whether the signed-in console deserves its own heading is a copy question for the owner, not a coverage fix; record it and leave the strings alone."
      status: pending
      verification: test
    - statement: "MUST NOT weaken or remove the existing NEGATIVE assertion in the participant browser leg that fails if the reflected-outcome copy appears on a hermetic run. The positive pin is added beside it; the pair is the point."
      status: pending
      verification: test
    - statement: "MUST NOT claim the live regtest browser leg exists, is scheduled, or is partially built. It is filed. The SUMMARY says filed."
      status: pending
      verification: test
    - statement: "MUST NOT force the residue to a target count. The expected shape is four owner decisions, one batched copy read and one wallet pass; if the honest result differs, record the difference and its reason rather than merging or splitting items to hit a number."
      status: pending
      verification: test
  artifacts:
    - path: e2e/browser-operator.ts
      provides: "the hermetic-boot absence check for the one-way broadcast control, and the heading assertion beside the control that actually proves sign-in"
      contains: "Disable broadcast"
    - path: packages/web/tests/completion-summary.spec.ts
      provides: "the positive hermetic pin on the honest-success sentence"
      contains: "reflected"
    - path: packages/service/tests/operator-auth-secure.spec.ts
      provides: "the first exercise of the session cookie with its Secure attribute on"
      contains: "Secure"
    - path: .planning/phases/05-operator-cohort-lifecycle-control/05-UAT.md
      provides: "the reconciled manual list: the honest residue plus a fully cited automation ledger"
      contains: "Retired: covered by automation"
  key_links:
    - from: packages/web/src/components/cohort/CompletionSummary.tsx
      to: packages/web/tests/completion-summary.spec.ts
      via: "the honest-success sentence has exactly one definition and one call site, both pinned"
      pattern: "reflected"
    - from: .planning/phases/05-operator-cohort-lifecycle-control/05-UAT.md
      to: .planning/phases/05-operator-cohort-lifecycle-control/05-UAT-PROCEDURES.md
      via: "a retired test's procedure is not deleted; the citation is corrected so the procedure matches what is now automated"
      pattern: "Retired"
---

## Gap source

Closes **#27's browser leg (entry 16, Medium)**, **#28 (entry 19, Medium, not phase 5)** and
**#3 (entry 24, Low, not phase 5)** from `05-AUDIT-2.md`, plus the higher-value adjacent gap entry 24
names (the session cookie's Secure attribute, never exercised true by any of its fourteen call
sites), and performs the round's single reconciliation pass over `05-UAT.md`.

Round-wide rules are stated in full in `05-21-PLAN.md` and apply here unchanged.

This plan depends on all six earlier plans, because the reconciliation reads the ledger they wrote.

**Entry 24's adjacent gap is included and labelled.** The Secure-attribute row is not one of the 24
confirmed defects; the audit raises it as the higher-value gap beside the heading it was actually
reporting. It is cheap, it is real (the attribute is defaulted on at boot and asserted nowhere), and
it is labelled as adjacent in the SUMMARY so the round's defect count is not inflated.

<objective>
Give the shipped bundle an independent browser witness for the one-way control, give the participant's
honest-success sentence a positive pin, correct a UAT procedure that cites a heading which proves
nothing, and reconcile the manual test list down to what genuinely needs a human.

Purpose: `05-UAT.md` lists 18 manual procedures. A coverage triage found 17 of the 18 human-only
claims were automatable, and this round automated most of them. A manual list that still claims 18
items after that is as inaccurate as the coverage claims this round was written to correct, in the
other direction.

Output: two browser assertions, two new specs, one filed todo, and a `05-UAT.md` whose remaining
items are all genuine judgment calls or genuinely un-hermetic legs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-operator-cohort-lifecycle-control/05-AUDIT-2.md
@.planning/phases/05-operator-cohort-lifecycle-control/05-UAT.md
@e2e/browser-operator.ts
@packages/web/src/components/cohort/CompletionSummary.tsx
@packages/web/src/components/operator/OperatorConsole.tsx
@packages/web/src/components/operator/LoginPanel.tsx
@packages/service/src/operator-auth.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Give the shipped bundle a browser witness for the one-way control, and stop a heading from standing in for proof of sign-in</name>
  <files>e2e/browser-operator.ts, packages/service/tests/operator-auth-secure.spec.ts, .planning/phases/05-operator-cohort-lifecycle-control/05-UAT-PROCEDURES.md</files>
  <read_first>
    - e2e/browser-operator.ts lines 140 through 175 (the sign-in sequence, which already waits on a signed-in-only control rather than on a heading) and lines 235 through 250 (the count-of-zero idiom this task reuses for an absence check)
    - packages/web/src/components/operator/OperatorConsole.tsx around line 130 and packages/web/src/components/operator/LoginPanel.tsx around line 25 (the two identical headings)
    - packages/service/src/operator-auth.ts around lines 244 through 258 (the cookie attributes on a successful login) and packages/service/src/index.ts around line 696 (where the secure flag defaults on)
    - packages/service/src/operator-auth.spec.ts around lines 110 through 120 (the existing cookie row, which asserts three attributes and not the fourth)
    - .planning/phases/05-operator-cohort-lifecycle-control/05-UAT-PROCEDURES.md around lines 2431 and 2608 (the two places that cite the console heading as the observable proof of sign-in)
    - .planning/phases/05-operator-cohort-lifecycle-control/05-AUDIT-2.md, entries 16 and 24
  </read_first>
  <behavior>
    - Test: on the hermetic boot the browser leg already performs, the one-way broadcast control is ABSENT. Assert a count of zero, reusing the file's existing absence idiom, and fail with a message naming the mode so a future failure is self-explaining.
    - Test: after sign-in, the console heading is asserted by exact text, AND a signed-in-only control is asserted present, with a comment stating that the heading alone cannot discriminate because the login panel renders the identical string.
    - Test: a successful operator login on a service booted with the secure cookie flag on sets the Secure attribute, asserted from the response's set-cookie header alongside the three attributes already covered.
    - Test: the same login on a service booted with the flag off does NOT set it, so the row is proven to be reading the flag rather than a constant.
    - Mutation, run explicitly: delete the mode half of the kill-switch guard in the component. The browser leg's absence check must fail. If the browser leg cannot be run in this environment, record that plainly and note that 05-22's unit matrix already covers the same mutation.
  </behavior>
  <action>
In `e2e/browser-operator.ts`, add the absence check for the one-way broadcast control immediately after the sign-in confirmation, reusing the count-of-zero idiom the file already uses for the explorer link. The boot this leg uses is hermetic, so the control must not exist; a danger-toned one-way control appearing there is verbatim the fail signal the UAT procedure describes.

Add the heading assertion beside the existing signed-in-only wait, not instead of it. Assert the heading's exact text so a rename is caught, and write the comment that makes the row honest: this heading is identical to the one the login panel renders, so it is a copy pin and not a sign-in proof, and the control beside it is the sign-in proof. Do not change either heading string. Whether the signed-in console deserves its own heading is a copy question for the owner; record it in the SUMMARY as a question rather than answering it here.

Create `packages/service/tests/operator-auth-secure.spec.ts` with the two cookie rows. Boot the service through its real construction path for each flag value and read the set-cookie header off a successful login response. Label this in the file docstring as an ADJACENT hardening row rather than one of the 24 confirmed defects, and say why it earned a place: the attribute is defaulted on at boot, it is the one that keeps the session off plaintext, and none of the fourteen call sites exercises it true.

Then correct the two citations in `05-UAT-PROCEDURES.md`. They name the console heading as the observable proof of sign-in. Replace the observable with the signed-in-only control the browser leg actually waits on, and add one sentence recording why: the heading is identical on both sides of the sign-in boundary, so it can never have been the proof. Correct only those two citations; do not restructure the procedure document.

Run the mutation, or record why the browser leg could not be run here.
  </action>
  <verify>
    <automated>pnpm vitest run packages/service/tests/operator-auth-secure.spec.ts && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/browser-operator.ts` carries the hermetic absence check for the one-way control and the heading assertion beside the signed-in-only wait.
    - Neither heading string changed: `git diff packages/web/src/components/operator/OperatorConsole.tsx packages/web/src/components/operator/LoginPanel.tsx` is empty.
    - The Secure-attribute rows exist for both flag values and are labelled adjacent in the file docstring.
    - The two `05-UAT-PROCEDURES.md` citations name the signed-in-only control, and the document is otherwise unrestructured.
    - `pnpm e2e:browser:operator` is run and its result recorded, or its inability to run here is recorded plainly.
  </acceptance_criteria>
  <done>The shipped bundle has a browser witness for the one-way control, and no procedure cites a heading as proof of sign-in.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the honest-success sentence positively, and file the live leg it really needs</name>
  <files>packages/web/src/components/cohort/CompletionSummary.tsx, packages/web/tests/completion-summary.spec.ts, .planning/todos/pending/2026-07-30-live-regtest-browser-leg-for-the-reflected-outcome.md, .planning/phases/05-operator-cohort-lifecycle-control/deferred-items.md</files>
  <read_first>
    - packages/web/src/components/cohort/CompletionSummary.tsx lines 150 through 185 (the resolve-outcome arms: resolving, failed, reflected, hermetic-genesis, and whatever follows)
    - e2e/browser-participant-cohort.ts around line 255 (the negative assertion that FAILS if the reflected copy appears on a structurally hermetic run, which stays)
    - packages/web/tests/support/render.tsx (the harness, its limits, the hoisting recipe and the typed-holder rule) and packages/web/tests/support/render.spec.tsx (the trap demonstrated)
    - packages/web/src/components/cohort/CompletionSummary.tsx lines 44 through 70 and around line 297 (both network reads, each passing the store's network through the registry, which is why the fixture needs a real network name)
    - .planning/phases/05-operator-cohort-lifecycle-control/05-AUDIT-2.md, entry 19 and the "New infrastructure required" note
    - .planning/todos/pending/ (an existing todo, for the file shape this round follows)
  </read_first>
  <behavior>
    - Test: the honest-success sentence is produced by one exported builder, and its output is pinned for the with-version and the without-version cases.
    - Test: the builder has exactly one call site in the component, so the pinned sentence is the shipped sentence.
    - Test: rendering the completion summary in the reflected round-trip state produces that sentence. The component reads the participant store, and a static render sees `getInitialState()`, so the fixture MUST be installed through the selector-faithful fake from `packages/web/tests/support/render.tsx`, following the hoisting recipe in that harness docstring, with the holder typed as a partial of the exported participant state, and never through `useParticipant.setState`, which would render the pre-resolve state and pass whatever the arm does. The fixture's network field must carry a REAL registry network name, because the component resolves it through the network registry twice and an unknown value will not render; that is a two-word requirement, not a reason to drop the row. Pair it with a differing-fixture row so the block carries an anti-vacuity control. DROP this row ONLY if the reflected arm cannot be reached without faking the component's own resolve logic, which would pin the stub, and record that reason specifically in the SUMMARY. A render that merely throws on a first attempt is a fixture bug, not grounds to drop.
    - Test: the hermetic-genesis arm keeps its own distinct sentence, so the reflected pin is proven not to be matching whatever the component happens to render.
    - Mutation, run explicitly: delete the reflected arm. The builder's call-site count assertion and, if present, the render row must go RED.
  </behavior>
  <action>
In `packages/web/src/components/cohort/CompletionSummary.tsx`, extract the reflected-outcome sentence into an exported builder taking the optional version, and call it from the arm. This is a structural extraction, not a copy change: the rendered words must be byte-identical afterwards, including the interpolated version clause and its spacing. Add a docstring saying why it is exported: this is the honest-success half of the pair whose dishonest half is already guarded, and a positive pin is the only thing that stops a rename from making a hermetic browser leg MORE green. Cite `05-AUDIT-2.md` entry 19.

Create `packages/web/tests/completion-summary.spec.ts` with the rows from the behavior list, including the call-site count assertion over the component source and the hermetic-genesis contrast row.

Leave the negative assertion in `e2e/browser-participant-cohort.ts` exactly as it is. That file is not in `files_modified`, and it must not be. The pair is the point: the hermetic leg fails if the success copy appears, and the unit pin fails if the success copy stops existing.

Then file the live leg. Create the pending todo naming exactly what is needed: a Chromium page bolted onto the existing live UAT harness, a new e2e script entry, and the reason no existing harness can reach the arm (every browser harness in this repo is hermetic by construction, so a genuine live success is unreachable). State plainly that this is FILED and not built, and add a matching row to the phase's `deferred-items.md` so it is visible from the phase record rather than only from the todo directory.

Run the mutation, observe, revert, record.
  </action>
  <verify>
    <automated>pnpm vitest run packages/web/tests/completion-summary.spec.ts && pnpm --filter @btcr2-aggregation/web build</automated>
  </verify>
  <acceptance_criteria>
    - The reflected-outcome sentence has one exported builder and one call site, both pinned, and the rendered words are byte-identical to before the extraction.
    - The hermetic-genesis contrast row exists.
    - The render row exists, seeded through the fake with a typed holder and a real registry network name, and paired with a differing-fixture anti-vacuity row. If absent, the SUMMARY names the specific unreachable-without-stubbing reason, not a fixture problem.
    - `e2e/browser-participant-cohort.ts` is unchanged: `git diff e2e/browser-participant-cohort.ts` is empty.
    - The pending todo exists, names the harness, the script entry and the reason, and says FILED rather than done. `deferred-items.md` carries the matching row.
    - The mutation is observed RED and reverted, with output recorded.
  </acceptance_criteria>
  <done>The honest-success sentence is positively pinned, and the live leg it needs is filed rather than claimed.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile 05-UAT.md down to the honest residue</name>
  <files>.planning/phases/05-operator-cohort-lifecycle-control/05-UAT.md</files>
  <read_first>
    - .planning/phases/05-operator-cohort-lifecycle-control/05-UAT.md in full: the 18 item Tests list, the ledger 05-21 opened and plans 05-22 through 05-26 appended to, the Automated Coverage table, and the Summary counts
    - The SUMMARY files of 05-21 through 05-26, for the spec names each plan actually landed, so citations name real describe blocks rather than intended ones
    - .planning/phases/05-operator-cohort-lifecycle-control/05-AUDIT-2.md, the "How this was found" section (the coverage triage that found 17 of 18 human-only claims automatable)
  </read_first>
  <behavior>
    - Verify: every ledger citation added by 05-21 through 05-26 names a spec file and a describe or test name that actually exists in the tree. A citation that does not resolve is a false claim of coverage and must be corrected or removed.
    - Verify: every test moved out of the Tests list has a ledger row for EVERY clause of its expected text, not merely for its headline.
    - Verify: the Summary counts match the reconciled list.
  </behavior>
  <action>
Do the reconciliation in one pass, in this order, so nothing is retired on an intention.

First, resolve every ledger citation against the tree. For each row, open the cited spec and confirm the named block exists. Correct any that drifted; delete any that cannot be resolved and record the deletion in the SUMMARY. This step is what keeps the ledger from becoming the same kind of overstatement the round exists to correct.

Second, walk each of the 18 tests clause by clause against the resolved ledger. A test moves into `## Retired: covered by automation` only when every clause of its expected text has a resolved row. Where a test is mostly covered but retains a genuine human clause, do not retire it: NARROW it. Rewrite its expected text down to the clause that still needs a human, and add ledger rows for the clauses you removed from it. A narrowed test stays in `## Tests` with its reduced scope stated.

Third, classify what remains. The expected shape, from the coverage triage, is four owner decisions, one batched copy read and one wallet pass, plus the legs that need a real chain or a real third-party host. Group the residue under headings that name the KIND of human input each needs, so the owner can see at a glance which items are judgment and which are environment. The known judgment calls are: the deliberate divergence from the literal wording of roadmap success criterion 1, where close is an automatic nth-seat lock rather than a button; whether the reserved-anchor relabelling reads as honest rather than as a regression; whether the disclosed cost of the broadcast stand-down is acceptable; and the visual pass over the three console surfaces at a real viewport. The known environment items are: the real third-party esplora hosts, the real wallet PSBT round trip, and the live boot with broadcasting.

Do not force the count. If the honest result is five judgment items or three, record the difference and the reason. The round brief's shape is a target derived from a triage, not a specification.

Fourth, handle test 18 explicitly. It was classified as a backstop because the settle-race timing cannot be reliably driven from a harness, which means it cannot be reliably driven by hand either. Decide honestly between keeping it as a stated backstop with no runnable procedure and retiring it as un-runnable with the backstop recorded, and say which you chose and why. Do not leave it sitting in the list as if it were a procedure someone can follow.

Fifth, rewrite the Summary counts to match, and update the Automated Coverage narrative so its schema-drift note reflects what this round changed. The note about plans 05-08 through 05-14 using a drifted coverage schema stays: this round did not fix that, and the note should say so rather than being quietly dropped.

Sixth, add a short paragraph at the top of the Tests section recording the provenance: this list was reduced by the second gap-closure round after a coverage triage found 17 of 18 human-only claims automatable, the retired items are cited below rather than deleted, and the remaining items are the ones that genuinely need a person.
  </action>
  <verify>
    <automated>pnpm test && pnpm lint && pnpm --filter @btcr2-aggregation/web build</automated>
  </verify>
  <acceptance_criteria>
    - Every ledger citation resolves to a spec file and a block that exists; unresolvable rows are corrected or removed and the removals are recorded.
    - No test was retired without a resolved row for every clause of its expected text.
    - Narrowed tests state their reduced scope, and the clauses removed from them appear in the ledger.
    - The residue is grouped by the kind of human input each item needs, and any deviation from the expected shape is recorded with its reason.
    - Test 18's disposition is decided explicitly and justified.
    - The Summary counts match the reconciled list, and the coverage-schema-drift note is retained rather than dropped.
    - `grep -rlP '\x{2014}' .planning/phases/05-operator-cohort-lifecycle-control/05-UAT.md` lists no files.
    - `pnpm test`, `pnpm lint` and `pnpm --filter @btcr2-aggregation/web build` are green, and the final test count is recorded beside the 1049 baseline for the whole round.
  </acceptance_criteria>
  <done>`05-UAT.md` lists only what genuinely needs a person, and everything it dropped is cited to the automation that replaced it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network to operator session cookie | The session identifier must not travel in plaintext, which is what the Secure attribute enforces. |
| participant expectation to rendered outcome | The honest-success sentence is the one that tells a participant their update actually landed. |
| project record to reader | A manual test list and a coverage ledger are claims about what was verified, and an overstated one is the defect class this round exists to correct. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-27-01 | Information Disclosure | the operator session cookie | high | mitigate | Exercise the Secure attribute on and off, so the attribute that keeps the session off plaintext is asserted rather than defaulted and forgotten. |
| T-05-27-02 | Tampering | the one-way broadcast control's mode guard | medium | mitigate | An independent browser witness on the shipped bundle, beside 05-22's unit matrix. |
| T-05-27-03 | Repudiation | the honest-success sentence | medium | mitigate | A positive pin beside the existing negative hermetic assertion, so neither direction can drift alone. |
| T-05-27-04 | Repudiation | the manual test list | medium | mitigate | Every retirement is cited to a resolved spec block, and every unresolvable citation is removed and recorded. |
| T-05-27-SC | Tampering | npm install surface | high | mitigate | No package install in this plan. Empty lockfile diff is an acceptance criterion of the round. |
</threat_model>

<verification>
- `pnpm test` green, final round count recorded against the 1049 baseline.
- `pnpm lint` green.
- `pnpm --filter @btcr2-aggregation/web build` green.
- `pnpm e2e:gate` green, and `pnpm e2e:browser:operator` run with its result recorded or its unavailability recorded plainly.
- Every ledger citation resolved against the tree.

### Read-only regression runs

`e2e/browser-participant-cohort.ts` and `packages/service/src/operator-auth.spec.ts` are read but NOT
modified and are not in `files_modified`. The participant leg's negative assertion is load-bearing
and stays.

### What this round did not close

Two things, both recorded rather than papered over. The live regtest browser leg that entry 19 needs
for real closure is filed, not built. And a DOM environment, which would turn roughly eight
initial-render assertions into interaction tests, was deliberately not adopted; the reasoning and
what it would buy are recorded in `packages/web/tests/support/render.tsx` and in 05-21's SUMMARY.
</verification>

<success_criteria>
The manual UAT list contains only items that genuinely need a person, each retirement is cited to
automation that exists, and the round's last three defects fail the suite when mutated.
</success_criteria>

<output>
Create `.planning/phases/05-operator-cohort-lifecycle-control/05-27-SUMMARY.md` when done.
</output>
