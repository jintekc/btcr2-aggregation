---
phase: 05-operator-cohort-lifecycle-control
plan: 22
type: execute
wave: 2
gap_closure: true
depends_on: ["05-21"]
files_modified:
  - packages/web/tests/service-controls.spec.ts
  - packages/web/src/components/operator/HealthStrip.tsx
  - packages/web/src/components/operator/CohortDetail.tsx
  - .planning/phases/05-operator-cohort-lifecycle-control/05-UAT.md
autonomous: true
requirements: [SVC-04]
must_haves:
  truths:
    - "The health strip's mode chip renders its three labels from the SERVED mode and from nothing else, so a hermetic claim can never be painted onto a live service, and its pre-first-read fallback renders no mode claim at all (audit #25, D-14/D-17)."
    - "Relabelling any of the three modes, or rewriting the mode chip expression to read the broadcast kill switch, fails the suite."
    - "The eight test-peer copy constants are pinned as literals with an em-dash guard, and the seat-exhausted refusal reason is pinned byte for byte on the web side as well as the service side (audit #22 and #10, UI-SPEC E11)."
    - "A cohort with no seats left renders the test-peer control DISABLED beside the refusal reason, asserted against real rendered output rather than a source grep (audit #10)."
    - "The dismissal confirm body is pinned by exact equality on all three of its sentences, so the promise it makes about what a dismissal costs cannot narrow silently (audit #20, D-15)."
    - "The one-way broadcast kill switch renders ONLY on a live service that still has broadcasting available, asserted by rendering the controls card across every service mode (audit #27, D-14)."
  prohibitions:
    - statement: "MUST NOT assert the mode chip through a source-containment grep where a render is available. 05-21 added the harness precisely so this surface stops being pinned by reading its own source text."
      status: pending
      verification: test
    - statement: "MUST NOT import anything from `packages/service` into a `packages/web` spec. The web package does not depend on the service package, and adding that dependency to make a byte-identity claim testable would couple the browser bundle to the server for the sake of a test."
      status: pending
      verification: test
    - statement: "MUST NOT change any shipped copy string. This plan pins what ships; if a pin and the shipped string disagree, the pin is wrong unless the string carries an em-dash, which is a house-style defect and is fixed at the source."
      status: pending
      verification: test
    - statement: "MUST NOT widen `broadcastControlState` or `serviceControlsView` to make a render assertion easier. The selectors are already covered; what is missing is proof the component consults them."
      status: pending
      verification: test
  artifacts:
    - path: packages/web/src/components/operator/HealthStrip.tsx
      provides: "the exported MODE_LABEL map, so the single most consequential piece of copy on this surface is assertable"
      contains: "export const MODE_LABEL"
    - path: packages/web/src/components/operator/CohortDetail.tsx
      provides: "the exported TestPeerAction component, so the seat-exhausted disabled wiring is reachable by a static render"
      contains: "export function TestPeerAction"
    - path: packages/web/tests/service-controls.spec.ts
      provides: "the mode-chip matrix, the test-peer copy family with its em-dash guard, the exact dismissal body, and the kill-switch mode guard, all rendered"
      contains: "renderStatic"
  key_links:
    - from: packages/web/src/components/operator/HealthStrip.tsx
      to: packages/web/tests/service-controls.spec.ts
      via: "the served mode is the only input to the rendered mode chip, proven by rendering every mode and every kill-switch combination"
      pattern: "MODE_LABEL"
    - from: packages/web/src/components/operator/CohortDetail.tsx
      to: packages/web/tests/service-controls.spec.ts
      via: "the disabled control and its reason render together from one seat count"
      pattern: "TestPeerAction"
---

## Gap source

Closes **#25 (entry 6, High)**, **#22 and #10 (entry 14, Medium)**, **#27 (entry 16, Medium)** and
**#20 (entry 22, Low)** from `05-AUDIT-2.md`. This is the audit's own second group,
`packages/web/tests/service-controls.spec.ts`, which it ranks second by defects closed per file
touched, with #27 folded in because 05-21's render harness now closes it in the unit gate rather
than only in a browser leg.

Round-wide rules are stated in full in `05-21-PLAN.md` and apply here unchanged: singleton waves,
the mutate-observe-revert proof discipline, the 1049 test baseline, the three-command gate, unsigned
commits with no trailers, no em-dash, and new tests under `tests/`.

**Why #27 moves into the unit gate.** The audit proposed closing it in `e2e/browser-operator.ts`
with a count-of-zero check, because no spec in this repo could render a component. That is no longer
true. The browser leg is still added, in 05-27, as a second and independent witness on a real
Chromium page; this plan closes the defect where it is cheapest to keep green.

<objective>
Stop the operator console's most consequential copy and its two mode-gated controls from being
changeable without a test noticing.

Purpose: the health strip's mode chip is the sentence that tells an operator whether this service
touches the chain. The semantic feed behind it is genuinely covered end to end, from
`monitor.serviceHealth` over the wire into `state.health`. Only the last hop, mode to pixel, is not,
and `05-VALIDATION.md:76` records that hop's verify method as `build`, which is itself the
admission. The same last-hop gap covers the kill switch's mode guard, the test-peer refusal, and the
dismissal body's promise.

Output: three exported symbols, one render matrix per control, and four defects that now fail the
suite when mutated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-operator-cohort-lifecycle-control/05-AUDIT-2.md
@.planning/phases/05-operator-cohort-lifecycle-control/05-UAT.md
@packages/web/src/components/operator/HealthStrip.tsx
@packages/web/src/components/operator/ServiceControls.tsx
@packages/web/src/components/operator/CohortDetail.tsx
@packages/web/src/stores/operator.ts
@packages/web/tests/service-controls.spec.ts
@packages/web/tests/support/render.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the mode chip against every served mode, and prove it reads nothing else</name>
  <files>packages/web/src/components/operator/HealthStrip.tsx, packages/web/tests/service-controls.spec.ts</files>
  <read_first>
    - packages/web/src/components/operator/HealthStrip.tsx in full (the module-private `MODE_LABEL` map at the top, the `mode` / `broadcastOff` / `paused` derivations, and the chip row starting at the Badge that renders the mode)
    - packages/web/src/lib/operator.ts, the `ServiceMode` union, so the matrix is exhaustive by construction rather than by hand
    - packages/web/tests/service-controls.spec.ts around line 218 (where the existing service-control copy pins sit, which this task extends)
    - packages/web/tests/settings.spec.ts lines 203 through 213 (the em-dash guard idiom this round reuses)
    - .planning/phases/05-operator-cohort-lifecycle-control/05-AUDIT-2.md, entry 6
  </read_first>
  <behavior>
    - Test: `MODE_LABEL` carries exactly one entry per member of the `ServiceMode` union, and each label is pinned by exact equality.
    - Test: with no served health at all, the strip renders the pre-first-read fallback label and no mode claim, because presuming hermetic on a service that has not answered would be a false claim about whether it touches the chain.
    - Test: for each served mode, the rendered strip contains that mode's label and contains none of the other two labels.
    - Test: a served live mode with the broadcast kill switch engaged still renders the LIVE mode label, and renders the broadcast-off chip beside it rather than instead of it. This is the D-14 property in one assertion: the service really did boot live and its chain reads really are still live.
    - Test: the paused bit likewise renders its own chip beside the mode chip without altering the mode label.
    - Mutation, run explicitly: relabel one mode entry. The matrix must go RED.
    - Mutation, run explicitly: rewrite the mode chip expression so it renders the hermetic label whenever the broadcast switch is engaged. The live-plus-broadcast-off row must go RED.
  </behavior>
  <action>
Export `MODE_LABEL` from `packages/web/src/components/operator/HealthStrip.tsx`, keeping it where it is, beside the file's other chip labels. Add a short docstring saying why it is exported: it is the strip's most consequential label, a module-private record is unassertable, and the same reasoning already moved the chip presentation map out of `OperatorCohortList.tsx` in 05-20. Cite `05-AUDIT-2.md` entry 6.

In `packages/web/tests/service-controls.spec.ts`, add a describe block for the mode chip.

First pin the map itself: assert exact equality on each of the three labels, and assert the key set equals the `ServiceMode` union's members so a fourth mode cannot be added without a row. Derive the expected key set from a typed array of the union rather than hand-listing strings in a way the type checker cannot police.

Then render. Seed the operator store's `health` through `useOperator.setState` and render `HealthStrip` through `renderStatic` for each row in the behavior list. For each served mode assert both directions: the expected label is present AND the other two labels are absent. Asserting presence alone would pass a chip that rendered every label at once.

The pre-first-read row is a distinct assertion, not a fourth mode: seed `health` as undefined and assert the fallback label renders and that none of the three mode labels does.

The live-plus-broadcast-off row is the one that carries D-14. Seed a live mode with the broadcast bit set, and assert three things in one row: the live label is present, the hermetic label is absent, and the broadcast-off chip constant is present. That combination is exactly what a mutation to the chip expression breaks, and no single one of the three would catch it alone. Say so in a comment.

Then run both mutations from the behavior list, one at a time, observing RED and reverting each. Record the observed output in the SUMMARY.
  </action>
  <verify>
    <automated>pnpm vitest run packages/web/tests/service-controls.spec.ts && pnpm --filter @btcr2-aggregation/web build</automated>
  </verify>
  <acceptance_criteria>
    - `MODE_LABEL` is exported and its three values are pinned by exact equality, with the key set derived from the `ServiceMode` union.
    - Every served mode is rendered and asserted in both directions, present and absent.
    - The pre-first-read row asserts the fallback label and the absence of all three mode labels.
    - The live-plus-broadcast-off row asserts the live label present, the hermetic label absent, and the broadcast-off chip present, in one row.
    - Both mutations are observed RED and reverted, with output recorded.
    - No shipped copy string changed: `git diff packages/web/src/components/operator/HealthStrip.tsx` shows only the export keyword and the added docstring.
  </acceptance_criteria>
  <done>Relabelling a mode, or making the mode chip depend on the kill switch, fails the suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the test-peer copy family and render its seat-exhausted refusal</name>
  <files>packages/web/src/components/operator/CohortDetail.tsx, packages/web/tests/service-controls.spec.ts</files>
  <read_first>
    - packages/web/src/stores/operator.ts lines 296 through 337 (the whole test-peer copy set, including the three interpolating helpers and the seat-exhausted reason, plus the docstring stating the reason is byte-identical to the service's own 409 refusal)
    - packages/web/src/components/operator/CohortDetail.tsx lines 149 through 215 (`TestPeerAction`, the non-exported component holding the disabled control and the reason span)
    - packages/service/src/operator-cohorts.ts, the service-side seat-exhausted refusal reason and its existing pin in packages/service/tests/test-peers.spec.ts around line 407
    - packages/web/tests/settings.spec.ts lines 199 through 214 (the literal-pin plus em-dash-guard shape)
    - .planning/phases/05-operator-cohort-lifecycle-control/05-AUDIT-2.md, entry 14
  </read_first>
  <behavior>
    - Test: each of the eight test-peer constants is pinned. The five plain strings by exact equality; the three interpolating helpers by exact equality of their output for a representative seat count and, for the live line, a representative network name.
    - Test: every one of the eight is free of an em-dash, in the shape of the existing settings guard.
    - Test: the seat-exhausted reason is pinned as a literal on the web side, with a comment naming the service-side constant and its spec as the counterpart that makes the byte-identity claim real. The web spec must not import from the service package.
    - Test: `TestPeerAction` rendered with zero remaining seats produces a disabled control and the seat-exhausted reason.
    - Test: `TestPeerAction` rendered with a positive remaining count produces an enabled control and the help line naming that count, not the refusal reason.
    - Mutation, run explicitly: delete the disabled binding from the control. The zero-seat row must go RED.
    - Mutation, run explicitly: reword the seat-exhausted reason. Both the literal pin and the zero-seat render row must go RED.
  </behavior>
  <action>
Export `TestPeerAction` from `packages/web/src/components/operator/CohortDetail.tsx`. It already takes pure props and holds only its own confirm-open state, so exporting is a one word change. Add a sentence to its docstring saying why: the control's disabled binding and the reason beside it are a single fact rendered in two places, only reachable in the running app inside a drill-down that needs a whole seeded cohort, and a static render of the component itself is the cheapest honest witness. Cite `05-AUDIT-2.md` entry 14.

In `packages/web/tests/service-controls.spec.ts`, add a describe block for the test-peer surface.

Pin the copy first, following the existing settings-spec shape exactly: exact equality per constant, then one loop asserting no em-dash across the whole family. Include the three helpers by asserting their rendered output for a fixed seat count and network, so a change to an interpolation is caught as well as a change to the surrounding words.

For the seat-exhausted reason, add a comment stating the contract in words: this string is the sentence an operator reads before clicking, and the service's own 409 reason is the sentence they would read if they clicked anyway, and the two must be one string rather than two that can drift. Name the service-side constant and the spec that pins it. Do not import it. The web package does not depend on the service package, and adding that dependency for a test would couple the browser bundle to the server.

Then render `TestPeerAction` through `renderStatic` for the two seat counts in the behavior list. For zero remaining, assert the refusal reason is present, the help line is absent, and the control is disabled in the markup. For a positive count, assert the inverse. Two directions per row again, for the same reason as task 1.

Run both mutations, observe RED, revert, record.
  </action>
  <verify>
    <automated>pnpm vitest run packages/web/tests/service-controls.spec.ts && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - All eight test-peer constants are pinned, the three helpers through their output, and all eight pass an em-dash guard.
    - The web spec contains no import from `packages/service`: `grep -c "packages/service\|@btcr2-aggregation/service" packages/web/tests/service-controls.spec.ts` returns 0 for the import lines.
    - `TestPeerAction` is exported and rendered at zero and at a positive remaining count, each asserted in both directions.
    - Both mutations are observed RED and reverted, with output recorded.
    - `pnpm lint` is green.
  </acceptance_criteria>
  <done>The test-peer refusal copy and its disabled wiring both fail the suite when changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin the dismissal body exactly, gate the kill switch by mode, and record the UAT clauses</name>
  <files>packages/web/tests/service-controls.spec.ts, .planning/phases/05-operator-cohort-lifecycle-control/05-UAT.md</files>
  <read_first>
    - packages/web/src/stores/operator.ts lines 251 through 278 (`DISMISS_BODY` and the additive re-advertise line, with the docstring explaining why they are two constants)
    - packages/web/tests/service-controls.spec.ts around lines 220 through 235 (where the dismissal heading and both buttons already get exact equality while the body gets a partial containment check)
    - packages/web/src/components/operator/ServiceControls.tsx lines 181 through 250 (the store reads, `broadcastControlState`, and the kill-switch button guarded on the control state and on the confirm not being open)
    - packages/web/tests/service-controls.spec.ts lines 160 through 181 (the existing pure-selector rows for `broadcastControlState`, which this task leaves alone and complements)
    - .planning/phases/05-operator-cohort-lifecycle-control/05-AUDIT-2.md, entries 16 and 22
    - .planning/phases/05-operator-cohort-lifecycle-control/05-UAT.md, the ledger section 05-21 opened
  </read_first>
  <behavior>
    - Test: the dismissal body is pinned by exact equality across all three of its sentences, replacing the partial containment check. Deleting the phrase about the activity log, or the phrase scoping the clearance to this session, must fail.
    - Test: the additive re-advertise line keeps its own exact pin, so the two constants stay distinguishable.
    - Test: `ServiceControls` rendered on a hermetic service does NOT render the kill-switch control.
    - Test: `ServiceControls` rendered on a live service with broadcasting still available DOES render it.
    - Test: `ServiceControls` rendered on a live service whose switch is already engaged renders the one-way line instead of the control, so the control is proven gone rather than merely disabled.
    - Test: `ServiceControls` rendered on a live-no-broadcast service does NOT render the control, which is verbatim the fail signal the UAT procedure describes for a danger-toned one-way control appearing where the service will never broadcast.
    - Mutation, run explicitly: delete the mode half of the kill-switch guard. The hermetic and live-no-broadcast rows must go RED.
  </behavior>
  <action>
Promote the dismissal body's partial containment check to exact equality against the shipped constant. Keep the sentence-level containment assertions that already exist if they add readable intent, but the exact pin is what makes the promise unshrinkable. Add a one line comment saying what the two dropped phrases would cost if they went: without them the confirm reads as if the record were permanently destroyed server side, when in fact it clears one console for one session.

Then add the kill-switch render matrix. Seed the operator store's `health` for each row in the behavior list and render `ServiceControls` through `renderStatic`. Assert on the presence or absence of the shipped kill-switch label, and on the one-way line for the engaged row. Do not re-test `broadcastControlState` itself; those rows already exist and are load-bearing about the RULE. What this matrix adds is proof the component consults the rule.

Give the engaged row two assertions rather than one: the one-way line present AND the control label absent. A disabled-but-present control would satisfy a presence check on the line alone, and the shipped decision is that the control is gone, not disabled.

Run the mutation from the behavior list, observe RED, revert, record.

Then record this plan's UAT clauses in the `## Retired: covered by automation` ledger in `05-UAT.md`, following the rule 05-21 wrote above the table:

- Test 6, the clause that the paused chip sits beside the mode chip.
- Test 9, the clause that the zero-seat disabled reason reads correctly on the test-peer control.
- Test 11, the clause that the dismissal confirmation body discloses what dismissing costs.
- Test 17, the clause that the kill-switch control is offered only in a live boot mode with broadcasting available, and the clause that the health strip still reports the live boot mode with a separate warn chip beside it afterwards.

Do not remove any test from `## Tests`. None of these four is fully covered yet: test 6 still needs the public paused notice and the narrow-width wrap, test 9 still needs the live-cohort registration disclosure that 05-26 closes, test 11 still needs the expired-row fate read that 05-25 closes, and test 17 still needs a real LIVE plus BROADCAST boot. Leave the `## Summary` counts for 05-27.
  </action>
  <verify>
    <automated>pnpm vitest run packages/web/tests/service-controls.spec.ts && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - The dismissal body is pinned by exact equality, and the additive re-advertise line keeps its own exact pin.
    - `ServiceControls` is rendered across all four service-mode rows, and the engaged row asserts both the one-way line present and the control label absent.
    - The mode-half mutation is observed RED and reverted, with output recorded.
    - `05-UAT.md` carries the four new ledger rows with citations, and no test was removed from `## Tests`.
    - `grep -rlP '\x{2014}' packages/web/tests/service-controls.spec.ts packages/web/src/components/operator/HealthStrip.tsx packages/web/src/components/operator/CohortDetail.tsx` lists no files.
    - `pnpm test` is green and its count is recorded beside the previous plan's total.
  </acceptance_criteria>
  <done>The dismissal promise, the kill-switch mode guard, and the test-peer refusal all fail the suite when changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| served service health to rendered claim | The mode chip is the operator's only statement of whether this service moves real Bitcoin. |
| operator intent to one-way act | The broadcast kill switch cannot be undone from the console, so offering it where it does not apply is itself a hazard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-22-01 | Spoofing | the health strip mode chip | high | mitigate | Render every served mode and assert both presence and absence, including the live-plus-kill-switch row, so a hermetic claim cannot be painted onto a live service. |
| T-05-22-02 | Tampering | the kill-switch mode guard | medium | mitigate | Render the controls card across all four mode states and assert the control is absent on three of them. |
| T-05-22-03 | Repudiation | the dismissal confirm body | low | mitigate | Exact-equality pin on all three sentences, so the promise about what a dismissal costs cannot narrow silently. |
| T-05-22-SC | Tampering | npm install surface | high | mitigate | No package install in this plan. Empty lockfile diff is an acceptance criterion of the round. |
</threat_model>

<verification>
- `pnpm test` green, count recorded.
- `pnpm lint` green.
- `pnpm --filter @btcr2-aggregation/web build` green.
- Every mutation run recorded with its observed result.
- No shipped copy string changed. The only source edits are two export keywords and their docstrings.

### Read-only regression runs

`packages/web/tests/settings.spec.ts` is read for its em-dash guard idiom but is NOT modified and is
not in `files_modified`. If an expectation there turns out to need editing, stop, add it to
`files_modified`, and re-check the wave before editing. The wave guarantee is computed from that
field, so a spec edited outside it silently weakens the guarantee.

### What would have caught this and did not

The semantic feed behind the mode chip is genuinely covered: `monitor.serviceHealth` has server-side
rows, the wire shape has route rows, and `state.health` has store rows. `broadcastControlState`,
`serviceControlsView` and `operatorLogState` are all pure and all tested. Everything except the last
hop from a value to a rendered element was proven, which is precisely the hop `05-VALIDATION.md`
records as verified by `build`.
</verification>

<success_criteria>
The operator console's mode claim, its one-way control's mode guard, its test-peer refusal, and its
dismissal promise all fail the suite when their shipped source is mutated.
</success_criteria>

<output>
Create `.planning/phases/05-operator-cohort-lifecycle-control/05-22-SUMMARY.md` when done.
</output>
</content>
