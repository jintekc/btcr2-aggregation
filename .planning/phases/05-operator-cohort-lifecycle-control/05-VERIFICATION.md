---
phase: 05-operator-cohort-lifecycle-control
verified: 2026-08-03T19:24:21Z
status: human_needed
score: 7/7 roadmap Success Criteria verified at code level (0 present, behavior-unverified); 16 human-verification items still pending (unchanged in count, one new backstop added by 05-35); 3 new non-gating warnings found by the round-5 re-review (WR-08, WR-09, WR-10) and independently confirmed in this pass
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: "6/7 roadmap Success Criteria verified at code level; 1 FAILED (SC3, review CR-02) (per 05-VERIFICATION.md dated 2026-08-03, before the fifth gap-closure round)"
  gaps_closed:
    - "SC3 (review CR-02, the settings-save byte wedge): the gated PUT /v1/operator/settings route's independently chosen 4 KiB bodyLimit, which refused every settings save once stored terms passed roughly 3900 characters (well inside the 20000-character ceiling the holder documents as storable), is replaced by SETTINGS_BODY_LIMIT_BYTES, a value DERIVED from MAX_TERMS_CHARS at a proven worst-case six bytes per UTF-16 code unit plus fixed headroom (124096 bytes total), landed in 05-33. Independently reconfirmed by reading the derivation's docstring and source expression directly (not copied from the SUMMARY), by reading the route's consumption of the constant, and by running the touched spec files directly: a full console-shaped save carrying a terms document of exactly 20000 ASCII characters, and the same document in a three-byte-per-character script, both reach applySettings and are stored; a save of an unrelated field succeeds while a ceiling-length terms document is already in place; one character past the cap is answered 400 carrying the holder's own terms-refusal sentence rather than 'request too large'. All confirmed green in a direct rerun of lifecycle-routes.spec.ts and runtime-settings.spec.ts (219/219 passed)."
    - "IN-03 (the service display name held a dead second unbounded path): the static serviceName option on HonoAppOptions and its GET /v1/config fallback are deleted; the holder is the display name's only source. Confirmed by reading hono-adapter.ts directly: no serviceName option remains, and the config route resolves the name from the holder alone."
    - "IN-04 (the retained last-write whole-minute guard was indistinguishable from its own deletion): pinned as a no-op property across HOSTILE_SEEDS, asserted on boot output. Confirmed present in runtime-settings.spec.ts and reproduced green in the direct rerun."
    - "WR-07 (a refused free-text boot seed was captioned as though the environment set nothing): droppedSeeds now carries the refused variable NAMES on the gated settings read, and SettingsView.tsx renders a third, distinct caption naming the variable and the concrete cost, landed in 05-35. Confirmed by reading runtime-settings.ts's collector, hono-adapter.ts's SettingsFieldKey-keyed labels, and SettingsView.tsx's REFUSED_SEEDS/droppedSeedCaption directly; the anonymous GET /v1/config body is unaffected (names ride the gated read only)."
    - "WR-06 (the list-poll guard compared an auth STATUS rather than a session IDENTITY, so a logged-in -> logged-out -> logged-in sequence could still repaint a dead session's answer into a live one): closed in 05-34 by sessionRound, a monotonic identifier bumped on every path that starts or ends a session (signIn on 200, probe on a live result, signOut, expireSession), compared by identity in refreshCohorts alongside the retained status check. Confirmed by reading the guard directly (operator.ts:940, :975) and by rerunning operator.spec.ts, which carries the ABA regression rows for both ways a session can end plus the anti-vacuity same-session and new-session-own-read controls."
  gaps_remaining: []
  regressions: []
new_findings_this_pass:
  - "WR-08 (confirmed real, non-gating): refreshCohorts' 401 branch (operator.ts:942-952) still expires the session unconditionally on any unauthorized answer, with no round comparison, ahead of the round guard the same function added for the ok path. The function's own comment claims this is safe ('both are session-scoped, so neither order changes what happens'); that claim is false for the same reason WR-06 was real. A stale 401 from a session that already ended, arriving after the operator has signed back in, signs the NEW live session out with a false 'Your operator session ended' message and clears the whole gated slice (open drill-down, unsaved draft, settings snapshot). The identical unguarded-401 shape exists at pollDetail (:1359-1368) and loadSettings (:1277-1281). Confirmed by direct code read; not one of the 7 numbered roadmap Success Criteria, and does not require a process restart or leave the service down, so it does not gate this verification's status on its own, matching how WR-06 itself was treated in the prior pass before its own fix."
  - "WR-09 (confirmed real, non-gating): the session-identity rule sessionRound's own docstring states as general law ('any guard that must decide whether an answer still belongs to the session that asked compares this, never auth alone') was applied to exactly one of three gated read paths. pollDetail (:1348-1387) guards on cohort id only, not session, so a dead session's cohort detail (member DIDs, pubkeys, signed updates, funding state) can repaint into a live session that reopens the same cohort id. loadSettings (:1275-1289) carries NO session guard of any kind: a late-landing settings read from an ended session is written into a freshly signed-in console unconditionally, and because OperatorConsole.tsx re-reads settings only while settings is undefined, that write defeats the once-per-session latch and the new session never issues its own read. Confirmed by direct code read of all three functions. Non-gating for the reason WR-06 was: it does not by itself falsify a numbered roadmap Success Criterion, since the underlying service state is unchanged across the same operator's own re-sign-in in the common case."
  - "WR-10 (confirmed real, non-gating): the WR-07 disclosure's own caption text (SettingsView.tsx:69-80, REFUSED_SEEDS) tells the operator a refused field's cost lasts 'until the value is shortened and this service restarts', directly beneath the textarea that, since CR-02, lets the operator fix it in the same session with no restart. Confirmed by reading the caption strings directly against the now-fixed save path. Cosmetic/copy defect, not a functional block; does not gate SC3 or SC5, which are about the save mechanism and the acceptance mechanism respectively, not this caption's wording."
gaps: []
deferred:
  - truth: "The transport advert slot is also cleared when a cohort FILLS (keygen-complete), not only on the three settle paths, so a sibling open cohort stays listed but stops being joinable to a freshly connecting participant."
    addressed_in: "Phase 6 (suggested home; not yet claimed by a Phase 6 success criterion)"
    evidence: "Logged in .planning/phases/05-operator-cohort-lifecycle-control/deferred-items.md and documented as upstream limit 1 in docs/UPSTREAM-LIMITS.md. No Phase 5 must-have covers the fill path. Carried forward unchanged across every verification pass, including this one."
behavior_unverified_items: []
human_verification:
  - test: "Judgment 1 (05-UAT test 3): confirm by eye that no Close button exists anywhere on the console, and accept or refuse the deliberate re-reading of roadmap SC 1 (Closed is an automatic nth-seat stage, not an operator-pressed button, per locked CONTEXT D-01)."
    expected: "No Close control exists anywhere on the console; the Closed stage appears automatically the instant the nth seat fills, narrated with its caption, not as an act the operator performs."
    why_human: "A deliberate divergence from the literal wording of the roadmap goal that only the owner can accept or refuse. The stage's existence, trigger, and caption are automated and passing (packages/web/tests/operator-stage.spec.ts); the absence of a Close button anywhere on the rendered console is not asserted by any test."
  - test: "Judgment 2 (05-UAT test 12): read the Ended-group labels (a completed hermetic co-sign now reads 'Signed', not an anchor claim) and decide whether the change reads as newly honest rather than as a regression."
    expected: "Owner accepts the relabeling as an honest disclosure of what MuSig2 completion does and does not prove about an on-chain anchor."
    why_human: "Pure copy/framing judgment; every label and the anchor-wording guard are automated and passing (packages/web/tests/operator-rows.spec.ts)."
  - test: "Eyes (05-UAT test 13, plus narrowed 5/7/8): at a real browser viewport, walk the operator console list, drill-down (with cancel/finalize/seat-reclaim/funding disclosures all visible at once), health strip, a saved long SERVICE_NAME on both the console and the public directory header, and a long TERMS_TEXT body at a narrow viewport height. Now also covers the new third source caption (WR-07 fix) rendered at a real viewport (05-35's new backstop)."
    expected: "Nothing overflows, chips stay on screen, grouping is legible with no horizontal scroll; the long service name renders as plain escaped text on both surfaces without pushing chips off screen; the long terms body scrolls inside its capped container, wraps unbroken tokens, never escapes the card, and join controls stay reachable below it; the refused-seed caption reads as a warning, not as another grey caption."
    why_human: "Layout/legibility at a real screen size. The render harness is a static server render (no layout engine), so it can prove the capping/wrapping/caption-branch classes are present but never that anything actually fits or reads correctly."
  - test: "Copy (05-UAT test 9, plus two follow-on questions): read the four interpolated test-peer confirm strings, the verbatim 'unknown draft' refusal shown to an operator editing a stale draft, and the duplicated 'Operator console' heading shown on both sides of sign-in."
    expected: "All read correctly, in particular that the test-peer confirm states BEFORE the act that peers co-sign for real with throwaway keys; decide whether 'unknown draft' is the right sentence and whether the signed-in console deserves its own heading."
    why_human: "Copy/framing decisions; the four interpolated strings are pinned by nothing today (a named, not-yet-done follow-up), and the other two are deliberate, already-shipped choices awaiting the owner's sign-off."
  - test: "Hands (05-UAT tests 2, 4, 6, 10, 11): exercise 'Cancel edit' on a draft in place; open the finalize and ordinary-cancel confirms and read their in-place wording plus the in-flight disable; load the public directory paused/unpaused and narrow the window; cancel a cohort a participant is seated in and read their terminal card; dismiss an ended cohort row and refresh the page."
    expected: "Cancel edit closes the form with the draft unchanged; finalize/cancel confirms state the real consequence before it happens and disable correctly while in flight; the paused notice and empty states render distinctly and the controls row wraps at narrow widths; the cancel narration and next-step line appear together on the participant's terminal card; a dismissed row stays gone after a refresh."
    why_human: "The render harness is a static server render: no events fire, no effects run, so no state a click produces is reachable from it. Needs a DOM environment or a browser leg that does not exist yet."
  - test: "Environment 1 (05-UAT test 14): follow docs/DEPLOY.md from a clean machine using only the document (sign in, create, advertise, join, rehearse)."
    expected: "Every new control is reachable from the document alone; the retired filler knob appears nowhere; MIN_PARTICIPANTS/AUTO_FALLBACK/SSE_DEBUG and the /v1/config sample match what the service actually reads and serves. This walk should also confirm the corrected participation-terms paragraph and TERMS_TEXT row now match observed behavior (CR-02 is closed, so this should no longer trip)."
    why_human: "Needs a clean machine and a person following written instructions verbatim; not something a unit or e2e test can stand in for."
  - test: "Environment 2 (05-UAT test 15, narrowed, updated expectation): try a real host that blocks browser requests (CORS) and a real wrong-chain third-party esplora host against this service's actual default network."
    expected: "browser-rejected and mismatch (naming both chains) appear correctly, with the explicit switch-back control offered on failure and no silent fallback in either direction. This confirms SC6 (PART-05) at the code level, closed by 05-29, holds against a real host, which is the one thing the hermetic suite cannot prove."
    why_human: "Real CORS behavior and a real wrong-chain host cannot be produced hermetically."
  - test: "Environment 3 (05-UAT test 16): export the unsigned PSBT, sign it in an actual desktop or hardware wallet, return it, and broadcast; also confirm a large pasted PSBT keeps its field scrolling internally."
    expected: "The wallet accepts the exported .psbt; the returned PSBT validates against the template; the broadcast produces the same txid the local path would; the large-PSBT field does not reflow the step."
    why_human: "No wallet interoperability was verified in this environment; the 05-14 prohibition correctly forbids claiming any specific wallet works without this walk."
  - test: "Environment 4 (05-UAT test 17, narrowed): boot with LIVE=1 BROADCAST=1, engage Disable broadcast, and confirm the real boot behavior plus the disclosed cost of the stand-down."
    expected: "No route turns broadcast back on; the health strip still reports the live boot mode with a separate warn chip; a cohort advertised after the switch shows no Funding card while one advertised before keeps its funding surface; the owner accepts that the esplora-reachability badge stops refreshing once only post-switch cohorts remain."
    why_human: "Needs a real LIVE+BROADCAST boot, which cannot be produced hermetically, plus an owner cost/benefit judgment that only the owner can make."
  - test: "Cancel settle-race backstop (05-01, verification: backstop): attempt to cancel a cohort at the instant its signing round completes."
    expected: "The click is answered 404 with nothing changed, and the console never shows two ended records for one cohort id."
    why_human: "A timing race that cannot be reliably driven from a test harness; classified 'backstop' by design. What is checkable about it (the opaque 404 on an already-settled cohort, and idempotent folding so no duplicate ended record is ever filed) is automated and passing."
---

# Phase 5: Operator Cohort Lifecycle Control Verification Report

**Phase Goal:** The operator runs aggregation and manages a cohort's lifecycle (open, then close, then finalize) and pauses, cancels, or reconfigures advertising from the console, without restarting the process, removing the last hardwired, uncontrollable behavior.
**Verified:** 2026-08-03
**Status:** human_needed
**Re-verification:** Yes. This is the sixth verification pass. The prior `05-VERIFICATION.md` (dated 2026-08-03, `status: gaps_found`, score 6/7, FAILED on SC3 due to the settings-save byte wedge, review CR-02) drove a fifth gap-closure round (05-33, 05-34, 05-35), executed 2026-08-03. This report independently confirms all three round-5 targets (CR-02, WR-06, WR-07, plus the two INFO findings IN-03/IN-04) are genuinely closed, and independently confirms the fresh round-5 re-review's finding (05-REVIEW.md, three new warnings WR-08/WR-09/WR-10) that a related but distinct defect family remains open in the session-guard code this round touched, none of which falsifies a numbered roadmap Success Criterion on its own.

## Method

I did not trust SUMMARY.md narrative, 05-REVIEW.md's own severity labels, or the prior VERIFICATION.md's gap list as complete. For every claim that mattered to a Success Criterion, I re-derived it from the live tree, and for every new review finding I read the exact lines myself rather than accepting the review's citation.

- Ran `pnpm test` directly: **68 files, 1271 tests, all passing** (matches 05-35-SUMMARY.md's claimed total, confirmed independently, up from the 1234 baseline before this round: 05-33 added 13, 05-34 added 10, 05-35 added 24).
- Read `packages/service/src/runtime-settings.ts` lines 380-421 in full and confirmed `SETTINGS_BODY_LIMIT_BYTES` is a source EXPRESSION (`MAX_TERMS_CHARS * WORST_CASE_TERMS_BYTES_PER_CODE_UNIT + SETTINGS_BODY_HEADROOM_BYTES`), not a literal, with the derivation and the rejected-alternative measurements stated in the docstring.
- Read `packages/service/src/hono-adapter.ts` and confirmed the settings route (line 917-920) consumes `SETTINGS_BODY_LIMIT_BYTES`, that all eight other `bodyLimit` call sites in the file are untouched (4 KiB / 16 KiB / 64 KiB / 512 KiB, unchanged), and that the static `serviceName` option is gone (`grep` for it returns nothing on `HonoAppOptions`).
- Ran `pnpm vitest run packages/service/tests/lifecycle-routes.spec.ts packages/service/tests/runtime-settings.spec.ts packages/web/tests/operator.spec.ts packages/web/tests/settings.spec.ts` directly: 4 files, 219 tests, all green, independently of the SUMMARYs' own reported figures.
- Read `docs/DEPLOY.md`'s participation-terms paragraph (lines 343-356) and the `TERMS_TEXT` row (line 513) directly and confirmed both now state a single character-based cap true of the console save path, with no byte figure exposed to the reader, matching the landed code.
- Read `packages/web/src/stores/operator.ts`'s `refreshCohorts` (lines 930-985), `pollDetail` (lines 1348-1387), and `loadSettings` (lines 1275-1289) in full, independently of the review's own line citations, and confirmed: (a) `sessionRound` is a real monotonic identifier bumped at four sites (`signIn`, `probe`, `signOut`, `expireSession`) and `refreshCohorts`'s ok-path guard compares it by identity, closing the ABA sequence for that one path; (b) the `refreshCohorts` unauthorized branch still calls `expireSession()` unconditionally with no round comparison, exactly as the review's WR-08 describes, and the code's own comment defending this ("neither order changes what happens") is false for the same reason WR-06 was; (c) `pollDetail` guards only on cohort id, not session, and `loadSettings` has no session guard of any kind, exactly as WR-09 describes.
- Read `packages/web/src/components/operator/SettingsView.tsx` lines 60-99 (`REFUSED_SEEDS`, `droppedSeedCaption`) directly and confirmed the WR-10 finding: both cost strings state a restart is required to fix the field, which is false for the terms field now that CR-02 makes the save path reachable within the same session.
- Independently re-ran `pnpm e2e:pause` and `pnpm e2e:cancel` directly (not just cited from a prior pass): both PASSED, confirming SC2 and SC4 hold unaffected by round 5 (neither round-5 plan touches the pause/advertise-gate or cancel-visibility path).
- Confirmed `packages/service/tests/tos.spec.ts` (28 tests, SVC-05/SC5), `packages/web/tests/tx-client.spec.ts` (55 tests, PART-05/SC6), `packages/shared/tests/psbt.spec.ts` and `packages/web/tests/psbt.spec.ts` (PART-06/SC7) all pass as part of the full 1271-test run; none is touched by round 5.
- Read `05-UAT.md` in full: `status: testing`, 16 items, all `result: [pending]`, unchanged in count from the prior pass (05-35 added one new backstop item folded into the existing "Eyes" entry rather than a new numbered UAT test).
- Cross-referenced `REQUIREMENTS.md`: exactly SVC-04, SVC-05, PART-05, PART-06 map to Phase 5; all four appear in PLAN frontmatter across the 35 plans (05-33/05-34: SVC-04; 05-35: SVC-05, SVC-04). No orphaned requirements. The traceability table currently reads `SVC-04 | Phase 5 | Complete` and, as of this round, `SVC-05 | Phase 5 | Complete` (both written by the `f895acf` commit recording round-5 completion); `PART-05` and `PART-06` still read `Gaps Found`. See the Requirements Coverage section and Anti-Patterns Found below for why marking SVC-04 and SVC-05 `Complete` is premature given the phase's own 16 pending human-verification items, two of which (Judgment 1, Judgment 2) bear directly on SVC-04's and SVC-05's own success criteria, matching the exact pattern of premature-completion-then-reversion this phase has already followed twice (commits `118cb9b`, `4a2445a`).
- Read `05-REVIEW.md` in full (round-5 re-review: 0 critical, 6 warnings, 6 info) and independently weighed WR-08, WR-09 and WR-10 against the 7 roadmap Success Criteria rather than accepting the review's own "non-gating" framing uncritically. All three are confirmed real by direct code read, and none is caused by a process restart requirement, a killed service, or a directory/state mismatch, so none falsifies a numbered Success Criterion on its own; they are recorded as findings for a future round.

## Goal Achievement

### Observable Truths (roadmap Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Operator moves a cohort through open, close and finalize from the console and the directory reflects each state change | VERIFIED | Unaffected by round 5 (no round-5 file touches `LifecycleActions.tsx` or `pollDetail`'s cohort-keyed guard). Confirmed by the full `pnpm test` pass (`operator.spec.ts`, `lifecycle.spec.ts`, both green). Human judgment item (Judgment 1) remains for the "no Close button" reading. |
| 2 | Operator pauses or cancels advertising so new cohorts stop being offered, without killing the running service | VERIFIED | `pnpm e2e:pause` (rerun directly during this pass, PASSED): `GET /v1/status` reports `paused:true` while still listing/counting the pre-pause cohort, a fresh participant still seats in it, a new advertise is refused 409, resume restores both bits. Unaffected by round 5. |
| 3 | Operator reconfigures cohort shape (capacity, threshold, beacon type for the next cohort) without editing env vars or restarting | VERIFIED | The defect that FAILED this truth in the prior pass (review CR-02, the settings-save byte wedge) is closed by 05-33 and independently reconfirmed: read the derivation directly in source, read the route's consumption of it, and independently reran the touched spec files (219/219 green) rather than trusting the SUMMARY's reported numbers. A full console-shaped save carrying a terms document at exactly the 20000-character stored ceiling, in both ASCII and a three-byte-per-character script, now reaches `applySettings` and is stored; a save of an unrelated field succeeds while a ceiling-length terms document is already in place; a refusal past the cap now names the field and the limit rather than "request too large". |
| 4 | A canceled or closed cohort no longer appears as joinable in the participant directory | VERIFIED | `pnpm e2e:cancel` (rerun directly, PASSED): the canceled cohort leaves the public directory and open count immediately; a still-open sibling seats a freshly constructed participant afterward. Unaffected by round 5. |
| 5 | Participation terms accepted at join and recorded as a DID-signed, server-verified, terms-hash-bound artifact, with the app-level enforcement boundary disclosed honestly (SVC-05) | VERIFIED (code level) | `packages/service/tests/tos.spec.ts` (28 tests, part of the full 1271-test pass, all green). 05-35 additionally closed WR-07 on this surface (a refused free-text seed no longer captioned as an environment default), confirmed by direct code read of `droppedSeeds`, `REFUSED_SEEDS` and `droppedSeedCaption`. Human judgment items remain (Judgment 2, Eyes). |
| 6 | Participant can point browser chain reads at their own esplora endpoint, with a network-mismatch guard, four distinguishable failure messages, no silent fallback, real-funds guard rails unweakened (PART-05) | VERIFIED (code level) | No round-5 file touches `esplora.ts` or `tx-client.ts`. Confirmed by the full `pnpm test` pass (`tx-client.spec.ts`, 55/55). The live leg (a real CORS-blocking host and a real wrong-chain host, 05-UAT test 15) still needs a person; see Human Verification. |
| 7 | Participant can sign the registration transaction in their own wallet through a PSBT round trip validated against the exact template before anything is broadcast (PART-06) | VERIFIED (code level) | No round-5 file touches this surface. Confirmed via the full `pnpm test` pass (`packages/shared/tests/psbt.spec.ts`, `packages/web/tests/psbt.spec.ts`, both green). Real wallet interoperability: human item (Environment). |

**Score:** 7/7 truths verified (0 present, behavior-unverified)

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | The transport advert slot is also cleared when a cohort FILLS (keygen-complete), not only on the three settle paths, so a sibling open cohort stays listed but stops being joinable to a freshly connecting participant | Phase 6 (suggested, not yet claimed) | `deferred-items.md` + `docs/UPSTREAM-LIMITS.md` limit 1. Not a Phase 5 must-have: 05-01 scopes repair to the three settle paths, all wired and behaviorally proven. Carried forward unchanged across every verification pass, including this one. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/service/src/runtime-settings.ts` (`SETTINGS_BODY_LIMIT_BYTES`, `MAX_TERMS_CHARS` exported) | A derived, checkable body budget replacing the independently chosen 4 KiB route limit | VERIFIED | Read in full; the constant is a source expression, its docstring states the derivation and the rejected-alternative measurements, and `MAX_TERMS_CHARS` is exported while `MAX_SERVICE_NAME_CHARS` correctly stays module-local. |
| `packages/service/src/hono-adapter.ts` (`PUT /v1/operator/settings` route) | Consumes the derived budget; all other routes' limits unchanged; static `serviceName` option removed (IN-03) | VERIFIED | `SETTINGS_BODY_LIMIT_BYTES` consumed at the settings route; the other eight `bodyLimit` sites (4 KiB/16 KiB/64 KiB/512 KiB) are byte-identical to before; `HonoAppOptions` no longer declares a `serviceName` option. |
| `packages/service/tests/lifecycle-routes.spec.ts`, `packages/service/tests/runtime-settings.spec.ts` | Ceiling-value save rows, anti-vacuity script-encoding row, reshaped 413 row, IN-04 no-op pin | VERIFIED | Present; independently rerun in this pass (219/219 across the four touched spec files, matching the 05-33/05-34/05-35 SUMMARYs' own mutation-revert evidence). |
| `docs/DEPLOY.md` (participation-terms paragraph, `TERMS_TEXT` row) | States a single character cap true of the console save path, no byte figure exposed | VERIFIED | Read directly at lines 343-356 and 513; both now match the landed CR-02 fix. |
| `packages/web/src/stores/operator.ts` (`sessionRound`, `refreshCohorts` guard) | Session-identity guard closing the WR-06 ABA case on the list-poll ok path | VERIFIED (for the ok-path case WR-06 named) | `sessionRound` bumped at four sites, compared by identity in `refreshCohorts`'s post-await guard. Confirmed by direct read and by the ABA regression rows in `operator.spec.ts`. |
| `packages/web/src/stores/operator.ts` (`refreshCohorts`, `pollDetail`, `loadSettings` unauthorized branches) | Session-identity discipline applied uniformly to every gated read's 401 handling and to every gated read's success handling | NOT ADDRESSED THIS ROUND (WR-08, WR-09) | `refreshCohorts`'s 401 branch still expires the session unconditionally with no round comparison (WR-08). `pollDetail` guards only on cohort id (WR-09). `loadSettings` has no session guard of any kind on either branch (WR-09). Confirmed by direct code read at the exact lines the review cites. None of these three falsifies a numbered roadmap Success Criterion; recorded as a warning for a future round. |
| `packages/web/src/components/operator/SettingsView.tsx` (`REFUSED_SEEDS` captions) | Accurate statement of what fixes a refused field, now that CR-02 makes the save path reachable in the same session | STALE (WR-10) | Both cost strings assert a restart is required; false for the terms field since CR-02 landed in the same round. Cosmetic; does not gate SC3 or SC5. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `runtime-settings.ts` (`MAX_TERMS_CHARS`) | `hono-adapter.ts` (`PUT /v1/operator/settings` `bodyLimit`) | The route's streaming byte budget is derived from the holder's own character cap | WIRED | Confirmed by direct read: `SETTINGS_BODY_LIMIT_BYTES` is imported as a value and consumed at the route's `bodyLimit` argument. |
| `SettingsView.tsx` (`settingsPatch`) | `hono-adapter.ts` (`PUT /v1/operator/settings`) | The console always sends the full form, including `termsText`, on every save | WIRED (and this is exactly why CR-02's fix had to be a derived budget, not a smaller chosen one) | Confirmed unchanged from the prior pass; the all-or-nothing patch contract is untouched by round 5, per its own explicit prohibition. |
| `OperatorConsole.tsx` (list poll) | `operator.ts` (`refreshCohorts`) | The session-identity round guard | WIRED for the ok-path ABA case (WR-06 closed); NOT WIRED for the 401-path case (WR-08, new) | Confirmed by direct code read; the ok-path guard compares `sessionRound` by identity, the 401-path branch does not compare it at all. |
| `CohortDetail.tsx` (detail poll) | `operator.ts` (`pollDetail`) | A session-identity guard, matching the house rule `sessionRound`'s own docstring states | NOT WIRED (WR-09, new) | `pollDetail` guards on cohort id only; a dead session's answer about the same cohort id can still repaint a live session's drill-down. |
| `SettingsView.tsx` (settings read on open) | `operator.ts` (`loadSettings`) | A session-identity guard | NOT WIRED (WR-09, new) | `loadSettings` has no session guard of any kind on either its unauthorized or its success branch; a late answer from an ended session can populate a live session's settings snapshot and defeat `OperatorConsole`'s once-per-session re-read latch. |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| SVC-04 | 05-01..05-35 (core wave + round 5) | Lifecycle control, pause/cancel/reconfigure advertising without restart | SATISFIED at code level (all 4 of its numbered Success Criteria VERIFIED); traceability table's `Complete` mark is PREMATURE | SC1, SC2, SC3, SC4 all VERIFIED. The `REQUIREMENTS.md` table reads `SVC-04 \| Phase 5 \| Complete` (written by `f895acf`), but Judgment 1 (05-UAT test 3, bearing directly on SC1's "no Close button" reading) remains an unresolved pending human item, and this pass's own overall status is `human_needed`. Marking a requirement `Complete` while a human item that bears on its own success criteria is still `[pending]` repeats the exact premature-completion pattern this phase already reverted twice (`118cb9b`, `4a2445a`). Recommend reverting to `Gaps Found` (or a new distinct status such as "Verified, pending human sign-off") until 05-UAT.md closes. |
| SVC-05 | 05-13, 05-35 | Participation terms, DID-signed, server-verified, hash-bound, honest boundary disclosure | SATISFIED at code level; traceability table's `Complete` mark is PREMATURE | `tos.spec.ts` 28/28 green; WR-07 closed. The `REQUIREMENTS.md` table now reads `SVC-05 \| Phase 5 \| Complete` (also written by `f895acf`), changed from the prior pass's correctly-`Gaps Found` state. Judgment 2 and the Eyes item (both bearing directly on SVC-05's rendered terms and honesty framing) remain `[pending]`. Same recommendation as SVC-04. |
| PART-05 | 05-11, 05-29 | Participant-supplied esplora endpoint, mismatch guard, four failure messages, no silent fallback | SATISFIED (code level) | `tx-client.spec.ts` 55/55 green, unaffected by round 5. Human Environment item remains. Traceability table already correctly reads `Gaps Found`; no change needed. |
| PART-06 | 05-14 | PSBT wallet round trip, template-validated, guarded broadcast | SATISFIED (code level) | `psbt.spec.ts` suites green, unaffected by round 5. Human Environment item remains. Traceability table already correctly reads `Gaps Found`; no change needed. |

No orphaned requirements: `SVC-04`, `SVC-05`, `PART-05`, `PART-06` are exactly the four requirements mapped to Phase 5 in `REQUIREMENTS.md`, and all four appear in PLAN frontmatter across the 35 plans.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `.planning/REQUIREMENTS.md` | 74-75 | Traceability table marks `SVC-04` and `SVC-05` `Complete` while this same phase carries 16 `[pending]` human-verification items, two of which (Judgment 1, Judgment 2) name those exact requirements' own success criteria | Warning | Not a code defect, a documentation-state inconsistency. This is the third time in this phase's history that a requirement was marked `Complete` ahead of its actual closure condition (after `118cb9b` and `4a2445a`, both reverted). `PART-05`/`PART-06` correctly still read `Gaps Found` for the identical reason (pending human Environment items); `SVC-04`/`SVC-05` should read the same until `05-UAT.md` closes. |
| `packages/web/src/stores/operator.ts` | 942-952, 1359-1368, 1277-1281 | Guard comments claim an invariant ("session-scoped, so neither order changes what happens", or no comment at all for `loadSettings`) that the code does not implement (WR-08, WR-09) | Warning | Confirmed real by direct read. Does not gate a numbered roadmap Success Criterion, matching how WR-06 itself was treated before its own fix. Worth a future round; the same discipline that closed WR-06 (session identity, not status) needs to be extended to all three gated reads' 401 branches and to `pollDetail`/`loadSettings`'s success branches. |
| `packages/web/src/components/operator/SettingsView.tsx` | 69-80 | A disclosure caption (WR-07's own fix) states a remedy ("shorten the value and restart") that is no longer the only remedy, since CR-02 (landed in the same round) made the field save reachable within the session | Warning | Confirmed real by direct read. Cosmetic; does not gate SC3 (the mechanism it describes, the derived body budget, is correct) or SC5 (the acceptance/verification mechanism is unaffected). |

No `TBD`, `FIXME`, or `XXX` markers found in any round-5-touched file. No em-dash found in any round-5-touched file (independently grepped over the seven files 05-33/05-34/05-35 modified).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full test gate is green after round 5 | `pnpm test` | 68 files, 1271 tests, all passing | PASS |
| The four round-5-touched spec files independently pass in isolation | `pnpm vitest run packages/service/tests/lifecycle-routes.spec.ts packages/service/tests/runtime-settings.spec.ts packages/web/tests/operator.spec.ts packages/web/tests/settings.spec.ts` | 4 files, 219 tests, all passing | PASS |
| Pause/resume advertising leaves the service and directory up | `pnpm e2e:pause` | PASSED | PASS |
| A canceled cohort disappears from the directory immediately, sibling still seats | `pnpm e2e:cancel` | PASSED | PASS |
| The CR-02 wedge (settings save above ~3900 characters of terms) is closed | Read `SETTINGS_BODY_LIMIT_BYTES`'s source expression and the route's consumption of it directly; confirmed by the ceiling-value rows in the rerun of `lifecycle-routes.spec.ts` | 20000-character ASCII and three-byte-per-character terms documents both reach `applySettings` and are stored; one character past the cap is answered 400 naming the limit | PASS |
| WR-08 (a stale 401 from an ended session signs a live session out) | Read `refreshCohorts`'s unauthorized branch (`operator.ts:942-952`) directly against its own defending comment | The branch calls `expireSession()` with no round comparison; the comment's claim that order does not matter is false for the reason WR-06 was real | CONFIRMED (non-gating warning, not a test failure) |
| WR-09 (`pollDetail`/`loadSettings` cross-session guard gap) | Read both functions directly (`operator.ts:1348-1387`, `:1275-1289`) | `pollDetail` compares cohort id only; `loadSettings` compares nothing | CONFIRMED (non-gating warning, not a test failure) |

### Probe Execution

Not applicable. This phase has no `scripts/*/tests/probe-*.sh` convention; verification relies on the vitest suite, the `e2e:*` harness scripts, and direct code reads.

## Human Verification Required

16 items carried forward, unchanged in count from the prior pass (see frontmatter `human_verification` and `05-UAT.md`, `status: testing`, all `result: [pending]`). 05-35 added one new backstop (the rendered refused-seed caption at a real viewport) folded into the existing "Eyes" item rather than a new numbered UAT test, per its own SUMMARY. None of the round-5 changes altered any item's preconditions except Environment 1 (the DEPLOY.md walk-through), which should now find the corrected participation-terms guidance accurate rather than tripping over CR-02 as it would have before this round.

## Gaps Summary

Round 5 (05-33, 05-34, 05-35) closed the only remaining code-level gap against a numbered roadmap Success Criterion: the settings-save byte wedge (review CR-02) that FAILED SC3 in the prior pass. This pass independently reconfirmed the fix by reading the derivation's source expression and docstring directly, reading the route's consumption of it, and rerunning the touched spec files rather than trusting the SUMMARYs' own reported figures. All 7 numbered roadmap Success Criteria now verify at the code level, and none is FAILED.

The round also closed WR-06 (the list-poll guard's status-vs-identity defect) and WR-07 (the dropped-seed disclosure), both independently reconfirmed by direct code read and by rerunning the relevant spec files.

A fresh code review conducted after round 5 landed (05-REVIEW.md) found, and this pass independently confirmed by direct code read rather than by citation, three new but related warnings in the exact session-guard surface round 5 touched: WR-08 (the list-poll guard's own 401 branch, and the identical shape in `pollDetail` and `loadSettings`, still act on a stale session's unauthorized answer with no round comparison, contradicting the guard's own defending comment), WR-09 (the session-identity rule `sessionRound`'s docstring states as general law was applied to one of three gated reads; `pollDetail` and `loadSettings` still cross sessions), and WR-10 (the WR-07 disclosure's own caption tells the operator a restart is required to fix a refused field, which CR-02, landed in the same round, made untrue for the terms field). None of these three falsifies a numbered roadmap Success Criterion: they concern session-handling correctness and disclosure accuracy in the operator console's UI layer, not the lifecycle-control, pause/cancel, reconfiguration, or terms-acceptance mechanisms themselves, and none of them requires a process restart or a killed service to manifest or to recover from. They are recorded here as warnings for a future round rather than as gaps blocking this verification.

Because all 7 Success Criteria now verify at the code level and no truth is FAILED, this pass's overall status is `human_needed` rather than `gaps_found`: the 16 pending items in `05-UAT.md` (unchanged in count) are what stands between this phase and a `passed` verification, not any remaining code defect.

One documentation-state finding is recorded as a warning rather than a gap: `REQUIREMENTS.md`'s traceability table now marks `SVC-04` and `SVC-05` `Complete` (written by the round-5 completion commit, `f895acf`), while `PART-05` and `PART-06` correctly remain `Gaps Found` pending their own human items. Two of the 16 pending human items (Judgment 1, Judgment 2) bear directly on SVC-04's and SVC-05's own success criteria, so marking those two requirements `Complete` ahead of `05-UAT.md`'s closure repeats a pattern this phase has already had to revert twice before (commits `118cb9b`, `4a2445a`). This does not affect the code-level verification result above, but it is worth correcting before the phase is considered ready to close, and before `/gsd-secure-phase 5` (still pending; no `05-SECURITY.md` exists yet) and the milestone-completion step that reads this table.

---

_Verified: 2026-08-03_
_Verifier: Claude (gsd-verifier)_
