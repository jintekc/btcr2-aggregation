---
phase: 05-operator-cohort-lifecycle-control
verified: 2026-08-03T00:00:00Z
status: gaps_found
score: 6/7 roadmap Success Criteria verified at code level; 1 FAILED (SC3, a new instance of the same failure mode); 16 human-verification items still pending (unresolved, per 05-UAT.md)
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: "6/7 roadmap Success Criteria verified at code level; 1 FAILED (SC3) (per 05-VERIFICATION.md dated 2026-08-02, before the fourth gap-closure round)"
  gaps_closed:
    - "SC3's round-4-targeted defect (the string-seed wedge, review CR-01): SERVICE_NAME/TERMS_TEXT boot seeds over 200/20000 characters used to be stored with no bound while applySettings enforced one, silently wedging every later settings save. Closed by 05-31's textKnob: an over-long seed is now dropped at the holder with a loud boot warning naming the length, the ceiling and the cost, and the invariant predicate (expectHolderInvariants) now covers the string fields as well as the numeric ones. Confirmed directly against the shipped module: createRuntimeSettings({ serviceName: 'x'.repeat(5000), termsText: 'y'.repeat(100000) }) now warns twice, stores neither value, and a following rename-only save succeeds."
    - "W6 (the clamp warning that named a variable no environment reference contains, review WR-02): a single COHORT_TTL_MS=90000 boot used to print two lines, the second false. Closed by 05-31 reordering the discovery-window quantizer to run on the seed before the ceiling comparison, so the clamp fires only when its own sentence is true, and by naming the settable environment variable in every window warning. Confirmed directly: the boot now prints exactly one line, naming COHORT_TTL_MS."
    - "W5 (the list-poll session round guard, review WR-01, for the case it was reproduced against): refreshCohorts used to write the gated slice (cohort list, metrics, operator log, the broadcast-mode chip) from an answer that arrived after a session ended (fast 401 then slow ok). Closed by 05-32's session round guard on refreshCohorts, placed ahead of both the unreachable branch and the ok write. Confirmed directly against the live tree and by rerunning operator.spec.ts, which now carries 7 new regression rows including the reproduced fast-401/slow-ok race, the sign-out race and two anti-vacuity controls."
  gaps_remaining:
    - "SC3 (NEW INSTANCE, same failure mode, review CR-02): 05-31 closed the >20000-character seed-side door into the settings wedge, but a second, independent door into the identical symptom (every settings save refused as a set, until restart) stands wide open through the gated PUT route's 4 KiB body limit, which trips at roughly 3900 characters of terms text, well inside the 20000-character ceiling 05-31 just made storable and docs/DEPLOY.md now documents as supported. Independently reproduced during this pass (byte-exact math against the real patch shape, matching the reviewer's own numbers)."
    - "WR-06 (new, from 05-REVIEW.md, NOT gating a numbered roadmap truth but undermines 05-32's own stated invariant): refreshCohorts's round guard compares an auth STATUS (get().auth === 'logged-in'), not a session identity, so a logged-in -> logged-out -> logged-in (ABA) sequence within the poll's 8000 ms timeout still lets a dead session's answer repaint the new session's gated slice, including the broadcast-mode chip. Confirmed by reading the guard directly: both the pre-await capture and the post-await recheck are the same status comparison, unlike pollDetail's cohort-IDENTITY comparison. Recorded as a warning, not a blocker, because it does not falsify any of the 7 numbered roadmap Success Criteria on its own."
  regressions: []
gaps:
  - truth: "SC3: The operator reconfigures cohort shape (e.g. capacity, threshold, beacon type for the next cohort) without editing env vars or restarting the process"
    status: failed
    reason: "05-31 genuinely closed the defect it targeted (an over-20000-character SERVICE_NAME/TERMS_TEXT boot seed silently wedging every later settings save). Independently reconfirmed against the shipped module: a boot with a 5000-character serviceName and a 100000-character termsText now warns twice, stores neither, and a following applySettings({ defaultSize: 3 }) succeeds. But a second, structurally identical wedge survives at a lower and far more realistic threshold, discovered by the fresh 05-REVIEW.md re-review (CR-02) and independently reproduced during this pass. The gated PUT /v1/operator/settings route enforces bodyLimit({ maxSize: 4 * 1024, ... }) (packages/service/src/hono-adapter.ts:904) BEFORE the JSON body is parsed. SettingsView.tsx always sends the full form on every save (settingsPatch at SettingsView.tsx:93-103 unconditionally includes termsText: form.termsText, seeded from the served snapshot), so any save at all carries the stored terms text whether or not the operator touched that field. Reproduced byte-exact (JSON.stringify of a realistic console-shaped patch): 3900 characters of terms produces a 4086-byte body (accepted); 4000 characters produces a 4186-byte body (rejected, 413); 5000, 8000 and 20000 characters all reject at 413. 20000 characters is the exact ceiling 05-31 just made a storable, warning-free, documented value (docs/DEPLOY.md:351-355 now states 'The terms are capped at 20000 characters ... A runtime save above the cap is refused with the limit named' - both clauses are false at any value the console can actually save above roughly 3900). The realistic trigger is an operator setting TERMS_TEXT via env var at boot to a genuine participation-terms document under 20000 characters (SVC-05's whole purpose) but over roughly 3900 (well under one page of text): the seed is accepted and stored (below 05-31's cap), the join flow works correctly with it, and the operator can never again save capacity, threshold, beacon type or the service name from the console without first shortening the terms and restarting - the literal SC3 wedge, reachable without a second env-var edit, since the boot seed alone is sufficient. An existing test (packages/service/tests/lifecycle-routes.spec.ts:577-582) already pins the 413 as correct behavior for an 8 KiB terms body, so this is not merely latent: it is asserted, shipped, and now actively contradicted by round 4's own DEPLOY.md claim."
    artifacts:
      - path: "packages/service/src/hono-adapter.ts"
        issue: "The PUT /v1/operator/settings route's bodyLimit (line ~904) is a bare 4 * 1024 bytes, chosen independently of MAX_TERMS_CHARS (20000, runtime-settings.ts:301), so the route rejects a syntactically valid, holder-accepted patch whenever the always-included termsText field exceeds roughly 3900 characters."
      - path: "packages/service/src/runtime-settings.ts"
        issue: "MAX_TERMS_CHARS (line 301) and applySettings's own terms refusal message (line 774-777, 'Participation terms must be 20000 characters or fewer.') are unreachable over HTTP: no PUT request that could trip that check survives the route's body limit first."
      - path: "packages/web/src/components/operator/SettingsView.tsx"
        issue: "settingsPatch (lines 93-103) unconditionally includes termsText: form.termsText on every save, so a save of an unrelated field (service name, capacity, threshold, beacon type) still carries the full stored terms document and inherits the same 413 wedge."
      - path: "docs/DEPLOY.md"
        issue: "Lines 351-355 (added by 05-31) and line 509 state the terms cap is 20000 characters 'on both paths that set them' and that a runtime save above the cap is 'refused with the limit named'. Both are false for the console save path: the binding limit is the route's 4 KiB body cap at roughly 3900 characters, and the refusal is 'request too large', not the terms-specific message."
    missing:
      - "One derived body-size budget instead of three independently chosen numbers: the route's bodyLimit must be computed from MAX_TERMS_CHARS (plus headroom for the rest of the form and JSON escaping) rather than set to a bare 4 KiB, or MAX_TERMS_CHARS must be lowered to fit under the existing 4 KiB budget with textKnob's bound and both DEPLOY.md figures updated to match in the same edit."
      - "A lifecycle-routes.spec.ts row that PUTs a full console-shaped patch carrying a terms document at exactly the (corrected) ceiling and asserts the status is NOT 413, so the field cap is what actually answers, not the route's byte budget."
      - "Correction of docs/DEPLOY.md:351-355 and :509 so the stated terms cap and the save behavior it describes are true of the console save path, not only of applySettings in isolation."
    prior_round_note: "This is NOT a regression from 05-31/05-32: the route's 4 KiB body limit predates this phase's gap-closure rounds and is unrelated code (hono-adapter.ts, untouched by 05-31 or 05-32). It is a second, independent door into the identical symptom the prior round closed one door of, surfaced by the fresh code review's cross-file trace (holder to createService to hono-adapter route to SettingsView's patch builder) that this verification pass independently reproduced."
deferred:
  - truth: "The transport advert slot is also cleared when a cohort FILLS (keygen-complete), not only on the three settle paths, so a sibling open cohort stays listed but stops being joinable to a freshly connecting participant."
    addressed_in: "Phase 6 (suggested home; not yet claimed by a Phase 6 success criterion)"
    evidence: "Logged in .planning/phases/05-operator-cohort-lifecycle-control/deferred-items.md and documented as upstream limit 1 in docs/UPSTREAM-LIMITS.md. No Phase 5 must-have covers the fill path: 05-01 scopes repair to 'cancel, signing-complete, cohort-failed', all three of which are wired and behaviorally proven. Carried forward unchanged across every verification pass, including this one (no round-4 file touches this path)."
behavior_unverified_items: []
human_verification:
  - test: "Judgment 1 (05-UAT test 3): confirm by eye that no Close button exists anywhere on the console, and accept or refuse the deliberate re-reading of roadmap SC 1 (Closed is an automatic nth-seat stage, not an operator-pressed button, per locked CONTEXT D-01)."
    expected: "No Close control exists anywhere on the console; the Closed stage appears automatically the instant the nth seat fills, narrated with its caption, not as an act the operator performs."
    why_human: "A deliberate divergence from the literal wording of the roadmap goal that only the owner can accept or refuse. The stage's existence, trigger, and caption are automated and passing (packages/web/tests/operator-stage.spec.ts); the absence of a Close button anywhere on the rendered console is not asserted by any test."
  - test: "Judgment 2 (05-UAT test 12): read the Ended-group labels (a completed hermetic co-sign now reads 'Signed', not an anchor claim) and decide whether the change reads as newly honest rather than as a regression."
    expected: "Owner accepts the relabeling as an honest disclosure of what MuSig2 completion does and does not prove about an on-chain anchor."
    why_human: "Pure copy/framing judgment; every label and the anchor-wording guard are automated and passing (packages/web/tests/operator-rows.spec.ts)."
  - test: "Eyes (05-UAT test 13, plus narrowed 5/7/8): at a real browser viewport, walk the operator console list, drill-down (with cancel/finalize/seat-reclaim/funding disclosures all visible at once), health strip, a saved long SERVICE_NAME on both the console and the public directory header, and a long TERMS_TEXT body at a narrow viewport height."
    expected: "Nothing overflows, chips stay on screen, grouping is legible with no horizontal scroll; the long service name renders as plain escaped text on both surfaces without pushing chips off screen; the long terms body scrolls inside its capped container, wraps unbroken tokens, never escapes the card, and join controls stay reachable below it."
    why_human: "Layout/legibility at a real screen size. The render harness is a static server render (no layout engine), so it can prove the capping/wrapping classes are present but never that anything actually fits."
  - test: "Copy (05-UAT test 9, plus two follow-on questions): read the four interpolated test-peer confirm strings, the verbatim 'unknown draft' refusal shown to an operator editing a stale draft, and the duplicated 'Operator console' heading shown on both sides of sign-in."
    expected: "All read correctly, in particular that the test-peer confirm states BEFORE the act that peers co-sign for real with throwaway keys; decide whether 'unknown draft' is the right sentence and whether the signed-in console deserves its own heading."
    why_human: "Copy/framing decisions; the four interpolated strings are pinned by nothing today (a named, not-yet-done follow-up), and the other two are deliberate, already-shipped choices awaiting the owner's sign-off."
  - test: "Hands (05-UAT tests 2, 4, 6, 10, 11): exercise 'Cancel edit' on a draft in place; open the finalize and ordinary-cancel confirms and read their in-place wording plus the in-flight disable; load the public directory paused/unpaused and narrow the window; cancel a cohort a participant is seated in and read their terminal card; dismiss an ended cohort row and refresh the page."
    expected: "Cancel edit closes the form with the draft unchanged; finalize/cancel confirms state the real consequence before it happens and disable correctly while in flight; the paused notice and empty states render distinctly and the controls row wraps at narrow widths; the cancel narration and next-step line appear together on the participant's terminal card; a dismissed row stays gone after a refresh."
    why_human: "The render harness is a static server render: no events fire, no effects run, so no state a click produces is reachable from it. Needs a DOM environment or a browser leg that does not exist yet."
  - test: "Environment 1 (05-UAT test 14): follow docs/DEPLOY.md from a clean machine using only the document (sign in, create, advertise, join, rehearse)."
    expected: "Every new control is reachable from the document alone; the retired filler knob appears nowhere; MIN_PARTICIPANTS/AUTO_FALLBACK/SSE_DEBUG and the /v1/config sample match what the service actually reads and serves. This walk should also surface CR-02 directly: an operator who follows the document's own stated 20000-character terms guidance and then tries to save any other setting will hit the 413 wedge."
    why_human: "Needs a clean machine and a person following written instructions verbatim; not something a unit or e2e test can stand in for."
  - test: "Environment 2 (05-UAT test 15, narrowed, updated expectation): try a real host that blocks browser requests (CORS) and a real wrong-chain third-party esplora host against this service's actual default network."
    expected: "browser-rejected and mismatch (naming both chains) appear correctly, with the explicit switch-back control offered on failure and no silent fallback in either direction. This confirms SC6 (PART-05) at the code level, closed by 05-29, holds against a real host, which is the one thing the hermetic suite cannot prove."
    why_human: "Real CORS behavior and a real wrong-chain host cannot be produced hermetically."
  - test: "Environment 3 (05-UAT test 16): export the unsigned PSBT, sign it in an actual desktop or hardware wallet, return it, and broadcast; also confirm a large pasted PSBT keeps its field scrolling internally."
    expected: "The wallet accepts the exported .psbt; the returned PSBT validates against the template; the broadcast produces the same txid the local path would; the large-PSBT field does not reflow the step."
    why_human: "No wallet interoperability was verified in this environment; the 05-14 prohibition correctly forbids claiming any specific wallet works without this walk."
  - test: "Environment 4 (05-UAT test 17, narrowed): boot with LIVE=1 BROADCAST=1, engage Disable broadcast, and confirm the real boot behavior plus the disclosed cost of the stand-down."
    expected: "No route turns broadcast back on; the health strip still reports the live boot mode with a separate warn chip; a cohort advertised after the switch shows no Funding card while one advertised before keeps its funding surface; the owner accepts that the esplora-reachability badge stops refreshing once only post-switch cohorts remain."
    why_human: "Needs a real LIVE+BROADCAST boot, which cannot be produced hermetically, plus an owner cost/benefit judgment that only the owner can make."
  - test: "Cancel settle-race backstop (05-01, verification: backstop): attempt to cancel a cohort at the instant its signing round completes."
    expected: "The click is answered 404 with nothing changed, and the console never shows two ended records for one cohort id."
    why_human: "A timing race that cannot be reliably driven from a test harness; classified 'backstop' by design. What is checkable about it (the opaque 404 on an already-settled cohort, and idempotent folding so no duplicate ended record is ever filed) is automated and passing."
---

# Phase 5: Operator Cohort Lifecycle Control Verification Report

**Phase Goal:** The operator runs aggregation and manages a cohort's lifecycle (open, then close, then finalize) and pauses, cancels, or reconfigures advertising from the console, without restarting the process, removing the last hardwired, uncontrollable behavior.
**Verified:** 2026-08-03
**Status:** gaps_found
**Re-verification:** Yes. This is the fifth verification pass. The prior `05-VERIFICATION.md` (dated 2026-08-02, `status: gaps_found`, score 6/7, FAILED on SC3 due to the string-seed wedge, CR-01) drove a fourth gap-closure round (05-31, 05-32), executed 2026-08-03. This report independently confirms both round-4 targets are genuinely closed for the cases they targeted, and independently confirms a fresh code review's finding (05-REVIEW.md CR-02) that a second, structurally identical defect leaves SC3 FAILED again, through a different door.

## Method

I did not trust SUMMARY.md narrative, 05-REVIEW.md's own severity labels, or the prior VERIFICATION.md's gap list as complete. For every claim that mattered to a Success Criterion, I re-derived it from the live tree.

- Ran `pnpm test` directly: **68 files, 1234 tests, all passing** (matches 05-32-SUMMARY.md's claimed total, confirmed independently, up from the 1215/1227 baselines). No `TBD`/`FIXME`/`XXX` markers and no em-dashes in any file the round-4 plans touched.
- Read `packages/service/src/runtime-settings.ts` in full and independently reproduced 05-31's fix by executing the shipped module directly (`bun`, not the test suite): `createRuntimeSettings({ serviceName: 'x'.repeat(5000), termsText: 'y'.repeat(100000) })` now warns twice (naming both lengths, both ceilings and the consequence of each drop), stores neither value, and a following `applySettings({ serviceName: 'renamed' })` succeeds. Read `textKnob` and confirmed it is a genuine warn-and-drop helper, not exported, applied at both seed sites.
- Read the discovery-window quantizer reorder directly and confirmed a `COHORT_TTL_MS=90000` boot now prints exactly one warning naming `COHORT_TTL_MS` and `DEFAULT_DISCOVERY_WINDOW_MS`, not the previous two lines of which one was false.
- Read `packages/web/src/stores/operator.ts`'s `refreshCohorts` directly and confirmed the session round guard is real for the case it was written against: the pre-await capture and post-await recheck genuinely discard a fast-401-then-slow-ok answer. Then, following the fresh review's cross-file trace, read the guard's actual comparison (`get().auth === 'logged-in'` at both the capture and the recheck) against `pollDetail`'s comparison (`current.cohortId !== askedFor`, an identity comparison) and confirmed the review's WR-06 finding: the list-read guard compares a STATUS, not a session identity, so a logged-in -> logged-out -> logged-in sequence inside the poll's window is not caught by either half of the guard. This is a real gap in the guard's own stated invariant, though it does not by itself falsify any of the 7 numbered roadmap Success Criteria.
- Read `packages/service/src/hono-adapter.ts`'s `PUT /v1/operator/settings` route directly and confirmed the fresh review's CR-02 finding independently, without relying on the review's own numbers: `bodyLimit({ maxSize: 4 * 1024, ... })` gates the route BEFORE `c.req.json()` parses the body, and `packages/web/src/components/operator/SettingsView.tsx`'s `settingsPatch` (lines 93-103) unconditionally includes `termsText: form.termsText` in every save, sourced from the served snapshot. I independently computed the byte math for a realistic console-shaped patch (`bun -e`, not copied from the review) and got the same breakpoint the review reported: 3900 characters of terms text produces a 4086-byte body (accepted), 4000 characters produces 4186 bytes (413), and every value up to and including 05-31's own new 20000-character ceiling also rejects at 413. Confirmed `packages/service/tests/lifecycle-routes.spec.ts:577-582` already pins the 413 as correct behavior for an 8 KiB terms body, so this is asserted and shipped, not merely latent.
- Read `docs/DEPLOY.md`'s round-4 terms paragraph (lines 351-355) and the `TERMS_TEXT` row (line 509) directly and confirmed both state a 20000-character cap "on both paths that set them" with a save above the cap "refused with the limit named" - both false for the console save path, which is bound by the route's 4 KiB body limit at roughly 3900 characters, refused with `request too large`, not the terms-specific message.
- Independently re-ran `pnpm e2e:pause` and `pnpm e2e:cancel` to confirm SC2 and SC4 hold unaffected by round 4 (neither round-4 plan touches the pause/advertise-gate or cancel-visibility path): both PASSED.
- Reran `packages/service/tests/tos.spec.ts` (28 tests, SVC-05/SC5) and confirmed no round-4 file touches this surface.
- Reran `packages/web/tests/tx-client.spec.ts` (55 tests, PART-05/SC6) and confirmed no round-4 file touches this surface.
- Confirmed `packages/shared/tests/psbt.spec.ts` and `packages/web/tests/psbt.spec.ts` (PART-06/SC7) are unaffected by round 4.
- Read `05-UAT.md` in full: `status: testing`, 16 items, still all `result: [pending]`, unchanged since the prior pass.
- Cross-referenced `REQUIREMENTS.md`: exactly SVC-04, SVC-05, PART-05, PART-06 map to Phase 5; all four appear in PLAN frontmatter across the 32 plans (05-31: SVC-04, 05-32: SVC-04). No orphaned requirements. The traceability table currently reads `SVC-04 | Phase 5 | Complete` (written by the `4a2445a` commit that recorded round-4 completion) and `SVC-05`/`PART-05`/`PART-06 | Phase 5 | Gaps Found`. Given this pass's finding (CR-02 falsifies SC3, which is SVC-04's own criterion 3), the `SVC-04: Complete` entry is premature and should revert to `Gaps Found`, matching the pattern the phase already followed once before (commit `118cb9b` reverted an earlier premature `Complete` write for the same reason).
- Read `05-REVIEW.md` in full (10 findings: 1 critical/CR-02, 5 warnings including the carried WR-03/04/05, 4 info) and independently weighed CR-02 and WR-06 against the 7 roadmap Success Criteria rather than accepting the review's severity labels uncritically. Both are confirmed real by direct code read and independent reproduction, not merely cited from the review.

## Goal Achievement

### Observable Truths (roadmap Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Operator moves a cohort through open, close and finalize from the console and the directory reflects each state change | ✓ VERIFIED | Unaffected by round 4 (no round-4 file touches `LifecycleActions.tsx` or `pollDetail`'s cohort-keyed guard). Confirmed by rerunning `operator.spec.ts` (part of the 1234-test green pass) and `lifecycle.spec.ts`. |
| 2 | Operator pauses or cancels advertising so new cohorts stop being offered, without killing the running service | ✓ VERIFIED | `pnpm e2e:pause` (rerun directly during this pass, PASSED): `GET /v1/status` reports `paused:true` while still listing/counting the pre-pause cohort, a fresh participant still seats in it, a new advertise is refused 409, and resume restores both bits. Unaffected by round 4. |
| 3 | Operator reconfigures cohort shape (capacity, threshold, beacon type for the next cohort) without editing env vars or restarting | ✗ FAILED | 05-31 genuinely closed the defect it targeted (the >20000-character string-seed wedge, CR-01), independently reconfirmed against the shipped module. But a second, structurally identical wedge (review CR-02) survives at a far more realistic threshold: `PUT /v1/operator/settings`'s 4 KiB body limit (`hono-adapter.ts:904`) rejects any save at all once the always-included `termsText` field (sent on every save regardless of what the operator edited, `SettingsView.tsx:93-103`) exceeds roughly 3900 characters, well inside the 20000-character ceiling 05-31 just made storable and `docs/DEPLOY.md` now documents as the supported cap. Independently reproduced with byte-exact math (see Method). An operator who sets a realistic `TERMS_TEXT` document via env at boot (under 20000 characters, SVC-05's whole purpose) can permanently lose the ability to save capacity, threshold, beacon type or the service name from the console, with a `request too large` message that names no field and no remedy, until restart. This directly falsifies roadmap SC3 and the "without editing env vars or restarting" clause of SVC-04, exactly as the prior gap did through a different door. See Gap 1 (this pass). |
| 4 | A canceled or closed cohort no longer appears as joinable in the participant directory | ✓ VERIFIED | `pnpm e2e:cancel` (rerun directly, PASSED): the canceled cohort leaves the public directory and open count immediately; a still-open sibling seats a freshly constructed participant afterward. Unaffected by round 4. |
| 5 | Participation terms accepted at join and recorded as a DID-signed, server-verified, terms-hash-bound artifact, with the app-level enforcement boundary disclosed honestly (SVC-05) | ✓ VERIFIED (code level) | `packages/service/tests/tos.spec.ts` (28 tests, rerun via the full `pnpm test` pass, all green). No round-4 file touches this surface. The CR-02 wedge affects the operator's ability to later RESAVE other settings once a mid-length terms document is stored; it does not affect the acceptance/verification/hash-binding mechanism itself, which reads whatever terms value is currently stored regardless of how it got there. Rendered composition with terms actually set: human item (Eyes). |
| 6 | Participant can point browser chain reads at their own esplora endpoint, with a network-mismatch guard, four distinguishable failure messages, no silent fallback, real-funds guard rails unweakened (PART-05) | ✓ VERIFIED (code level) | No round-4 file touches `esplora.ts` or `tx-client.ts`. Confirmed by rerunning `tx-client.spec.ts` (55/55, part of the 1234-test pass), which includes the default-network foreign-chain matrix and the whole-registry no-new-acceptance row 05-29 added. The live leg (a real CORS-blocking host and a real wrong-chain host, 05-UAT test 15) still needs a person; see Human Verification. |
| 7 | Participant can sign the registration transaction in their own wallet through a PSBT round trip validated against the exact template before anything is broadcast (PART-06) | ✓ VERIFIED (code level) | No round-4 file touches this surface. Confirmed via the full `pnpm test` pass (`packages/shared/tests/psbt.spec.ts`, `packages/web/tests/psbt.spec.ts`, both green). Real wallet interoperability: human item (Environment). |

**Score:** 6/7 truths verified (0 present, behavior-unverified)

### Deferred Items

Items not yet met but explicitly addressed in later milestone phases.

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | The transport advert slot is also cleared when a cohort FILLS (keygen-complete), not only on the three settle paths, so a sibling open cohort stays listed but stops being joinable to a freshly connecting participant | Phase 6 (suggested, not yet claimed) | `deferred-items.md` + `docs/UPSTREAM-LIMITS.md` limit 1. Not a Phase 5 must-have: 05-01 scopes repair to the three settle paths, all wired and behaviorally proven. Carried forward unchanged across every verification pass, including this one. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/service/src/runtime-settings.ts` (`textKnob`, quantizer reorder) | Bounded free-text boot seeds; a clamp warning that is true whenever it prints | ✓ VERIFIED | Read in full; `textKnob` applied at both `serviceName` and `termsText` seed sites, not exported; discovery-window quantizer moved onto the seed ahead of the ceiling comparison. Independently reproduced against the shipped module. |
| `packages/service/tests/runtime-settings.spec.ts` | Hostile-seed rows for both string fields; string half of `expectHolderInvariants`; real-boot over-long-terms row; single-accurate-warning pin | ✓ VERIFIED | Present, and load-bearing per the SUMMARY's recorded mutation reverts (each independently spot-confirmed by reading the mutation output tables in `05-31-SUMMARY.md`). |
| `packages/web/src/stores/operator.ts` (`refreshCohorts` guard) | Session round guard discarding an answer that outlived the asking session | ⚠️ PARTIALLY VERIFIED | The guard is real and closes the reproduced fast-401/slow-ok race (confirmed directly). It does not close the ABA case (logged-in -> logged-out -> logged-in) because it compares a status rather than a session identity (WR-06, confirmed by direct code read). |
| `packages/web/tests/operator.spec.ts` | Concurrent-read regression rows for the session guard | ✓ VERIFIED (for the cases covered) | 7 new rows present and load-bearing per the SUMMARY's mutation tables; no row exercises the ABA sequence, consistent with the guard not covering it. |
| `packages/service/src/hono-adapter.ts` (`PUT /v1/operator/settings` route) | A settings save surface whose body limit does not itself wedge a holder-accepted patch | ✗ NOT ADDRESSED THIS ROUND | `bodyLimit({ maxSize: 4 * 1024, ... })` (line ~904) predates round 4, is untouched by 05-31/05-32, and independently reproduces the CR-02 wedge at roughly 3900 characters of terms text, well under the 20000-character ceiling 05-31 made storable. |
| `docs/DEPLOY.md` (`TERMS_TEXT` row and terms paragraph) | Accurate statement of the terms cap and the save-path consequence of exceeding it | ✗ STALE | Lines 351-355 and 509 state a 20000-character cap "on both paths that set them" with the limit "named" on refusal; both are false for the console save path, whose binding limit is the route's 4 KiB body cap. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `demo-server.ts` (`SERVICE_NAME`/`TERMS_TEXT` env reads) | `runtime-settings.ts` (`createRuntimeSettings`) | Comment states where the trim happens and where the length bound is enforced | ✓ WIRED | `git diff -U0 packages/service/src/demo-server.ts` from the round-4 commits shows comment-only changes; the comment now correctly states the bound lives at the holder. |
| `runtime-settings.ts` (`createRuntimeSettings`) | `hono-adapter.ts` (`GET /v1/config`, `PUT /v1/operator/settings`) | The stored terms text is served publicly on `GET /v1/config` and re-validated per request by `applySettings` | ⚠️ PARTIAL | The holder-side bound (20000 chars) is wired to `GET /v1/config` correctly. It is NOT effectively wired to the `PUT` save path: the route's independent 4 KiB body limit intercepts before `applySettings` ever runs, so `applySettings`'s own 20000-character refusal is unreachable over HTTP for any value that would actually trip it via a normal console save. |
| `SettingsView.tsx` (`settingsPatch`) | `hono-adapter.ts` (`PUT /v1/operator/settings`) | The console always sends the full form, including `termsText`, on every save | ✓ WIRED (and this is exactly why CR-02 bites every field, not only the terms field) | Confirmed at `SettingsView.tsx:93-103`: `termsText: form.termsText` is unconditional. |
| `OperatorConsole.tsx` (list poll) | `operator.ts` (`refreshCohorts`) | The session round guard | ✓ WIRED (for the fast-401/slow-ok case); ⚠️ NOT WIRED for the ABA case | Confirmed by direct code read; see WR-06 above. |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| SVC-04 | 05-01..05-32 (core wave) | Lifecycle control, pause/cancel/reconfigure advertising without restart | ✗ GAPS FOUND | SC1, SC2, SC4 verified. SC3 (reconfigure without restart) FAILED on CR-02. The traceability table's current `Complete` entry (written by commit `4a2445a`) should revert to `Gaps Found`; this is the second time this requirement has been prematurely marked complete only to be reopened by a fresh finding (the first reversion was commit `118cb9b`). |
| SVC-05 | 05-13 | Participation terms, DID-signed, server-verified, hash-bound, honest boundary disclosure | ✓ SATISFIED (code level) | `tos.spec.ts` 28/28 green, unaffected by round 4. Human Eyes item remains for rendered composition. Traceability table already correctly reads `Gaps Found` pending that human item; no change needed. |
| PART-05 | 05-11, 05-29 | Participant-supplied esplora endpoint, mismatch guard, four failure messages, no silent fallback | ✓ SATISFIED (code level) | `tx-client.spec.ts` 55/55 green, unaffected by round 4. Human Environment item remains. Traceability table already correctly reads `Gaps Found`; no change needed. |
| PART-06 | 05-14 | PSBT wallet round trip, template-validated, guarded broadcast | ✓ SATISFIED (code level) | `psbt.spec.ts` suites green, unaffected by round 4. Human Environment item remains. Traceability table already correctly reads `Gaps Found`; no change needed. |

No orphaned requirements: `SVC-04`, `SVC-05`, `PART-05`, `PART-06` are exactly the four requirements mapped to Phase 5 in `REQUIREMENTS.md`, and all four appear in PLAN frontmatter across the 32 plans.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `packages/service/src/hono-adapter.ts` | ~904 | Independently-chosen numeric constant (`4 * 1024`) that silently conflicts with a related cap (`MAX_TERMS_CHARS = 20000`) defined in a different file | 🛑 Blocker | This is CR-02: the route's body limit is the actual binding constraint on the settings save surface, not the holder's own documented and tested cap, and the two disagree by roughly 5x. |
| `docs/DEPLOY.md` | 351-355, 509 | Documentation asserting a behavior (`refused with the limit named`) that does not match the shipped route | ⚠️ Warning | An operator following the runbook's own stated guidance (set terms up to 20000 characters) will be told an inaccurate story about what happens if they exceed it, when in fact a much smaller value already wedges the console. |
| `packages/web/src/stores/operator.ts` | 899, 926-928 | A guard comment claims an invariant ("the re-check rejects an answer that outlived the session that did ask") that the code does not fully implement (status comparison, not identity comparison) | ⚠️ Warning | WR-06: real, but does not by itself falsify a numbered roadmap Success Criterion. Recorded for a future round; not gating this verification's status beyond its own honesty. |

No `TBD`, `FIXME`, or `XXX` markers found in any round-4-touched file. No em-dash found in any round-4-touched file.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full test gate is green after round 4 | `pnpm test` | 68 files, 1234 tests, all passing | ✓ PASS |
| Pause/resume advertising leaves the service and directory up | `pnpm e2e:pause` | PASSED | ✓ PASS |
| A canceled cohort disappears from the directory immediately | `pnpm e2e:cancel` | PASSED | ✓ PASS |
| An over-long boot seed no longer wedges the settings surface | `bun -e 'createRuntimeSettings({ serviceName: "x".repeat(5000), termsText: "y".repeat(100000) })'` (direct module execution, not test suite) | Two warnings printed, both fields stored `undefined`, a following `applySettings({ serviceName: 'renamed' })` succeeds | ✓ PASS |
| A mid-length, holder-accepted terms document still wedges the settings surface via the route body limit | `bun -e` computing `JSON.stringify` byte length of a realistic console-shaped patch at 3900/4000/5000/8000/20000-character terms | 3900 -> 4086 bytes (accepted); 4000, 5000, 8000, 20000 -> all over 4096 bytes (413) | ✗ FAIL (this is the gap) |

### Probe Execution

Not applicable. This phase has no `scripts/*/tests/probe-*.sh` convention; verification relies on the vitest suite, the `e2e:*` harness scripts, and direct module execution.

## Human Verification Required

16 items carried forward unchanged from the prior pass (see frontmatter `human_verification` and `05-UAT.md`, `status: testing`, all `result: [pending]`). None of the round-4 changes altered any of these items' preconditions except Environment 1 (05-UAT test 14, the DEPLOY.md walk-through), which should now also surface the CR-02 wedge directly if walked before the gap is closed.

## Gaps Summary

Round 4 (05-31, 05-32) genuinely closed the two defects it targeted: the over-20000-character string-seed wedge (CR-01) and the false clamp warning (W6), both independently reconfirmed against the shipped code in this pass, and the fast-401-then-slow-ok list-read race (W5/WR-01), also independently reconfirmed. All three fixes are load-bearing, proven by the mutation-revert evidence recorded in their SUMMARYs and spot-confirmed here.

However, roadmap SC3 remains FAILED. A fresh code review (05-REVIEW.md, dated after round 4) found and this verification pass independently reproduced a second, structurally identical wedge (CR-02): the gated settings-save route's own 4 KiB body limit, unrelated code that predates this phase's gap-closure rounds, intercepts any settings save once the terms text (always sent, whether touched or not) exceeds roughly 3900 characters, well inside the very ceiling round 4 just made storable and the very ceiling round 4's own documentation update now advertises as supported. The practical consequence for an operator is identical to the defect round 4 closed: a realistic participation-terms document, set once at boot via `TERMS_TEXT`, permanently locks the console's settings surface until restart, with an opaque `request too large` error that names no field and no remedy.

This is not a regression introduced by 05-31 or 05-32 (neither plan touches `hono-adapter.ts`). It is a pre-existing defect that the phase's own gap-closure discipline (test-first, mutation-proof, then a fresh review pass) surfaced by tracing the fix's boundary all the way to the route that calls it, rather than stopping at the holder's own unit tests. The same discipline that closed CR-01 (the invariant predicate not covering every field) applies here: `applySettings`'s own cap is not the binding constraint on the surface an operator actually uses, and nothing in the round-4 test suite exercised a save through the real HTTP route at a value between roughly 3900 and 20000 characters.

Additionally, WR-06 (the list-read guard's status-vs-identity comparison) is recorded as a warning: real, confirmed, and worth a future round, but not gating this verification's status since it does not falsify any of the 7 numbered roadmap Success Criteria on its own (unlike the case W5/CR-01 originally reproduced, which did repopulate a cleared gated slice under an ordinary poll race with no sign-in in between).

The `REQUIREMENTS.md` traceability table's `SVC-04 | Phase 5 | Complete` entry should revert to `Gaps Found` given this finding, matching the phase's own established precedent (commit `118cb9b`) for correcting a premature completion mark.

---

_Verified: 2026-08-03_
_Verifier: Claude (gsd-verifier)_
