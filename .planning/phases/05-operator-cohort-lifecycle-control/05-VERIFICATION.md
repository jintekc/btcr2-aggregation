---
phase: 05-operator-cohort-lifecycle-control
verified: 2026-08-04T18:30:00Z
status: human_needed
score: 7/7 roadmap Success Criteria verified at code level (0 present, behavior-unverified); 16 human-verification items still pending (unchanged in count); all seven round-7 targets assigned to round 8 (WR-13, WR-14, WR-15, IN-15, IN-16, IN-17, IN-18) independently confirmed closed by direct code read and by an independent reproduction of the full gate; the round-8 re-review (05-REVIEW.md) found ZERO new critical findings for the second consecutive round, three new warnings (WR-16, WR-17, WR-18) and three new info findings (IN-19, IN-20, IN-21), all independently confirmed real by direct code read in this pass, none of which falsifies a numbered roadmap Success Criterion
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: "7/7 roadmap Success Criteria verified at code level (per 05-VERIFICATION.md pass 8, dated 2026-08-04T01:15:00Z, before the eighth gap-closure round). Pass 8 recorded three new warnings (WR-13, WR-14, WR-15) and four new info findings (IN-15..18), all narrower in blast radius than the critical finding round 7 closed, and recommended an eighth gap round before /gsd-secure-phase or milestone completion."
  gaps_closed:
    - "WR-13 (eleven gated action verbs guarded only their unauthorized branch, so a dead session's success, refusal or transport answer still wrote into whichever session was live when it landed): all twelve gated action verbs now carry one stillAsking(get, askedInRound) comparison directly below their unauthorized branch, covering the ok, refused, declined, unreachable and catch branches together. Confirmed by direct code read of runAdvertisingToggle, advertise, cancelCohort and exportCohort at packages/web/src/stores/operator.ts, and by grep: 'auth === logged-in'' appears exactly once in the file, inside askingRound's own body (line 918)."
    - "The advertise drill-down landing (the last status-as-identity comparison in the file) no longer reads the auth status; it re-reads stillAsking on the far side of the post-success list re-read, closing the reviewer's reproduced cross-session navigation. Confirmed by direct code read at operator.ts around the advertise success branch."
    - "IN-18 (GATED_SLICE_RESET's four nested containers were the same shared instances on every session end): the constant became gatedSliceReset(), a factory called at both signOut and expireSession, minting fresh containers per call. Confirmed by direct code read; no module-scope GATED_SLICE_RESET constant remains."
    - "WR-14 (submitDraft was a sixteenth gated call site the enumeration never named, so a 401 on cohort creation rendered the service's raw denial string as create-form validation copy): CreateDraftResult gained the unauthorized member UpdateDraftResult already carried, createDraft checks 401 before its 201 branch, and submitDraft is now shaped exactly like saveDraftEdit with a captured round and a guard. Confirmed by direct code read of packages/web/src/lib/operator.ts's CreateDraftResult and packages/web/src/stores/operator.ts's submitDraft."
    - "WR-15's narration half (a 5xx probe answer narrated a session-expired sentence the service never sent): SessionState gained an 'unreachable' member and sessionProbe now maps 401 apart from every other unexpected status, with a shared probeUnreachable() helper writing the honest unreachable copy instead of the session-expired copy. Confirmed by direct code read of packages/web/src/lib/operator.ts's sessionProbe (four-case mapping: 200/404/401/other) and probeUnreachable in the store. NOTE: this closes only the false COPY; the session is still ended on the fault (see WR-17 below, a residual the round-8 review found and this pass independently confirmed)."
    - "IN-15 (a probe answer landing after a session boundary could re-establish a phantom session for one tick): probe now captures its own round directly off the store before its await and refuses to start a session when a boundary was crossed while the question was in flight. Confirmed by direct code read of the probe method's session-START branch."
    - "IN-17 (both boot-refusal consequence clauses promised an in-session repair through an operator settings surface that a fail-closed boot with no OPERATOR_PASSWORD never mounts): RuntimeSettingsSeed gained operatorSurfaceMounted, derived in index.ts from the same operatorPassword binding that constructs operatorAuth, and both consequence clauses now have a mounted and an unmounted variant differing only in the in-session clause. Confirmed by direct code read of packages/service/src/runtime-settings.ts and packages/service/src/index.ts, and by the round's own real-boot test asserting the sentence and the absent settings route together."
    - "IN-16 (docs/DEPLOY.md's participation-terms prose sentence, fourteen lines above the two corrected environment rows, still named shortening as the only repair): corrected in the same voice as the environment rows (cost, in-session repair, environment edit), preceded by a repo-wide inventory of every statement of the fact recorded in the 05-44-SUMMARY.md. Confirmed by direct grep of docs/DEPLOY.md line 358-360 area; the overclaim sentence is gone."
  gaps_remaining: []
  regressions: []
new_findings_this_pass:
  - "WR-16 (CONFIRMED REAL by direct code read, non-gating): IN-15 captured askedAtRound and used it on exactly one branch of probe, the session-START branch. The session-END branch four lines below (`else if (get().liveSessionRound !== undefined) { ... get().expireSession(); ... }`) and the shared probeUnreachable() helper both still decide only from `liveSessionRound !== undefined`, never comparing against askedAtRound. Confirmed directly at packages/web/src/stores/operator.ts: the session-ended branch calls `get().expireSession()` unconditionally once a live session exists, with no round comparison anywhere in that branch or in probeUnreachable. Consequence: a probe issued before a session boundary, landing after an operator has signed back in, ends the BRAND NEW session and narrates the exact SESSION_EXPIRED copy this finding series exists to prevent. Weighed against the 7 numbered SCs: this corrupts the console's own session bookkeeping in a narrow interleaving window; it does not touch the server-side lifecycle actions (open/close/finalize, pause/cancel/reconfigure) or their correctness for the session that actually issues them."
  - "WR-17 (CONFIRMED REAL by direct code read, non-gating): probeUnreachable calls get().expireSession() whenever ANY session is live, on every transient fault (5xx, thrown fetch), not only on stale answers. Confirmed directly: `function probeUnreachable(set, get) { if (get().liveSessionRound !== undefined) { get().expireSession(); } set({ auth: 'logged-out', error: UNREACHABLE }); }`, with no distinction between a fault answering the currently-live session and a fault answering a dead one. This is a genuine inconsistency with the D-25 freeze posture the store follows everywhere else (refreshCohorts sets listStale and retries; pollDetail freezes; loadSettings keeps its snapshot); only the probe converts a transport fault into a full logout. Deployment consequence: one reverse-proxy reload or one container restart behind a load balancer costs the operator their whole console and a fresh password entry, on a service that never refused their cookie. The round-8 SUMMARY's characterization ('a 502 probe now freezes the console') does not match what is shipped; the shipped behavior logs the operator out. Weighed against the 7 numbered SCs: the underlying pause/cancel/reconfigure and lifecycle-control mechanisms still function correctly once the operator re-authenticates; the dashboard and resolve routes described in SC2 stay up (the SERVICE is not killed, only the console's own session state is dropped). This is a real UX/robustness defect, not a falsification of a numbered SC."
  - "WR-18 (CONFIRMED REAL by direct code read, non-gating): downloadExport in packages/web/src/lib/operator.ts wraps only the fetch call in its try/catch; `const blob = await res.blob();` at the line immediately after sits outside that try. exportCohort in the store has no try/catch around apiDownloadExport at all, unlike submitDraft, saveDraftEdit and saveSettings, the other three verbs whose client functions can throw. Confirmed directly by reading both files. Consequence: a body that fails mid-transfer (exactly the failure mode a large monitoring export invites) produces an unhandled promise rejection: no download, no actionError, no re-login, no log line. This reopens the exact silent-no-op class WR-06 (round 6) closed, in the one case WR-06 did not enumerate. Not a Phase-5 roadmap SC surface (the export feature is part of SVC-03 monitoring, Phase 4)."
  - "IN-19 (CONFIRMED REAL by direct grep): docs/DEPLOY.md lines 345 and 491 offer the in-session settings-surface repair without the '(present when OPERATOR_PASSWORD is set)' qualifier IN-17 added to the three other statements of the same fact (lines 359, 512, 518). Confirmed directly: grep for 'settings surface' finds five hits, two unqualified. The round-44's own documentation-count row measures the QUALIFIED count (asserts exactly 3) rather than measuring the absence of unqualified statements outside the section that defines the term, so an unqualified fourth statement can be added without failing the row. Not gating; a reader with no operator password is still pointed at a surface their boot does not mount in two places."
  - "IN-20 (CONFIRMED REAL by direct code read): four setTimeout calls (approximately lines 1011, 1466, 1510, 1794 in packages/web/src/stores/operator.ts) clear a transient confirmation field guarded only by message-identity equality, with no round comparison. Confirmed by grep for setTimeout and reading each guarded clear. Cosmetic harm only (a good-tone confirmation can disappear a couple of seconds early for a different, later session); never a false claim, since the field being cleared is being set back to undefined."
  - "IN-21 (CONFIRMED REAL by direct code read): askingRound returns undefined whenever auth !== 'logged-in' and stillAsking(get, undefined) is false by definition (`asked !== undefined && ...`), so a gated verb's early return on a false stillAsking leaves its own in-flight marker (cancelling, finalizing, etc.) set with nothing clearing it, UNLESS the false comparison came from a session end (which clears everything via gatedSliceReset). Confirmed directly at packages/web/src/stores/operator.ts's askingRound/stillAsking definitions. Currently unreachable in the shipped UI because OperatorConsole replaces the whole console with a 'Checking session...' card while auth === 'checking', so no gated control can be clicked during the one window where the capture could be undefined without a session having ended. Latent, not live."
gaps: []
deferred:
  - truth: "The transport advert slot is also cleared when a cohort FILLS (keygen-complete), not only on the three settle paths, so a sibling open cohort stays listed but stops being joinable to a freshly connecting participant."
    addressed_in: "Phase 6 (suggested home; not yet claimed by a Phase 6 success criterion)"
    evidence: "Logged in .planning/phases/05-operator-cohort-lifecycle-control/deferred-items.md and documented as upstream limit 1 in docs/UPSTREAM-LIMITS.md. No Phase 5 must-have covers the fill path. Carried forward unchanged across every verification pass, including this one."
behavior_unverified_items: []
human_verification:
  - test: "Judgment 1 (05-UAT test 3): confirm by eye that no Close button exists anywhere on the console, and accept or refuse the deliberate re-reading of roadmap SC 1 (Closed is an automatic nth-seat stage, not an operator-pressed button, per locked CONTEXT D-01)."
    expected: "No Close control exists anywhere on the console; the Closed stage appears automatically the instant the nth seat fills, narrated with its caption, not as an act the operator performs."
    why_human: "A deliberate divergence from the literal wording of the roadmap goal that only the owner can accept or refuse. The stage's existence, trigger, and caption are automated and passing (packages/web/tests/operator-stage.spec.ts); the absence of a Close button anywhere on the rendered console is not asserted by any test."
  - test: "Judgment 2 (05-UAT test 12): read the Ended-group labels (a completed hermetic co-sign now reads 'Signed', not an anchor claim) and decide whether the change reads as newly honest rather than as a regression."
    expected: "Owner accepts the relabeling as an honest disclosure of what MuSig2 completion does and does not prove about an on-chain anchor."
    why_human: "Pure copy/framing judgment; every label and the anchor-wording guard are automated and passing (packages/web/tests/operator-rows.spec.ts)."
  - test: "Eyes (05-UAT test 13, plus narrowed 5/7/8): at a real browser viewport, walk the operator console list, drill-down (with cancel/finalize/seat-reclaim/funding disclosures all visible at once), health strip, a saved long SERVICE_NAME on both the console and the public directory header, and a long TERMS_TEXT body at a narrow viewport height, including the refused-seed source caption's now-split cost/remedy text."
    expected: "Nothing overflows, chips stay on screen, grouping is legible with no horizontal scroll; the long service name renders as plain escaped text on both surfaces without pushing chips off screen; the long terms body scrolls inside its capped container, wraps unbroken tokens, never escapes the card, and join controls stay reachable below it; the refused-seed caption's cost and remedy read as a warning, not as another grey caption."
    why_human: "Layout/legibility at a real screen size. The render harness is a static server render (no layout engine), so it can prove the capping/wrapping/caption-branch classes are present but never that anything actually fits or reads correctly."
  - test: "Copy (05-UAT test 9, plus two follow-on questions): read the four interpolated test-peer confirm strings, the verbatim 'unknown draft' refusal shown to an operator editing a stale draft, and the duplicated 'Operator console' heading shown on both sides of sign-in."
    expected: "All read correctly, in particular that the test-peer confirm states BEFORE the act that peers co-sign for real with throwaway keys; decide whether 'unknown draft' is the right sentence and whether the signed-in console deserves its own heading."
    why_human: "Copy/framing decisions; the four interpolated strings are pinned by nothing today (a named, not-yet-done follow-up), and the other two are deliberate, already-shipped choices awaiting the owner's sign-off."
  - test: "Hands (05-UAT tests 2, 4, 6, 10, 11): exercise 'Cancel edit' on a draft in place; open the finalize and ordinary-cancel confirms and read their in-place wording plus the in-flight disable; load the public directory paused/unpaused and narrow the window; cancel a cohort a participant is seated in and read their terminal card; dismiss an ended cohort row and refresh the page."
    expected: "Cancel edit closes the form with the draft unchanged; finalize/cancel confirms state the real consequence before it happens and disable correctly while in flight; the paused notice and empty states render distinctly and the controls row wraps at narrow widths; the cancel narration and next-step line appear together on the participant's terminal card; a dismissed row stays gone after a refresh."
    why_human: "The render harness is a static server render: no events fire, no effects run, so no state a click produces is reachable from it. Needs a DOM environment or a browser leg that does not exist yet."
  - test: "Environment 1 (05-UAT test 14): follow docs/DEPLOY.md from a clean machine using only the document (sign in, create, advertise, join, rehearse)."
    expected: "Every new control is reachable from the document alone; the retired filler knob appears nowhere; MIN_PARTICIPANTS/AUTO_FALLBACK/SSE_DEBUG and the /v1/config sample match what the service actually reads and serves. This walk should also confirm the corrected participation-terms paragraph and the SERVICE_NAME/TERMS_TEXT rows' now-qualified in-session-repair wording match observed behavior, and may notice the two remaining unqualified 'settings surface' mentions at lines 345 and 491 (review IN-19, not yet closed)."
    why_human: "Needs a clean machine and a person following written instructions verbatim; not something a unit or e2e test can stand in for."
  - test: "Environment 2 (05-UAT test 15, narrowed, updated expectation): try a real host that blocks browser requests (CORS) and a real wrong-chain third-party esplora host against this service's actual default network."
    expected: "browser-rejected and mismatch (naming both chains) appear correctly, with the explicit switch-back control offered on failure and no silent fallback in either direction. This confirms SC6 (PART-05) at the code level, closed by 05-29, holds against a real host, which is the one thing the hermetic suite cannot prove."
    why_human: "Real CORS behavior and a real wrong-chain host cannot be produced hermetically."
  - test: "Environment 3 (05-UAT test 16): export the unsigned PSBT, sign it in an actual desktop or hardware wallet, return it, and broadcast; also confirm a large pasted PSBT keeps its field scrolling internally."
    expected: "The wallet accepts the exported .psbt; the returned PSBT validates against the template; the broadcast produces the same txid the local path would; the large-PSBT field does not reflow the step."
    why_human: "No wallet interoperability was verified in this environment; the 05-14 prohibition correctly forbids claiming any specific wallet works without this walk."
  - test: "Environment 4 (05-UAT test 17, narrowed): boot with LIVE=1 BROADCAST=1, engage Disable broadcast, and confirm the real boot behavior plus the disclosed cost of the stand-down."
    expected: "No route turns broadcast back on; the health strip still reports the live boot mode with a separate warn chip; a cohort advertised after the switch shows no Funding card while one advertised before keeps its funding surface; the owner accepts that the esplora-reachability badge stops refreshing once only post-switch cohorts remain."
    why_human: "Needs a real LIVE+BROADCAST boot, which cannot be produced hermetically, plus an owner cost/benefit judgment that only the owner can make."
  - test: "Cancel settle-race backstop (05-01, verification: backstop): attempt to cancel a cohort at the instant its signing round completes."
    expected: "The click is answered 404 with nothing changed, and the console never shows two ended records for one cohort id."
    why_human: "A timing race that cannot be reliably driven from a test harness; classified 'backstop' by design. What is checkable about it (the opaque 404 on an already-settled cohort, and idempotent folding so no duplicate ended record is ever filed) is automated and passing."
---

# Phase 5: Operator Cohort Lifecycle Control Verification Report

**Phase Goal:** The operator runs aggregation and manages a cohort's lifecycle (open, then close, then finalize) and pauses, cancels, or reconfigures advertising from the console, without restarting the process, removing the last hardwired, uncontrollable behavior.
**Verified:** 2026-08-04
**Status:** human_needed
**Re-verification:** Yes. This is the ninth verification pass, immediately following the eighth gap-closure round (05-42, 05-43, 05-44), which closed all seven owner-approved findings assigned to it from the round-7 re-review (WR-13, WR-14, WR-15, IN-15, IN-16, IN-17, IN-18) rather than a failed roadmap Success Criterion (pass 8 already scored 7/7 at code level with `gaps: []`). This report independently confirms all seven round-8 targets are genuinely closed at the mechanism, and independently confirms a fresh deep code review (`05-REVIEW.md`, run after round 8 landed) that found ZERO new critical findings for the second consecutive round, three new warnings (WR-16, WR-17, WR-18) and three new info findings (IN-19, IN-20, IN-21) in the same session-identity subsystem rounds 5 through 8 have each rewritten.

## Method

I did not trust SUMMARY.md narrative, `05-REVIEW.md`'s own severity labels, or the prior VERIFICATION.md's frontmatter as complete. For every claim that mattered to a Success Criterion or to a claimed gap closure, I re-derived it from the live tree myself, not by citation.

- Ran `pnpm test` directly (chains `tsc -b` then `vitest run`): **68 files, 1392 tests, all passing**, matching 05-44-SUMMARY.md's own final figure exactly, reproduced independently.
- Ran `pnpm lint` directly: clean, `eslint .` with no additional output.
- Ran `pnpm --filter @btcr2-aggregation/web build` directly: `tsc --noEmit` clean, `vite build` succeeded (714 modules transformed, no errors).
- Ran `pnpm e2e:gate` directly (all thirteen hermetic legs: e2e:operator, e2e:monitor, e2e:cancel, e2e:pause, e2e:testpeers, e2e:fallback, e2e:fallback:operator, e2e:browse, e2e:kofn, e2e:live:mock, e2e:resolve, e2e:config, e2e:persist): all thirteen PASSED, reproduced independently rather than trusting 05-44-SUMMARY.md's own table.
- Read `packages/web/src/stores/operator.ts`'s `runAdvertisingToggle`, `advertise`, `cancelCohort`, `exportCohort` and `saveDraftEdit` directly and confirmed WR-13's fix at the mechanism: each carries one `stillAsking(get, askedInRound)` comparison directly below its `unauthorized` branch, covering every subsequent branch that writes.
- Ran `grep -c "auth === 'logged-in'"` over `packages/web/src/stores/operator.ts`: exactly one hit, inside `askingRound`'s own body, confirming no status-as-identity comparison survives elsewhere in the file.
- Read `packages/web/src/lib/operator.ts`'s `CreateDraftResult` and `packages/web/src/stores/operator.ts`'s `submitDraft` directly and confirmed WR-14's fix: `CreateDraftResult` now carries an `unauthorized` member, `createDraft` checks 401 before its 201 branch, and `submitDraft` is shaped like `saveDraftEdit` with a captured round and a guard.
- Read `packages/web/src/lib/operator.ts`'s `SessionState` and `sessionProbe`, and `packages/web/src/stores/operator.ts`'s `probeUnreachable`, directly and confirmed WR-15's narration half is closed: `sessionProbe` maps 401 apart from every other unexpected status into a fourth `unreachable` value, and `probeUnreachable` writes the honest unreachable copy rather than the session-expired sentence. Separately confirmed, by reading the SAME function, that the underlying session is still ended on any transient fault whenever a session is live (`if (get().liveSessionRound !== undefined) { get().expireSession(); }` with no round comparison), which is the mechanism of the new WR-17 finding, matching the review's own citation exactly rather than trusting it.
- Read `packages/web/src/stores/operator.ts`'s `probe` method in full and confirmed IN-15's fix (the session-START branch captures its own round and refuses to start a session across a crossed boundary) and independently discovered, by reading the session-END branch and `probeUnreachable` four lines below the fix, that neither compares against the captured round before calling `expireSession()`, confirming WR-16 is real at the mechanism the review names.
- Read `packages/web/src/stores/operator.ts`'s `askingRound`/`stillAsking`/`expireIfStillAsking` definitions directly (`asked !== undefined && get().sessionRound === asked`), confirming IN-21's premise that `stillAsking` is false whenever `askingRound` returns `undefined` (i.e., whenever `auth !== 'logged-in'`), and confirmed this window is currently unreachable in the shipped UI because `OperatorConsole` swaps in a "Checking session..." card during `auth === 'checking'`.
- Read `packages/web/src/lib/operator.ts`'s `downloadExport` and `packages/web/src/stores/operator.ts`'s `exportCohort` directly and confirmed WR-18: `res.blob()` sits outside `downloadExport`'s own `try`, and `exportCohort` wraps no `try/catch` around the client call at all, unlike `submitDraft`, `saveDraftEdit` and `saveSettings`.
- Ran `grep -n "settings surface"` over `docs/DEPLOY.md` directly: five hits, two (lines 345, 491) lacking the `(present when OPERATOR_PASSWORD is set)` qualifier the other three (lines 359, 512, 518) carry, confirming IN-19 is real at the exact lines the review cites.
- Ran `grep -n "setTimeout"` over `packages/web/src/stores/operator.ts` directly: four call sites (approximately lines 1011, 1466, 1510, 1794), each guarded only by message-identity equality with no round comparison, confirming IN-20.
- Ran `grep -nE "TBD|FIXME|XXX"` and `grep -rlP '\x{2014}'` over every round-8-touched file: no markers, no em-dash found, independently confirmed.
- Ran `git diff --stat` for `packages/service` across the 05-42/05-43 commit range and for `packages/web/src/components/operator/SettingsView.tsx` across the whole round-8 range: both empty, confirming the round's own prohibitions held.
- Cross-referenced `.planning/REQUIREMENTS.md` directly: the traceability table (lines 74-81) still reads `Gaps Found` for all four Phase 5 requirements (SVC-04, SVC-05, PART-05, PART-06), unedited by round 8 (`git diff --stat 1c6ff5e 99b095c -- .planning/REQUIREMENTS.md` is empty). The pre-existing checkbox/traceability inconsistency noted in pass 8 is unchanged, not reintroduced or worsened by this round.
- Read `05-UAT.md` directly: `pending: 16`, byte-identical in count to the prior pass; round 8's plans each explicitly prohibited touching it.
- Read `05-REVIEW.md` in full (the round-8 re-review: 0 critical, 3 warnings, 3 info, `status: issues_found`) and independently weighed WR-16, WR-17, WR-18 and the three new info findings against the 7 roadmap Success Criteria rather than accepting the review's own framing. Reproduced each finding's core claim by direct code read of the unmodified source.

## Goal Achievement

### Observable Truths (roadmap Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Operator moves a cohort through open, close and finalize from the console and the directory reflects each state change | VERIFIED | Round 8 touches only the session-identity subsystem and boot-warning copy; no round-8 file touches `LifecycleActions.tsx` or the automatic-close mechanism 05-28 added. Confirmed by the full `pnpm test` pass (`operator.spec.ts`, `lifecycle.spec.ts`, both green) and by `e2e:cancel`/`e2e:operator` passing independently. New findings WR-16/WR-17 do not affect this mechanism: the underlying lifecycle actions still execute correctly server-side for whichever session issues them; what the findings corrupt is the CONSOLE's own session bookkeeping in narrow race windows, not the server-side state machine. Human judgment item (Judgment 1) remains for the "no Close button" reading. |
| 2 | Operator pauses or cancels advertising so new cohorts stop being offered, without killing the running service | VERIFIED | No round-8 file touches the pause/advertise-gate mechanism beyond the every-branch session guard (WR-13, confirmed closed) applied to `runAdvertisingToggle`. Confirmed by the full `pnpm test` pass and `e2e:pause` passing independently. WR-17 (a transient probe fault logs the operator out) does not kill the service, does not disable pause/cancel/reconfigure once the operator re-authenticates, and does not take the dashboard or resolve routes down; it is a console-side UX defect in session bookkeeping, not a service-availability defect. |
| 3 | Operator reconfigures cohort shape (capacity, threshold, beacon type for the next cohort) without editing env vars or restarting | VERIFIED | The save mechanism itself (`PUT /v1/operator/settings`) is unchanged by round 8: `git diff --stat` on `hono-adapter.ts` across the whole round is empty, confirmed directly. IN-17's fix (the boot-warning consequence clauses) is copy/disclosure only. Confirmed by the full `pnpm test` pass and `pnpm typecheck` clean. |
| 4 | A canceled or closed cohort no longer appears as joinable in the participant directory | VERIFIED | Unaffected by round 8. Confirmed by the full `pnpm test` pass and by `e2e:browse`/`e2e:kofn` passing independently; no round-8 file touches the directory-visibility path. |
| 5 | Participation terms accepted at join and recorded as a DID-signed, server-verified, terms-hash-bound artifact, with the app-level enforcement boundary disclosed honestly (SVC-05) | VERIFIED (code level) | `packages/service/tests/tos.spec.ts` (28 tests, part of the full 1392-test pass, all green), unaffected by round 8's own changes to that surface (05-44 touched only the boot-warning consequence text, the `docs/DEPLOY.md` prose, and a documentation-inventory pin, not the acceptance/verification mechanism). Human judgment items remain (Judgment 2, Eyes). |
| 6 | Participant can point browser chain reads at their own esplora endpoint, with a network-mismatch guard, four distinguishable failure messages, no silent fallback, real-funds guard rails unweakened (PART-05) | VERIFIED (code level) | No round-8 file touches `esplora.ts` or `tx-client.ts`. Confirmed by the full `pnpm test` pass. The live leg still needs a person; see Human Verification. |
| 7 | Participant can sign the registration transaction in their own wallet through a PSBT round trip validated against the exact template before anything is broadcast (PART-06) | VERIFIED (code level) | No round-8 file touches this surface. Confirmed via the full `pnpm test` pass. Real wallet interoperability: human item (Environment). |

**Score:** 7/7 truths verified (0 present, behavior-unverified)

**Independent note on the new session-identity findings (WR-16, WR-17, WR-18) and the seven numbered SCs:** all three are real (confirmed above by direct code read of the exact functions the review cites, not by trusting its framing). Weighed independently against the literal text of each of the 7 numbered Success Criteria, none falsifies any of them: the underlying lifecycle-control, pause/cancel, reconfiguration and terms-acceptance MECHANISMS all continue to function correctly when explicitly invoked by the operator whose session is asking. WR-16 and WR-17 corrupt only the CONSOLE's own session bookkeeping (a stale or transient-fault probe answer can end a session it has no evidence about), never the server-side lifecycle state a cohort actually holds; WR-18 is a silent-no-op on a monitoring-export click, bounded to one feature that is not itself a numbered Phase-5 SC surface. This is the same reasoning pass 8 applied to WR-13/WR-14/WR-15 before round 8 closed them, now applied a round later to the residuals round 8's own fixes surfaced.

**This is the fourth consecutive round (5, 6, 7, 8) to touch the session-identity subsystem in `packages/web/src/stores/operator.ts`, and the second consecutive round whose own re-review found new, real findings in the exact code it had just finished rewriting.** Round 7's review found zero critical (a first); round 8's review also found zero critical (a second, and the severity trend across all four rounds is flat-to-improving at the critical tier). But WR-16 and WR-17 are not narrower than what round 8 closed in every dimension: WR-17 in particular (any transient fault on a live session ends it) is a plainer, more easily triggered defect than the narrow interleaving windows WR-13/IN-15 fixed, since it needs no race at all, only one dropped packet or one proxy reload. The round-8 SUMMARY's own closing note anticipated this pattern and flagged it explicitly to the owner before this pass began; this pass independently confirms the pattern held rather than resolved.

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | The transport advert slot is also cleared when a cohort FILLS (keygen-complete), not only on the three settle paths, so a sibling open cohort stays listed but stops being joinable to a freshly connecting participant | Phase 6 (suggested, not yet claimed) | `deferred-items.md` + `docs/UPSTREAM-LIMITS.md` limit 1. Not a Phase 5 must-have. Carried forward unchanged across every verification pass, including this one. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/web/src/stores/operator.ts` (all twelve gated action verbs) | Every branch that writes honors the session that asked, not only the `unauthorized` branch (WR-13) | VERIFIED | Read directly; `stillAsking` guards sit below the `unauthorized` branch in `runAdvertisingToggle`, `saveDraftEdit`, `advertise`, `readvertise`, `discard`, `exportCohort`, `cancelCohort`, `finalizeCohort`, `addTestPeers`, `disableBroadcast`, `dismissEnded` and `submitDraft`. `grep -c "auth === 'logged-in'"` prints `1`. |
| `packages/web/src/stores/operator.ts` (`gatedSliceReset`) | A factory minting fresh containers per session end, not one shared instance (IN-18) | VERIFIED | Read directly; no module-scope `GATED_SLICE_RESET` constant remains, and both `signOut`/`expireSession` call the factory. |
| `packages/web/src/lib/operator.ts` (`CreateDraftResult`), `packages/web/src/stores/operator.ts` (`submitDraft`) | Cohort creation is the enumerated sixteenth gated call site, taking the one honest re-login on a 401 (WR-14) | VERIFIED | Read directly; `CreateDraftResult` carries `unauthorized`, `createDraft` checks 401 before 201, `submitDraft` captures a round and guards. |
| `packages/web/src/lib/operator.ts` (`sessionProbe`, `SessionState`) | A transient fault is distinguishable from a genuine 401 in the narrated COPY (WR-15) | VERIFIED (narration only) | Read directly; four-case mapping (200/404/401/other), `probeUnreachable` writes the unreachable copy, never the session-expired sentence. |
| `packages/web/src/stores/operator.ts` (`probe`'s session-END branch, `probeUnreachable`) | The SESSION itself is not ended by an answer with no evidence about the session that is live now, matching the narration fix above | **PARTIAL (WR-16/WR-17, new this pass)** | Read directly: the session-END branch and `probeUnreachable` both call `get().expireSession()` on the sole condition `liveSessionRound !== undefined`, with no comparison against `askedAtRound` anywhere in either. A stale probe answer or ANY transient fault on a live session still ends that session, even though the narrated copy is now honest about why. |
| `packages/web/src/stores/operator.ts` (`probe`'s session-START branch) | A probe cannot re-establish a session that ended while it was in flight (IN-15) | VERIFIED | Read directly; the START branch alone captures and compares `askedAtRound`. |
| `packages/web/src/lib/operator.ts` (`downloadExport`), `packages/web/src/stores/operator.ts` (`exportCohort`) | A failure anywhere in the export path is caught and surfaced, matching the other three throwable gated verbs | **PARTIAL (WR-18, new this pass)** | Read directly: `res.blob()` sits outside `downloadExport`'s own `try`, and `exportCohort` has no `try/catch` around the client call, unlike `submitDraft`, `saveDraftEdit` and `saveSettings`. A mid-transfer body failure is an unhandled rejection: no download, no error, no re-login. |
| `packages/service/src/runtime-settings.ts` (`operatorSurfaceMounted`, `textKnob` consequence variants), `packages/service/src/index.ts` | A refused-seed boot only promises an in-session repair where the settings surface actually mounts (IN-17) | VERIFIED | Read directly; the seed key is derived from the same `operatorPassword` binding that constructs `operatorAuth`, and the round's own real-boot test asserts the sentence and the absent settings route together. |
| `docs/DEPLOY.md` | One consistent account of the refused-terms fact, cost then in-session repair then environment edit, matching the console caption (IN-16) | VERIFIED (with a residual, IN-19) | Read directly; the line-358 overclaim ("until you shorten it, the join flow has no terms step at all") is gone. Two other unqualified statements of the settings-surface repair remain at lines 345 and 491 (new finding IN-19), and the round's own documentation row measures the qualified count rather than the absence of unqualified ones, so it cannot detect a further unqualified addition. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `packages/web/src/stores/operator.ts` (all twelve gated action verbs) | `stillAsking` | The session-identity rule applied on every branch that writes, not only `unauthorized` | WIRED (WR-13 closed) | Confirmed by direct code read of five representative verbs plus a source-level `grep` for the retired comparison. |
| `packages/web/src/stores/operator.ts` (`signOut`, `expireSession`) | `gatedSliceReset()` | Fresh containers minted per session end | WIRED (IN-18 closed) | Confirmed by direct code read. |
| `packages/web/src/stores/operator.ts` (`submitDraft`) | `expireIfStillAsking` / `stillAsking` | A 401 on cohort creation taking the one honest re-login path | WIRED (WR-14 closed) | Confirmed by direct code read; `submitDraft` is now shaped like `saveDraftEdit`. |
| `packages/web/src/lib/operator.ts` (`sessionProbe`) | `packages/web/src/stores/operator.ts` (`probeUnreachable`) | A transient fault narrated honestly, distinct from a genuine expiry | WIRED for narration (WR-15 closed) | Confirmed; the copy is correct. |
| `packages/web/src/stores/operator.ts` (`probe`'s session-END branch, `probeUnreachable`) | `askedAtRound` (the captured round IN-15 introduced) | The session-ENDING half of the same rule the session-STARTING half already follows | **NOT WIRED (WR-16/WR-17, new this pass)** | Confirmed by direct code read: `askedAtRound` is captured once, used once (the START branch), and never read by the END branch or by `probeUnreachable`. |
| `packages/web/src/lib/operator.ts` (`downloadExport`'s `res.blob()`), `packages/web/src/stores/operator.ts` (`exportCohort`) | A caught failure surfaced as `EXPORT_FAILED` | **NOT WIRED (WR-18, new this pass)** | Confirmed by direct code read: no `try` wraps `res.blob()`, no `try/catch` wraps the store's call to `apiDownloadExport`. |
| `docs/DEPLOY.md` (all statements of the refused-seed / settings-surface fact) | The `OPERATOR_PASSWORD`-present qualifier | Every offer of the in-session repair states its own precondition | **PARTIALLY WIRED (IN-19, new this pass)** | Confirmed by direct grep: 3 of 5 statements carry the qualifier; lines 345 and 491 do not. |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| SVC-04 | 05-01..05-44 (core wave + all eight gap rounds) | Lifecycle control, pause/cancel/reconfigure advertising without restart | SATISFIED at code level (all 4 of its numbered Success Criteria VERIFIED); traceability table correctly reads `Gaps Found` pending `05-UAT.md` and the new WR-16/WR-17/WR-18 findings | SC1-SC4 all VERIFIED. `.planning/REQUIREMENTS.md` traceability table (lines 74-81) correctly reads `SVC-04 \| Phase 5 \| Gaps Found`. The requirements CHECKBOX list (line 18) still marks SVC-04 `[x]`, a pre-existing inconsistency with the traceability table's `Gaps Found`, predating round 7 and unedited by round 8 (`git diff --stat` on `.planning/REQUIREMENTS.md` across the round is empty). |
| SVC-05 | 05-13, 05-35, 05-37, 05-41, 05-44 | Participation terms, DID-signed, server-verified, hash-bound, honest boundary disclosure | SATISFIED at code level; traceability table correctly reads `Gaps Found` | `tos.spec.ts` 28/28 green, unaffected by round 8's mechanism. IN-17/IN-16 closed this round on the boot-warning/deploy-doc disclosure surface; IN-19 (new) leaves two remaining unqualified statements of the same fact in `docs/DEPLOY.md`. |
| PART-05 | 05-11, 05-29 | Participant-supplied esplora endpoint, mismatch guard, four failure messages, no silent fallback | SATISFIED (code level) | `tx-client.spec.ts` green, unaffected by round 8. Human Environment item remains. Traceability table already correctly reads `Gaps Found`. |
| PART-06 | 05-14 | PSBT wallet round trip, template-validated, guarded broadcast | SATISFIED (code level) | `psbt.spec.ts` suites green, unaffected by round 8. Human Environment item remains. Traceability table already correctly reads `Gaps Found`. |

No orphaned requirements: `SVC-04`, `SVC-05`, `PART-05`, `PART-06` are exactly the four requirements mapped to Phase 5 in `.planning/REQUIREMENTS.md`, and all four appear in PLAN frontmatter across the 44 plans, including `05-42`/`05-43` (`[SVC-04]`) and `05-44` (`[SVC-04, SVC-05]`).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `packages/web/src/stores/operator.ts` | probe's session-END branch, and `probeUnreachable` | The session-ENDING half of the rule IN-15 taught the session-STARTING half: neither compares against `askedAtRound` before ending a session, so a stale or transient-fault probe answer can end a session it has no evidence about (WR-16, WR-17) | Warning (does not gate a numbered roadmap Success Criterion) | Confirmed real by direct code read. WR-17 is the plainer of the two: no race is needed, only one 5xx or one dropped connection on a live console. |
| `packages/web/src/lib/operator.ts`, `packages/web/src/stores/operator.ts` | `downloadExport`'s `res.blob()`, `exportCohort` | The one gated verb whose client function can still throw uncaught, reopening the WR-06 silent no-op in the one case that finding did not enumerate (WR-18) | Warning | Confirmed real by direct code read. Bounded to the monitoring-export click; not a Phase-5 SC surface. |
| `docs/DEPLOY.md` | 345, 491 | Two statements of the refused-seed / in-session-repair fact remain unqualified while three others carry the `OPERATOR_PASSWORD`-present qualifier (IN-19) | Info | Confirmed real by direct grep. The round's own documentation row measures the qualified count, not the absence of unqualified statements, so it cannot detect this. |
| `packages/web/src/stores/operator.ts` | ~1011, 1466, 1510, 1794 | Four transient-confirmation `setTimeout` clears are guarded only by message identity, not by the session round, so they are writing branches the WR-13 sweep's own stated rule ("one comparison per branch that writes") does not reach (IN-20) | Info | Confirmed real by direct code read. Cosmetic only: a confirmation line can disappear a couple of seconds early for a different session, never a false claim. |
| `packages/web/src/stores/operator.ts` | `askingRound`/`stillAsking` definitions | A gated verb's early return on a false `stillAsking` leaves its own in-flight marker set if the false comparison came from `askingRound` returning `undefined` (not logged in) rather than from a moved round (session ended) (IN-21) | Info | Confirmed real by direct code read. Currently unreachable: `OperatorConsole` replaces the whole console with a checking-session card during the one window this could fire, so no gated control is on screen to click. |
| `.planning/REQUIREMENTS.md` | 18-19 vs 74-75 | The v1-requirements checkbox list marks `SVC-04` and `SVC-05` `[x]` while the traceability table below correctly reads `Gaps Found` for both | Info (pre-existing, not introduced or worsened by round 8) | Confirmed by `git diff --stat 1c6ff5e 99b095c -- .planning/REQUIREMENTS.md` being empty: round 8 did not touch this file at all. Not gating; the traceability table is the field this workflow checks and it is correct. |

No `TBD`, `FIXME`, or `XXX` markers found in any round-8-touched file (independently grepped: `packages/web/src/stores/operator.ts`, `packages/web/src/lib/operator.ts`, `packages/web/tests/operator.spec.ts`, `packages/service/src/runtime-settings.ts`, `packages/service/src/index.ts`, `packages/service/tests/runtime-settings.spec.ts`, `docs/DEPLOY.md`). No em-dash found in the same files (independently grepped).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full test gate is green after round 8, including typecheck (`tsc -b` chained before `vitest run`) | `pnpm test` | 68 files, 1392 tests, all passing | PASS |
| Lint is clean | `pnpm lint` | No output beyond the invocation line | PASS |
| Web build is clean (typecheck + Vite build) | `pnpm --filter @btcr2-aggregation/web build` | `tsc --noEmit` clean, `vite build` succeeded, 714 modules transformed | PASS |
| All thirteen hermetic e2e legs pass | `pnpm e2e:gate` | All thirteen legs (`e2e:operator`, `e2e:monitor`, `e2e:cancel`, `e2e:pause`, `e2e:testpeers`, `e2e:fallback`, `e2e:fallback:operator`, `e2e:browse`, `e2e:kofn`, `e2e:live:mock`, `e2e:resolve`, `e2e:config`, `e2e:persist`) PASSED | PASS |
| WR-13/WR-14/WR-15(narration)/IN-15/IN-17/IN-16/IN-18 fixes hold | Read the relevant source directly (12 gated verbs, `submitDraft`, `sessionProbe`, `probe`'s START branch, `operatorSurfaceMounted`, `docs/DEPLOY.md`, `gatedSliceReset`) | All seven confirmed landed as claimed | PASS |
| WR-16 (probe's session-END branch, and `probeUnreachable`, ignore `askedAtRound`) | Read `probe`'s session-END branch and `probeUnreachable` directly | Confirmed real: `get().liveSessionRound !== undefined` is the sole condition; `askedAtRound` never appears in either | CONFIRMED (new warning finding) |
| WR-17 (any transient fault on a live session ends it, not only stale answers) | Read `probeUnreachable` directly | Confirmed real: `expireSession()` called whenever `liveSessionRound !== undefined`, with no distinction between the currently-live session and a dead one | CONFIRMED (new warning finding) |
| WR-18 (`exportCohort`'s uncaught throw path) | Read `downloadExport` and `exportCohort` directly | Confirmed real: `res.blob()` outside the client function's `try`; no `try/catch` in the store action | CONFIRMED (new warning finding) |
| IN-19 (two unqualified `docs/DEPLOY.md` statements) | `grep -n "settings surface" docs/DEPLOY.md` | 5 hits, 2 unqualified (lines 345, 491) | CONFIRMED (new info finding) |
| IN-20 (four unguarded timer clears) | `grep -n "setTimeout" packages/web/src/stores/operator.ts` | 4 hits, none comparing the session round | CONFIRMED (new info finding) |
| IN-21 (in-flight marker left set on a false-but-not-expired `stillAsking`) | Read `askingRound`/`stillAsking` directly | Confirmed real, currently unreachable per the console's own presentation guard | CONFIRMED (new info finding) |
| REQUIREMENTS.md traceability unedited by round 8, as its own prohibition required | `git diff --stat 1c6ff5e 99b095c -- .planning/REQUIREMENTS.md` | Empty diff | PASS |
| `packages/service` untouched by 05-42/05-43 | `git diff --stat bcd78f6~1 5f390bb -- packages/service` | Empty diff | PASS |
| `SettingsView.tsx` (the console caption, the fixed point) unedited across the whole round | `git diff --stat 1c6ff5e 99b095c -- packages/web/src/components/operator/SettingsView.tsx` | Empty diff | PASS |

### Probe Execution

Not applicable. This phase has no `scripts/*/tests/probe-*.sh` convention; verification relies on the vitest suite, the `e2e:gate` harness (independently reproduced above), and direct code reads.

## Human Verification Required

16 items carried forward, unchanged in count from the prior pass (see frontmatter `human_verification` and `05-UAT.md`, `pending: 16`). Round 8 touched no UAT item (confirmed directly against `05-UAT.md`'s frontmatter, and each round-8 plan's own prohibitions explicitly forbid claiming to close any pending human item). None of round 8's changes altered any existing item's preconditions in a way that would change its expected outcome, except that Environment 1 (the `DEPLOY.md` walk-through) may now notice the two remaining unqualified "settings surface" mentions at lines 345 and 491 (new finding IN-19, not yet closed).

WR-16, WR-17, WR-18, and the three new info findings are NOT human-verification items: each is a confirmed, reproducible code observation established by direct code read, not something requiring a person, a browser, or a clean machine to establish. They belong in a future gap-closure round's scope, not in `05-UAT.md`.

## Gaps Summary

Round 8 (05-42, 05-43, 05-44) closed all seven of its owner-approved targets: WR-13 (all twelve gated action verbs now honor the session that asked on every branch that writes, not only the `unauthorized` branch, and the last status-as-identity comparison in the file is gone), WR-14 (cohort creation is now the enumerated sixteenth gated call site, taking the one honest re-login path on a 401 instead of rendering the service's raw denial string as form validation), WR-15's narration half (a transient fault on the session probe now narrates the honest unreachable line rather than a false session-expired claim), IN-15 (a probe answer landing after a session boundary can no longer re-establish a phantom session), IN-16 (`docs/DEPLOY.md`'s participation-terms prose sentence no longer contradicts its own section by naming a restart as the only repair), IN-17 (a fail-closed boot with no operator password no longer promises an in-session repair in a settings surface it never mounts), and IN-18 (every session end now mints its own fresh containers rather than sharing four instances across every reset). All seven were independently reconfirmed in this pass by direct code read and by an independent rerun of `pnpm test` (68 files, 1392 tests, matching the round's own final figure), `pnpm lint`, the web build, and all thirteen hermetic `e2e:gate` legs, not by trusting the SUMMARYs' own reported numbers.

All 7 numbered roadmap Success Criteria continue to verify at the code level; none is FAILED. This pass's overall status remains `human_needed` rather than `gaps_found`: the 16 pending items in `05-UAT.md` (unchanged in count) are what stands between this phase and a `passed` verification, not any remaining code defect that falsifies a numbered truth.

A fresh deep code review conducted after round 8 landed (`05-REVIEW.md`) found, and this pass independently confirmed by direct code read rather than by citation, ZERO new critical findings for the second consecutive round, three new warnings, and three new info findings, all in the same session-identity subsystem rounds 5, 6, 7 and 8 have each touched. WR-16 is the session-ENDING mirror of the fix IN-15 gave only the session-STARTING branch: `probe`'s session-END branch and the shared `probeUnreachable` helper both still decide whether to end a session purely from whether ANY session is live, never from whether the session that is live now is the one this probe's question was asked in, so a stale probe answer can end a brand-new session and narrate the exact expiry copy this whole finding series exists to prevent. WR-17 is sharper still and needs no race at all: `probeUnreachable` ends the CURRENTLY LIVE session on any transient fault (a 5xx, a dropped connection), which is a genuine inconsistency with the freeze-not-claim posture (D-25) the store follows everywhere else, and the round's own summary description ("a 502 probe now freezes the console") does not match what the code does, which is log the operator out. WR-18 is a narrower, well-bounded defect: the one gated verb whose client function can still throw uncaught (`exportCohort`, via `downloadExport`'s `res.blob()` sitting outside its own `try`), reopening the exact silent-no-op class WR-06 closed in round 4, in the one case that finding did not enumerate. Weighed independently against the literal text of all 7 numbered roadmap Success Criteria, none of these three findings falsifies any of them: the underlying lifecycle-control, pause/cancel, reconfiguration and terms-acceptance mechanisms all continue to function correctly on explicit, current operator input, the service itself is never killed, and the dashboard/resolve routes stay up throughout. What these findings corrupt is the console's own session-identity bookkeeping (WR-16, WR-17) and one monitoring-export click's failure handling (WR-18), on surfaces none of the 7 SCs states as their observable truth.

This is the FOURTH consecutive round to surface new, real findings in the exact same session-identity subsystem (round 5's review found the originals; round 6's found CR-03/WR-11/WR-12/IN-09 through IN-14; round 7 closed all nine of those and its own re-review immediately found WR-13/WR-14/WR-15/IN-15 through IN-18; round 8 closed all seven of those and its own re-review immediately found WR-16/WR-17/WR-18/IN-19 through IN-21). The critical-tier severity trend is flat at zero for two consecutive rounds now, which is genuine progress, but WR-17 in particular is not narrower in triggering difficulty than what round 8 closed: it fires on a single transient fault with no interleaving required, which is more easily reached in production than the race windows WR-13 and IN-15 needed. The round-8 SUMMARY's own closing note anticipated this exact pattern and recommended the owner weigh a fifth round against a design pass on the store's session identity rather than another sweep; this pass's independent findings support that recommendation rather than contradicting it. Either an owner-directed ninth gap round closes WR-16/WR-17/WR-18 (and, ideally, addresses the pattern rather than only the instances) before the phase moves to `/gsd-secure-phase 5` or milestone completion, or the owner accepts the current state (zero critical for two rounds running, no falsified Success Criterion, WR-17 flagged prominently as the one finding that needs no race to trigger) and proceeds with these three recorded as known, carried findings alongside the already-carried WR-03/WR-04/WR-05/IN-01/IN-02 and the deferred advert-slot-on-fill item. Both paths are consistent with this phase's own numbered goal being achieved at the code level; the choice is a risk-tolerance decision for the owner, not a gate this verification can resolve unilaterally.

---

_Verified: 2026-08-04_
_Verifier: Claude (gsd-verifier)_
