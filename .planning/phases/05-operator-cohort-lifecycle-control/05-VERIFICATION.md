---
phase: 05-operator-cohort-lifecycle-control
verified: 2026-08-04T01:15:00Z
status: human_needed
score: 7/7 roadmap Success Criteria verified at code level (0 present, behavior-unverified); 16 human-verification items still pending (unchanged in count); all nine round-7 targets (CR-03, WR-11, IN-11, WR-12, IN-09, IN-10, IN-12, IN-13, IN-14) independently confirmed closed by direct code read and by an independent rerun of the full suite; the round-7 re-review found ZERO new critical findings (first time in this series), three new warnings (WR-13, WR-14, WR-15) and four new info findings (IN-15..18), none of which falsifies a numbered roadmap Success Criterion, all independently confirmed real by direct code read in this pass
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: "7/7 roadmap Success Criteria verified at code level (per 05-VERIFICATION.md pass 7, dated 2026-08-03T22:31:57Z, before the seventh gap-closure round). Pass 7 recorded ONE new critical finding (CR-03, unaddressed at that time) plus WR-11, WR-12 and six info findings, and recommended a seventh gap round before /gsd-secure-phase or milestone completion."
  gaps_closed:
    - "CR-03 (a probe that discovered a session had ended neither retired the round nor cleared the gated slice): probe's three non-live outcomes (no live session recorded, the fail-closed 404, and the transport-fault catch) now route through the shared expireSession() operation whenever liveSessionRound is defined, retiring the round and clearing the whole 35-field gated slice before the disabled or unreachable status lands on top. Confirmed by direct code read at operator.ts:960-1030 in this pass (not by citation): the non-live-session-recorded branch calls expireSession() at line 996 and 1020, and the shipped comment states both the never-existed case and the just-ended case, citing review CR-03 by name."
    - "WR-11 (the transition-into-a-live-session condition was decided from a pre-await status snapshot a second overlapping probe could erase): a new stored fact, liveSessionRound (operator.ts:426), is set beside every round bump that starts a session (signIn's 200 branch, probe's session-starts branch) and cleared by GATED_SLICE_RESET, replacing the wasLive pre-await capture. Confirmed by direct code read: the wasLive capture and its trap comment are gone from the file, and the condition at line 981 reads the stored fact at landing time."
    - "IN-11 (signOut and expireSession cleared two different field lists, so a failed-advertise message or a create-form error from one session survived an expiry and rendered on the next session's create form): both paths now spread one module-scope GATED_SLICE_RESET constant (operator.ts:786), holding every field both paths clear including the five expireSession previously missed (createStatus, formError, advertiseStatus, advertisingId, advertiseMessage). Confirmed by direct code read; only auth, sessionRound and error remain path-specific."
    - "WR-12 (advertise and readvertise collapsed every non-ok answer, including an expired-session 401 and a paused-advertising 409, into one generic retry sentence): both verbs now return a discriminated AdvertiseActionResult / ReadvertiseActionResult (lib/operator.ts) with unauthorized, refused (carrying the service's own 409 reason), declined and unreachable members, and the store branches on each exactly as finalizeCohort does. Confirmed by direct code read of the advertise action (operator.ts:1260-1300): the refused branch renders the service's reason through actionFailedWith, the declined branch keeps the verb-specific sentence, and unauthorized takes the shared expiry path."
    - "IN-09 (the session-identity rule was stated as general law in the sessionRound docstring but followed by 4 of 15 gated call sites): three module-scope helpers (askingRound, stillAsking, expireIfStillAsking, operator.ts:860-878) now hold the rule in one place and are used at all four gated reads plus eleven gated action verbs. Confirmed by direct code read: 42 usages of the three helper names across the file, and the helper docstring names probe's session-ended branches as the one documented exception."
    - "IN-10 (refreshCohorts alone additionally compared the auth status, an asymmetry that acquired teeth once the probe stopped bumping on a confirming landing): the status clause was dropped from refreshCohorts's guard, leaving the capture-and-round comparison the other three gated reads already used. Confirmed by direct code read; the guard's comment now agrees with the 401 branch's comment in the same method instead of arguing the opposite case."
    - "IN-12 (the settings route's own comment in hono-adapter.ts said the budget was computed from MAX_TERMS_CHARS alone, stale since 05-38 made it a function of both caps): the comment at hono-adapter.ts:897-919 now names settingsBodyLimitBytes and both MAX_TERMS_CHARS and MAX_SERVICE_NAME_CHARS explicitly, with the bodyLimit call itself unedited. Confirmed by direct code read."
    - "IN-13 (round 6 corrected only the console caption's cost/remedy split, leaving the two boot-warning consequence clauses and the two docs/DEPLOY.md environment rows naming a restart as the only remedy): both textKnob consequence clauses in runtime-settings.ts now name the in-session repair through the operator settings surface first and the environment edit second, matching the console caption's own order, and docs/DEPLOY.md's SERVICE_NAME and TERMS_TEXT rows carry the same split. Confirmed by direct code read at runtime-settings.ts:820-843 and docs/DEPLOY.md:511/517."
    - "IN-14 (the derived settings body budget charges the STORED/trimmed length while applySettings trims before it bounds, so a padded value could theoretically exceed the transmitted-body budget while remaining under the holder's cap): settingsBodyLimitBytes's docstring (runtime-settings.ts:406-435) now states which length the budget charges, names the alternative not taken (bounding the untrimmed field) and why it is disproportionate, and is measured by a dedicated row rather than merely argued. Confirmed by direct code read; this is documented and measured as a decision, not closed by a behavior change, exactly as the SUMMARY records."
  gaps_remaining: []
  regressions:
    - "WR-15 (new this pass, confirmed real, non-gating): the CR-03 fix gave sessionProbe's coarsest branch teeth it did not have before round 7. sessionProbe (lib/operator.ts:50-63) maps every non-200/non-404 HTTP status, including a 5xx, to the same 'logged-out' outcome as a genuine 401, and probe's non-live branch (as landed by CR-03) now calls expireSession() on that outcome whenever a live session is recorded. Before round 7 this branch only set a status string, so the mislabeling of a 5xx as 'logged-out' was cosmetic; after round 7 it clears the whole gated slice and narrates a session-expired claim the service never made, on a transient fault the store's own sibling catch branch explicitly reasons is NOT evidence of expiry (operator.ts's fault-branch comment: 'A transport fault is NOT evidence that a session ended'). This is a genuine increase in blast radius introduced by the round's own fix, confirmed by direct code read of sessionProbe's status mapping and of the non-live branch's unconditional treatment of any non-'logged-in' state answer as equivalent to an ended session."
new_findings_this_pass:
  - "WR-13 (CONFIRMED REAL by direct code read, non-gating): the IN-09/05-40 session-identity sweep guards only the unauthorized (401) branch of the eleven gated action verbs; every other branch (ok, refused, declined, unreachable) still writes unconditionally into whichever session is live when the answer lands, rather than checking stillAsking as the four gated reads do on every branch. Confirmed at operator.ts:1290 directly: the advertise success branch reads `if (get().auth === 'logged-in')` before opening the drill-down, the exact status-as-identity comparison the sessionRound guard exists to replace, and the same shape recurs at the equivalent point in the other ten action verbs. Consequence: a dead session A's advertise or cancel answer, landing after session B has signed in, can navigate B into A's cohort or paint an ACTION_FAILED sentence onto a cohort B never touched. Weighed against the 7 numbered roadmap Success Criteria: the underlying lifecycle actions (advertise, cancel, finalize, etc.) still execute correctly server-side for the session that took them; what leaks is the DISPLAY in a different, later session, which none of the 7 SCs states as their observable truth. Same reasoning pass 7 applied to CR-03 before it was fixed."
  - "WR-14 (CONFIRMED REAL by direct code read, non-gating): submitDraft (operator.ts:1191-1204) is a sixteenth gated call site the IN-09 enumeration does not contain. CreateDraftResult (lib/operator.ts:168) has no unauthorized member (only `ok: true` or `ok: false; error: string`), so a 401 on cohort creation is treated like a validation failure and the service's raw denial string ('operator authentication required') renders in the create form's field-level error slot, ending no session and retiring no round. Confirmed directly: grep of CreateDraftResult's definition shows no unauthorized branch, matching the review's finding. Blast radius bounded by the list poll (refreshCohorts clears formError as part of GATED_SLICE_RESET within one 4-second tick). Not a Phase-5 roadmap Success Criterion surface (cohort creation is SVC-01, Phase 1)."
  - "WR-15: see regressions above (new behavior, not merely a new observation)."
  - "IN-15 through IN-18 (CONFIRMED REAL by direct code read, all info/cosmetic, none gating): IN-15 (probe's session-START branch takes no capture of its own, so a probe answer landing after an expiry that a different action already discovered can re-establish a self-healing phantom session for one tick); IN-16 (docs/DEPLOY.md:358's prose section, fourteen lines above the two corrected environment rows, still states shortening as the only repair, missed by the IN-13 repo-wide correction); IN-17 (both new boot-consequence clauses promise an operator settings surface that a fail-closed boot with no OPERATOR_PASSWORD does not mount, since the settings routes are registered only inside `if (operatorAuth)`); IN-18 (GATED_SLICE_RESET's four nested array/object values are shared instances copied by reference on every spread, not fresh per reset, a latent hazard if a future surface ever mutates one in place)."
gaps: []
deferred:
  - truth: "The transport advert slot is also cleared when a cohort FILLS (keygen-complete), not only on the three settle paths, so a sibling open cohort stays listed but stops being joinable to a freshly connecting participant."
    addressed_in: "Phase 6 (suggested home; not yet claimed by a Phase 6 success criterion)"
    evidence: "Logged in .planning/phases/05-operator-cohort-lifecycle-control/deferred-items.md and documented as upstream limit 1 in docs/UPSTREAM-LIMITS.md. No Phase 5 must-have covers the fill path. Carried forward unchanged across every verification pass, including this one."
behavior_unverified_items: []
human_verification:
  - test: "Judgment 1 (05-UAT test 3): confirm by eye that no Close button exists anywhere on the console, and accept or refuse the deliberate re-reading of roadmap SC 1 (Closed is an automatic nth-seat stage, not an operator-pressed button, per locked CONTEXT D-01)."
    expected: "No Close control exists anywhere on the console; the Closed stage appears automatically the instant the nth seat fills, narrated with its caption, not as an act the operator performs."
    why_human: "A deliberate divergence from the literal wording of the roadmap goal that only the owner can accept or refuse. The stage's existence, trigger, and caption are automated and passing (packages/web/tests/operator-stage.spec.ts); the absence of a Close button anywhere on the rendered console is not asserted by any test."
  - test: "Judgment 2 (05-UAT test 12): read the Ended-group labels (a completed hermetic co-sign now reads 'Signed', not an anchor claim) and decide whether the change reads as newly honest rather than as a regression."
    expected: "Owner accepts the relabeling as an honest disclosure of what MuSig2 completion does and does not prove about an on-chain anchor."
    why_human: "Pure copy/framing judgment; every label and the anchor-wording guard are automated and passing (packages/web/tests/operator-rows.spec.ts)."
  - test: "Eyes (05-UAT test 13, plus narrowed 5/7/8): at a real browser viewport, walk the operator console list, drill-down (with cancel/finalize/seat-reclaim/funding disclosures all visible at once), health strip, a saved long SERVICE_NAME on both the console and the public directory header, and a long TERMS_TEXT body at a narrow viewport height, including the refused-seed source caption's now-split cost/remedy text."
    expected: "Nothing overflows, chips stay on screen, grouping is legible with no horizontal scroll; the long service name renders as plain escaped text on both surfaces without pushing chips off screen; the long terms body scrolls inside its capped container, wraps unbroken tokens, never escapes the card, and join controls stay reachable below it; the refused-seed caption's cost and remedy read as a warning, not as another grey caption."
    why_human: "Layout/legibility at a real screen size. The render harness is a static server render (no layout engine), so it can prove the capping/wrapping/caption-branch classes are present but never that anything actually fits or reads correctly."
  - test: "Copy (05-UAT test 9, plus two follow-on questions): read the four interpolated test-peer confirm strings, the verbatim 'unknown draft' refusal shown to an operator editing a stale draft, and the duplicated 'Operator console' heading shown on both sides of sign-in."
    expected: "All read correctly, in particular that the test-peer confirm states BEFORE the act that peers co-sign for real with throwaway keys; decide whether 'unknown draft' is the right sentence and whether the signed-in console deserves its own heading."
    why_human: "Copy/framing decisions; the four interpolated strings are pinned by nothing today (a named, not-yet-done follow-up), and the other two are deliberate, already-shipped choices awaiting the owner's sign-off."
  - test: "Hands (05-UAT tests 2, 4, 6, 10, 11): exercise 'Cancel edit' on a draft in place; open the finalize and ordinary-cancel confirms and read their in-place wording plus the in-flight disable; load the public directory paused/unpaused and narrow the window; cancel a cohort a participant is seated in and read their terminal card; dismiss an ended cohort row and refresh the page."
    expected: "Cancel edit closes the form with the draft unchanged; finalize/cancel confirms state the real consequence before it happens and disable correctly while in flight; the paused notice and empty states render distinctly and the controls row wraps at narrow widths; the cancel narration and next-step line appear together on the participant's terminal card; a dismissed row stays gone after a refresh."
    why_human: "The render harness is a static server render: no events fire, no effects run, so no state a click produces is reachable from it. Needs a DOM environment or a browser leg that does not exist yet."
  - test: "Environment 1 (05-UAT test 14): follow docs/DEPLOY.md from a clean machine using only the document (sign in, create, advertise, join, rehearse)."
    expected: "Every new control is reachable from the document alone; the retired filler knob appears nowhere; MIN_PARTICIPANTS/AUTO_FALLBACK/SSE_DEBUG and the /v1/config sample match what the service actually reads and serves. This walk should also confirm the corrected participation-terms paragraph and the SERVICE_NAME/TERMS_TEXT rows' now-split cost/remedy wording match observed behavior."
    why_human: "Needs a clean machine and a person following written instructions verbatim; not something a unit or e2e test can stand in for."
  - test: "Environment 2 (05-UAT test 15, narrowed, updated expectation): try a real host that blocks browser requests (CORS) and a real wrong-chain third-party esplora host against this service's actual default network."
    expected: "browser-rejected and mismatch (naming both chains) appear correctly, with the explicit switch-back control offered on failure and no silent fallback in either direction. This confirms SC6 (PART-05) at the code level, closed by 05-29, holds against a real host, which is the one thing the hermetic suite cannot prove."
    why_human: "Real CORS behavior and a real wrong-chain host cannot be produced hermetically."
  - test: "Environment 3 (05-UAT test 16): export the unsigned PSBT, sign it in an actual desktop or hardware wallet, return it, and broadcast; also confirm a large pasted PSBT keeps its field scrolling internally."
    expected: "The wallet accepts the exported .psbt; the returned PSBT validates against the template; the broadcast produces the same txid the local path would; the large-PSBT field does not reflow the step."
    why_human: "No wallet interoperability was verified in this environment; the 05-14 prohibition correctly forbids claiming any specific wallet works without this walk."
  - test: "Environment 4 (05-UAT test 17, narrowed): boot with LIVE=1 BROADCAST=1, engage Disable broadcast, and confirm the real boot behavior plus the disclosed cost of the stand-down."
    expected: "No route turns broadcast back on; the health strip still reports the live boot mode with a separate warn chip; a cohort advertised after the switch shows no Funding card while one advertised before keeps its funding surface; the owner accepts that the esplora-reachability badge stops refreshing once only post-switch cohorts remain."
    why_human: "Needs a real LIVE+BROADCAST boot, which cannot be produced hermetically, plus an owner cost/benefit judgment that only the owner can make."
  - test: "Cancel settle-race backstop (05-01, verification: backstop): attempt to cancel a cohort at the instant its signing round completes."
    expected: "The click is answered 404 with nothing changed, and the console never shows two ended records for one cohort id."
    why_human: "A timing race that cannot be reliably driven from a test harness; classified 'backstop' by design. What is checkable about it (the opaque 404 on an already-settled cohort, and idempotent folding so no duplicate ended record is ever filed) is automated and passing."
---

# Phase 5: Operator Cohort Lifecycle Control Verification Report

**Phase Goal:** The operator runs aggregation and manages a cohort's lifecycle (open, then close, then finalize) and pauses, cancels, or reconfigures advertising from the console, without restarting the process, removing the last hardwired, uncontrollable behavior.
**Verified:** 2026-08-04
**Status:** human_needed
**Re-verification:** Yes. This is the eighth verification pass, following the seventh gap-closure round (05-39, 05-40, 05-41), which closed all nine owner-approved findings from the round-6 re-review (CR-03, WR-11, IN-11, WR-12, IN-09, IN-10, IN-12, IN-13, IN-14) rather than a failed roadmap Success Criterion (pass 7 already scored 7/7 at code level with `gaps: []`). This report independently confirms all nine round-7 targets are genuinely closed, and independently confirms a fresh deep code review (`05-REVIEW.md`, run after round 7 landed) that found ZERO new critical findings, three new warnings (WR-13, WR-14, WR-15) and four new info findings (IN-15..18) in the same session-identity surface round 7 rewrote.

## Method

I did not trust SUMMARY.md narrative, `05-REVIEW.md`'s own severity labels, or the prior VERIFICATION.md's frontmatter as complete. For every claim that mattered to a Success Criterion or to a claimed gap closure, I re-derived it from the live tree myself.

- Ran `pnpm test` directly (which chains `tsc -b` then `vitest run` per `package.json`): **68 files, 1348 tests, all passing**, matching the round's own final SUMMARY (05-41) exactly, reproduced independently rather than copied.
- Ran `pnpm lint` directly: clean, zero output beyond the invocation line.
- Read `packages/web/src/stores/operator.ts` lines 960 through 1030 directly (the whole `probe` method as amended by round 7) and confirmed CR-03's fix at the mechanism: the branch answering no live session, the fail-closed 404, and the transport-fault `catch` all route through `expireSession()` whenever `liveSessionRound` is defined, and the disabled/unreachable status lands only after the clear.
- Read the same file's `GATED_SLICE_RESET` and `liveSessionRound` declarations (lines 786-949 area) and confirmed IN-11's fix: one shared reset constant spread by both `signOut` and `expireSession`, holding the five previously-missed fields.
- Ran `grep -n "GATED_SLICE_RESET\|liveSessionRound"` over the store: 12+ hits confirming the constant and field are wired through `probe`, `signIn`, `signOut` and `expireSession` consistently.
- Read `packages/web/src/lib/operator.ts`'s `advertise`/`readvertise` result types and `packages/web/src/stores/operator.ts`'s corresponding store actions (lines 1260-1300) directly and confirmed WR-12's discriminated-result fix, but ALSO independently discovered the residual status-as-identity comparison at line 1290 (`get().auth === 'logged-in'`) that is the mechanism of the new WR-13 finding, matching the review's own citation exactly.
- Confirmed `askingRound` / `stillAsking` / `expireIfStillAsking` exist at `operator.ts:860-878` (IN-09) and are used 42 times across the file, consistent with the review's claim of fifteen swept call sites plus the guard trio's own internal uses.
- Confirmed `submitDraft` (operator.ts:1191-1204) and `CreateDraftResult` (lib/operator.ts:168) directly by reading both: `CreateDraftResult` carries no `unauthorized` member, confirming WR-14 (a sixteenth gated call site the IN-09 sweep does not contain) is real, not merely cited from the review.
- Read `sessionProbe` (lib/operator.ts:50-63) directly and confirmed WR-15: any HTTP status other than 200 or 404 (including a 5xx) maps to the same `'logged-out'` return value as a genuine 401, and CR-03's fix now routes that same outcome through `expireSession()` whenever a live session is recorded, which increases the blast radius of a pre-existing coarse status mapping from cosmetic (a wrong status label) to load-bearing (a false expiry claim plus a full state clear on a transient fault). Recorded as a `regressions` entry rather than a plain new finding because the consequence, not the mapping itself, is new to round 7.
- Read `packages/service/src/runtime-settings.ts`'s two `textKnob` consequence clauses directly (around lines 820-843) and confirmed IN-13's fix: both now name the in-session repair before the environment edit, matching the console caption's order.
- Read `docs/DEPLOY.md` lines 511 and 517 directly and confirmed the `SERVICE_NAME` and `TERMS_TEXT` rows carry the same split, and separately grepped for the un-corrected prose sentence at line 358 (fourteen lines above the two corrected rows), confirming new finding IN-16.
- Read `packages/service/src/hono-adapter.ts` lines 895-919 directly and confirmed IN-12's fix: the settings route's comment now names `settingsBodyLimitBytes` and both `MAX_TERMS_CHARS`/`MAX_SERVICE_NAME_CHARS`, with the `bodyLimit` call itself unedited (a comment-only diff).
- Read `packages/service/src/runtime-settings.ts`'s `settingsBodyLimitBytes` docstring (lines 406-435) directly and confirmed IN-14's fix: the docstring states which length the budget charges (stored/trimmed vs transmitted), names the alternative not taken, and cites the measurement row that proves the claim, matching the SUMMARY's characterization of this as "documented and measured, not closed by a code change."
- Cross-referenced `.planning/REQUIREMENTS.md` directly: the traceability table (lines 74-81) still reads `Gaps Found` for all four Phase 5 requirements (SVC-04, SVC-05, PART-05, PART-06), unedited by round 7 as the round's own prohibition required. Separately noticed the v1-requirements checkbox list (lines 18-19) marks SVC-04 and SVC-05 `[x]`, which visually disagrees with the traceability table's `Gaps Found`; confirmed this predates round 7 (last touched at `ed27d79`, before round 7's first commit `31baed2`) and was not introduced or worsened by this round. Recorded as an info-level anti-pattern, not a gap, since the traceability table (the field this workflow's Step 6 actually checks) is correct and round 7 explicitly declined to touch `REQUIREMENTS.md`.
- Read `05-UAT.md` directly: `pending: 16`, byte-identical in count to the prior pass; round 7 touched no UAT item (confirmed by 05-41-SUMMARY.md's own explicit statement, independently verified against the file's frontmatter).
- Read `05-REVIEW.md` in full (the fresh round-7 re-review: 0 critical, 3 warnings, 4 info, `status: issues_found`) and independently weighed WR-13, WR-14, WR-15 and the four new info findings against the 7 roadmap Success Criteria rather than accepting the review's own framing. Reproduced each finding's core claim by direct code read of the unmodified source, not by re-running the review's own repro scripts (which are not part of the committed tree).
- Confirmed no `TBD`/`FIXME`/`XXX` markers and no em-dash in any round-7-touched file, independently grepped.

## Goal Achievement

### Observable Truths (roadmap Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Operator moves a cohort through open, close and finalize from the console and the directory reflects each state change | VERIFIED | Unaffected by round 7 (no round-7 file touches `LifecycleActions.tsx` or the cohort-keyed guard 05-28 added). Confirmed by the full `pnpm test` pass (`operator.spec.ts`, `lifecycle.spec.ts`, both green). New finding WR-13 does not affect this mechanism: the underlying lifecycle actions still execute correctly server-side for the session that took them; WR-13 corrupts only a DIFFERENT session's later display, which no numbered SC states as its observable truth. Human judgment item (Judgment 1) remains for the "no Close button" reading. |
| 2 | Operator pauses or cancels advertising so new cohorts stop being offered, without killing the running service | VERIFIED | No round-7 file touches the pause/advertise-gate mechanism beyond the discriminated result on `advertise`/`readvertise` (WR-12, confirmed closed). Confirmed by the full `pnpm test` pass. |
| 3 | Operator reconfigures cohort shape (capacity, threshold, beacon type for the next cohort) without editing env vars or restarting | VERIFIED | The save mechanism itself (`PUT /v1/operator/settings`, `settingsBodyLimitBytes`) is unchanged by round 7: IN-12/IN-13/IN-14 are comment, caption and documentation corrections plus one measurement row, not behavior changes, confirmed by reading the derivation function and the route's `bodyLimit` call (both non-comment-diff-empty per the SUMMARY, independently confirmed by direct read). |
| 4 | A canceled or closed cohort no longer appears as joinable in the participant directory | VERIFIED | Unaffected by round 7. Confirmed by the full `pnpm test` pass; no round-7 file touches the directory-visibility path. |
| 5 | Participation terms accepted at join and recorded as a DID-signed, server-verified, terms-hash-bound artifact, with the app-level enforcement boundary disclosed honestly (SVC-05) | VERIFIED (code level) | `packages/service/tests/tos.spec.ts` (28 tests, part of the full 1348-test pass, all green), unaffected by round 7's own changes to that surface (05-41 touched only the boot-warning consequence text and the budget's docstring, not the acceptance/verification mechanism). Human judgment items remain (Judgment 2, Eyes). |
| 6 | Participant can point browser chain reads at their own esplora endpoint, with a network-mismatch guard, four distinguishable failure messages, no silent fallback, real-funds guard rails unweakened (PART-05) | VERIFIED (code level) | No round-7 file touches `esplora.ts` or `tx-client.ts`. Confirmed by the full `pnpm test` pass. The live leg still needs a person; see Human Verification. |
| 7 | Participant can sign the registration transaction in their own wallet through a PSBT round trip validated against the exact template before anything is broadcast (PART-06) | VERIFIED (code level) | No round-7 file touches this surface. Confirmed via the full `pnpm test` pass. Real wallet interoperability: human item (Environment). |

**Score:** 7/7 truths verified (0 present, behavior-unverified)

**Independent note on the new session-identity findings (WR-13, WR-14, WR-15) and the seven numbered SCs:** all three are real (confirmed above by direct code read and, for WR-15, by tracing the pre-existing `sessionProbe` status mapping against round 7's own `probe` fix). Weighed independently against the literal text of each of the 7 numbered Success Criteria, none falsifies any of them: the underlying lifecycle-control, pause/cancel, reconfiguration and terms-acceptance MECHANISMS all continue to function correctly when explicitly invoked by the operator whose session is asking. What these three findings corrupt is what a DIFFERENT session's console displays after receiving an answer to a question a now-ended session asked (WR-13), what a create-form error slot shows on an expired-session 401 for cohort creation (WR-14, a Phase-1/SVC-01 surface rather than a Phase-5 SC), and how a transient 5xx probe is narrated (WR-15). This is the same reasoning pass 7 applied to CR-03 before it was fixed, now applied a round later to its unswept siblings. Notably, the round-7 re-review found ZERO critical findings for the first time in this series (round-5's review: 1 critical; round-6's review: 1 critical), and the three warnings found this time are narrower in blast radius than CR-03 was: WR-13's window is one click plus one round trip rather than an indefinitely-stale settings latch, WR-14's is bounded by one poll tick, and WR-15's is a transient fault self-correcting on the next successful probe. Given that this is the THIRD consecutive review pass to surface new findings in the exact same session-identity subsystem (round 6's review found CR-03/WR-11/WR-12/IN-09..14; round 7 closed all nine; round 7's own review immediately found WR-13/WR-14/WR-15/IN-15..18 in the code the round just finished), this pattern is called out prominently below and an eighth gap round is recommended for the owner's decision, though the phase's numbered goal remains achieved at the code level either way.

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | The transport advert slot is also cleared when a cohort FILLS (keygen-complete), not only on the three settle paths, so a sibling open cohort stays listed but stops being joinable to a freshly connecting participant | Phase 6 (suggested, not yet claimed) | `deferred-items.md` + `docs/UPSTREAM-LIMITS.md` limit 1. Not a Phase 5 must-have. Carried forward unchanged across every verification pass, including this one. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/web/src/stores/operator.ts` (`probe`'s three non-live outcomes) | Every path that discovers a session has ended routes through the shared `expireSession()` operation (CR-03) | VERIFIED | Read directly at lines 960-1030; all three non-live outcomes call `expireSession()` when `liveSessionRound` is defined, with the disabled/unreachable status set only after the clear. |
| `packages/web/src/stores/operator.ts` (`GATED_SLICE_RESET`, `liveSessionRound`) | One shared field list for both session-ending paths; a stored fact a second overlapping probe cannot erase (IN-11, WR-11) | VERIFIED | Read directly; the `wasLive` pre-await snapshot is gone, replaced by a landing-time read of the stored fact. |
| `packages/web/src/lib/operator.ts` (`AdvertiseActionResult`, `ReadvertiseActionResult`) | Both advertise verbs discriminate a 401 (honest re-login) from a 409 (the service's own paused reason) (WR-12) | VERIFIED | Read directly; the store branches on `unauthorized`/`refused`/`declined`/`unreachable` exactly as `finalizeCohort` does. |
| `packages/web/src/stores/operator.ts` (`askingRound`, `stillAsking`, `expireIfStillAsking`) | The session-identity rule applied uniformly to all four gated reads and eleven gated action verbs (IN-09, IN-10) | VERIFIED (with a documented residual, see WR-13/WR-14 below) | Read directly; 42 usages across the file. The helper trio itself is correctly applied to the `unauthorized` branch of every swept verb; the residual is that non-`unauthorized` branches of the action verbs, and one call site (`submitDraft`) entirely, were left outside the sweep, per the new findings. |
| `packages/service/src/runtime-settings.ts` (both `textKnob` consequence clauses), `docs/DEPLOY.md` | A refused boot seed states the same repair, in the same order, in the boot output, the console and the deploy document (IN-13) | VERIFIED | Read directly; both boot warnings and both `DEPLOY.md` rows now name the in-session repair before the environment edit, matching `REFUSED_SEEDS`'s console order. One further, un-corrected statement of the same fact remains at `docs/DEPLOY.md:358` (new finding IN-16). |
| `packages/service/src/hono-adapter.ts` (settings route comment) | The comment at the consuming end names the derivation and both caps it carries (IN-12) | VERIFIED | Read directly; comment-only diff, `bodyLimit` call unedited. |
| `packages/service/src/runtime-settings.ts` (`settingsBodyLimitBytes` docstring) | States which length the budget charges and why the trimmed-vs-transmitted gap is an accepted, measured decision (IN-14) | VERIFIED (documented and measured, not a behavior change) | Read directly; names the alternative not taken and cites the measurement row. |
| `packages/web/src/stores/operator.ts` (action verbs' non-`unauthorized` branches) | An answer to a question a now-ended session asked should never write into whichever session is live when it lands, on every branch, not only the 401 branch | **PARTIAL (WR-13, new this pass)** | Read directly at line 1290 and the equivalent points in the other ten action verbs: only the `unauthorized` branch is guarded by `stillAsking`; every other branch writes unconditionally, and the advertise success branch still uses the pre-round-6 status-as-identity comparison (`get().auth === 'logged-in'`) the sessionRound guard exists to replace. |
| `packages/web/src/stores/operator.ts` (`submitDraft`) | Every gated call site follows the one-honest-re-login rule on a 401 | **MISSING (WR-14, new this pass)** | Read directly: `CreateDraftResult` has no `unauthorized` member; a 401 on create renders the service's raw denial string as create-form validation copy and neither ends the session nor retires the round. |
| `packages/web/src/lib/operator.ts` (`sessionProbe`) | A transient server fault (5xx) should not be indistinguishable from a genuine session end | **REGRESSED IN CONSEQUENCE (WR-15, new this pass)** | Read directly: `sessionProbe` maps every non-200/non-404 status to `'logged-out'`, and `probe`'s CR-03 fix now routes that outcome through `expireSession()`, so a 5xx now clears the whole gated slice and narrates a session-expired claim the service never made, contradicting the store's own sibling reasoning that a transport fault is not evidence of expiry. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `packages/web/src/stores/operator.ts` (`probe`) | `expireSession()` | Every non-live probe outcome that discovers a live session has ended | WIRED (CR-03 closed) | Confirmed by direct code read; all three non-live branches call the shared operation when `liveSessionRound` is defined. |
| `packages/web/src/stores/operator.ts` (`signOut`, `expireSession`) | `GATED_SLICE_RESET` | One shared field list spread by both session-ending paths | WIRED (IN-11 closed) | Confirmed by direct code read. |
| `packages/web/src/lib/operator.ts` (`advertise`, `readvertise`) | `packages/web/src/stores/operator.ts` (the corresponding store actions) | A discriminated result routed to `expireIfStillAsking` on `unauthorized`, `actionFailedWith` on `refused` | WIRED (WR-12 closed) | Confirmed by direct code read. |
| `packages/web/src/stores/operator.ts` (all four gated reads, eleven gated action verbs) | `askingRound` / `stillAsking` / `expireIfStillAsking` | The session-identity rule applied uniformly, on the `unauthorized` branch | WIRED (IN-09 closed for the `unauthorized` branch) | Confirmed; 42 usages. |
| `packages/web/src/stores/operator.ts` (the same eleven action verbs' ok/refused/declined/unreachable branches) | `stillAsking` | The same rule applied to every OTHER branch, not only the 401 | **NOT WIRED (WR-13, new this pass)** | Confirmed by direct code read at line 1290 and the equivalent points elsewhere: only the `unauthorized` branch checks; every other branch writes unconditionally. |
| `packages/web/src/stores/operator.ts` (`submitDraft`) | `expireIfStillAsking` | A 401 on cohort creation taking the one honest re-login path | **NOT WIRED (WR-14, new this pass)** | Confirmed: `CreateDraftResult` has no `unauthorized` member for `submitDraft` to branch on. |
| `packages/web/src/lib/operator.ts` (`sessionProbe`) | `packages/web/src/stores/operator.ts` (`probe`'s non-live branch) | A 5xx should be distinguished from a 401 the way the store's own fault-catch branch already argues | **MISWIRED (WR-15, new this pass, consequence of the CR-03 fix)** | Confirmed: `sessionProbe` folds every non-200/non-404 status into the same value a 401 produces, and the CR-03 fix now treats that value as proof of expiry. |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| SVC-04 | 05-01..05-41 (core wave + all seven gap rounds) | Lifecycle control, pause/cancel/reconfigure advertising without restart | SATISFIED at code level (all 4 of its numbered Success Criteria VERIFIED); traceability table correctly reads `Gaps Found` pending `05-UAT.md` and the new WR-13/WR-14/WR-15 findings | SC1-SC4 all VERIFIED. `.planning/REQUIREMENTS.md` traceability table (lines 74-81) correctly reads `SVC-04 \| Phase 5 \| Gaps Found`. Note: the requirements CHECKBOX list (line 18) marks SVC-04 `[x]`, a pre-existing inconsistency with the traceability table's `Gaps Found`, predating round 7 (last edited at `ed27d79`) and untouched by this round; flagged as an info anti-pattern below. |
| SVC-05 | 05-13, 05-35, 05-37, 05-41 | Participation terms, DID-signed, server-verified, hash-bound, honest boundary disclosure | SATISFIED at code level; traceability table correctly reads `Gaps Found` | `tos.spec.ts` 28/28 green, unaffected by round 7. IN-13 closed this round on the boot-warning/deploy-doc disclosure surface. |
| PART-05 | 05-11, 05-29 | Participant-supplied esplora endpoint, mismatch guard, four failure messages, no silent fallback | SATISFIED (code level) | `tx-client.spec.ts` green, unaffected by round 7. Human Environment item remains. Traceability table already correctly reads `Gaps Found`. |
| PART-06 | 05-14 | PSBT wallet round trip, template-validated, guarded broadcast | SATISFIED (code level) | `psbt.spec.ts` suites green, unaffected by round 7. Human Environment item remains. Traceability table already correctly reads `Gaps Found`. |

No orphaned requirements: `SVC-04`, `SVC-05`, `PART-05`, `PART-06` are exactly the four requirements mapped to Phase 5 in `.planning/REQUIREMENTS.md`, and all four appear in PLAN frontmatter across the 41 plans, including `05-39`/`05-40` (`[SVC-04]`) and `05-41` (`[SVC-04, SVC-05]`).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `packages/web/src/stores/operator.ts` | 1290, and the equivalent point in the other ten gated action verbs | The session-identity sweep (IN-09) guards only the `unauthorized` branch of each action verb; every other branch, including the residual status-as-identity comparison `get().auth === 'logged-in'`, writes unconditionally into whichever session is live when the answer lands (WR-13) | Warning (does not gate a numbered roadmap Success Criterion) | Confirmed real by direct code read. A dead session's late answer can navigate a different, later session into a cohort it never opened, or paint a bad-tone failure sentence over an action it never took. Window is one click plus one round trip. |
| `packages/web/src/stores/operator.ts` | 1191-1204, over `lib/operator.ts:168` | `submitDraft` is a sixteenth gated call site the IN-09 enumeration omits; `CreateDraftResult` has no `unauthorized` member, so a 401 on cohort creation renders the service's raw denial string as form validation copy and ends no session (WR-14) | Warning | Confirmed real by direct code read. Bounded blast radius (one poll tick); not a Phase-5 roadmap surface. |
| `packages/web/src/lib/operator.ts` | 50-63, consumed by `packages/web/src/stores/operator.ts`'s `probe` | `sessionProbe` folds a 5xx into the same value a 401 produces; the CR-03 fix now treats that value as proof of an ended session, clearing the gated slice and narrating a false expiry on a transient fault (WR-15) | Warning (a genuine increase in blast radius introduced by this round's own fix, recorded in the frontmatter `regressions` list) | Confirmed real by direct code read of both files. Self-correcting on the next successful probe. |
| `packages/web/src/stores/operator.ts` | 976-995 | `probe`'s session-START branch takes no capture of its own landing-time context, so an answer landing after a different action already discovered the expiry can briefly re-establish a self-healing phantom session (IN-15) | Info | Confirmed real by direct code read. Self-healing within one further read. |
| `docs/DEPLOY.md` | 358 | A prose sentence fourteen lines above the two corrected `SERVICE_NAME`/`TERMS_TEXT` environment rows still states shortening as the only repair, missed by the IN-13 repo-wide correction (IN-16) | Info | Confirmed real by direct code read; the two table rows fourteen lines below are correctly split. |
| `packages/service/src/runtime-settings.ts` | 823-824, 839-841 | Both new boot-consequence clauses promise an "operator settings surface" that a fail-closed boot with no `OPERATOR_PASSWORD` never mounts (the settings routes are registered only inside `if (operatorAuth)`) (IN-17) | Info | Confirmed real by direct code read of `hono-adapter.ts`'s route registration; the same boot already prints a separate loud no-operator-password warning. |
| `packages/web/src/stores/operator.ts` | 786-826 | `GATED_SLICE_RESET`'s four nested array/object values (`cohorts: []`, `rows: []`, `operatorActions: []`, `view: {...}`) are the SAME shared instances copied by reference on every spread, a latent hazard if a future surface ever mutates one in place (IN-18) | Info | Confirmed real by direct code read; no current mutation site exists (checked: no `sort`/`reverse`/`splice`/`push` against a store array anywhere in `packages/web/src/components/operator/*`). |
| `.planning/REQUIREMENTS.md` | 18-19 vs 74-75 | The v1-requirements checkbox list marks `SVC-04` and `SVC-05` `[x]` while the traceability table below correctly reads `Gaps Found` for both | Info (pre-existing, not introduced or worsened by round 7) | Confirmed by `git log`: the checkbox list was last edited before `ed27d79` (the commit that corrected the traceability table alone), and round 7's own SUMMARYs confirm `.planning/REQUIREMENTS.md` is unedited by this round. Not gating; the traceability table is the field this workflow checks and it is correct. |

No `TBD`, `FIXME`, or `XXX` markers found in any round-7-touched file (independently grepped). No em-dash found in any round-7-touched file (independently grepped over the files 05-39/05-40/05-41 modified).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full test gate is green after round 7, including typecheck (`tsc -b` chained before `vitest run`) | `pnpm test` | 68 files, 1348 tests, all passing | PASS |
| Lint is clean | `pnpm lint` | No output beyond the invocation line | PASS |
| CR-03/WR-11/IN-11 fixes hold (probe routes non-live outcomes through the shared expiry, decided from a stored fact) | Read `probe`, `GATED_SLICE_RESET` and `liveSessionRound` directly | All three confirmed landed as claimed | PASS |
| WR-12 fix holds (advertise/readvertise discriminate 401/409/declined/unreachable) | Read the advertise store action directly | Confirmed, with the residual WR-13 finding noted separately | PASS (with residual noted) |
| IN-09/IN-10 fixes hold (one shared comparison, applied uniformly to the 401 branch of every gated call, and to every branch of the four gated reads) | Read `askingRound`/`stillAsking`/`expireIfStillAsking` and their 42 usages directly | Confirmed, with the residual WR-13/WR-14 findings noted separately | PASS (with residual noted) |
| IN-12/IN-13/IN-14 fixes hold (route comment, boot warnings, deploy doc, and budget docstring match shipped behavior) | Read `hono-adapter.ts`, `runtime-settings.ts` and `docs/DEPLOY.md` directly | Confirmed, with new finding IN-16 noted for one un-corrected prose sentence | PASS (with residual noted) |
| WR-13 (unswept success/failure branches of the eleven gated action verbs) | Read the advertise store action's success branch and the equivalent points in the other ten verbs | Confirmed real: `get().auth === 'logged-in'` at line 1290, not `stillAsking` | CONFIRMED (new warning finding) |
| WR-14 (submitDraft missing from the IN-09 sweep) | Read `submitDraft` and `CreateDraftResult` directly | Confirmed real: no `unauthorized` member | CONFIRMED (new warning finding) |
| WR-15 (a 5xx probe now narrates a false expiry) | Read `sessionProbe`'s status mapping and `probe`'s non-live branch directly | Confirmed real: every non-200/non-404 status maps to `'logged-out'`, now routed through `expireSession()` | CONFIRMED (new warning finding, recorded as a regression) |
| REQUIREMENTS.md traceability unedited by round 7, as its own prohibition required | Read lines 74-81 directly; `git log` on the file since `31baed2` | Table unchanged, correctly reads `Gaps Found` for all four | PASS |

### Probe Execution

Not applicable. This phase has no `scripts/*/tests/probe-*.sh` convention; verification relies on the vitest suite, the `e2e:*` harness scripts, and direct code reads.

## Human Verification Required

16 items carried forward, unchanged in count from the prior pass (see frontmatter `human_verification` and `05-UAT.md`, `pending: 16`). Round 7 touched no UAT item (confirmed directly against `05-UAT.md`'s frontmatter, and by `05-41-SUMMARY.md`'s own explicit statement). None of round 7's changes altered any existing item's preconditions in a way that would change its expected outcome, except that Environment 1 (the `DEPLOY.md` walk-through) should now find the boot-warning/environment-row repair guidance naming the in-session fix before the restart, matching the console caption.

WR-13, WR-14, WR-15, and the four new info findings are NOT human-verification items: each is a confirmed, reproducible code observation established by direct code read, not something requiring a person, a browser, or a clean machine to establish. They belong in a future gap-closure round's scope, not in `05-UAT.md`.

## Gaps Summary

Round 7 (05-39, 05-40, 05-41) closed all nine of its owner-approved targets: CR-03 (a probe discovering a session had ended now retires the round and clears the whole gated slice before anything renders under the next sign-in), WR-11 (the session-boundary decision now reads a stored fact rather than a pre-await snapshot, so two overlapping probes cross one session boundary between them), IN-11 (both session-ending paths now clear one shared field list, closing the five-field gap that let a stale advertise message reach the next session's create form), WR-12 (advertise and readvertise now discriminate an expired session from the service's own paused-advertising refusal, on the console's two most-used buttons), IN-09 (the session-identity rule is now followed, on the 401 branch, by all four gated reads and all eleven gated action verbs rather than four of fifteen), IN-10 (all four gated reads now compare one thing composed one way), IN-12 (the settings route's own comment now names the derivation it actually consumes), IN-13 (a refused boot seed now reads the same repair, in the same order, in the boot output, the console and the deploy document), and IN-14 (the derived settings budget's trimmed-versus-transmitted exception is now documented as a decision and measured by a dedicated row). All nine were independently reconfirmed in this pass by direct code read and by an independent rerun of `pnpm test` (68 files, 1348 tests, matching the round's own final figure) and `pnpm lint`, not by trusting the SUMMARYs' own reported numbers.

All 7 numbered roadmap Success Criteria continue to verify at the code level; none is FAILED. This pass's overall status remains `human_needed` rather than `gaps_found`: the 16 pending items in `05-UAT.md` (unchanged in count) are what stands between this phase and a `passed` verification, not any remaining code defect that falsifies a numbered truth.

A fresh deep code review conducted after round 7 landed (`05-REVIEW.md`) found, and this pass independently confirmed by direct code read rather than by citation, ZERO new critical findings (the first review in this series to find none), three new warnings, and four new info findings, all in the same session-identity subsystem round 6 and round 7 both rewrote. WR-13 is the success/failure half of the rule IN-09 swept only the `401` half of: eleven gated action verbs guard their `unauthorized` branch with `stillAsking` but write every other branch (ok, refused, declined, unreachable) unconditionally, so a dead session's late answer can still navigate a different, later session or paint a false failure into it, on a window of one click plus one round trip. WR-14 is a sixteenth gated call site (`submitDraft`) the IN-09 enumeration never named, where a 401 on cohort creation renders the service's raw denial string as form-validation copy rather than taking the one-honest-re-login path, bounded to one poll tick and outside any Phase-5 roadmap surface (cohort creation is SVC-01, Phase 1). WR-15 is a genuine consequence-level regression: the CR-03 fix gave `sessionProbe`'s pre-existing coarse status mapping (every non-200/non-404 answer, including a 5xx, mapped to the same value a 401 produces) new teeth it did not have before this round, so a transient server fault now clears the whole gated slice and narrates a session-expired claim the service never made, contradicting the store's own sibling reasoning that a transport fault is not evidence of expiry. Weighed independently against the literal text of all 7 numbered roadmap Success Criteria, none of these three findings falsifies any of them: the underlying lifecycle-control, pause/cancel, reconfiguration and terms-acceptance mechanisms all continue to function correctly on explicit, current operator input; what these findings corrupt is cross-session display state and one transient-fault narrative, on surfaces none of the 7 SCs states as their observable truth. The same reasoning applies as it did to CR-03 in the previous pass, before it was fixed.

This is the THIRD consecutive review pass to surface new, real findings in the exact same session-identity subsystem (round 6's review found CR-03/WR-11/WR-12/IN-09 through IN-14; round 7 closed all nine of those; round 7's own immediate re-review found WR-13/WR-14/WR-15/IN-15 through IN-18 in the code the round just finished). The severity trend is downward (zero critical this pass, versus one critical each of the prior two reviews) and each new finding's blast radius is narrower than its predecessor's, but the pattern itself, review-fix-review-fix-review revealing a new residual each time in one file, is worth the owner's explicit attention: either an eighth gap round closes WR-13/WR-14/WR-15 before the phase moves to `/gsd-secure-phase 5` or milestone completion, or the owner accepts the current state (zero critical, narrow-window warnings, no falsified Success Criterion) and proceeds with these three recorded as known, carried findings alongside the already-carried WR-03/WR-04/WR-05/IN-01/IN-02 and the deferred advert-slot-on-fill item. Both paths are consistent with this phase's own numbered goal being achieved at the code level; the choice is a risk-tolerance decision for the owner, not a gate this verification can resolve unilaterally.

---

_Verified: 2026-08-04_
_Verifier: Claude (gsd-verifier)_
