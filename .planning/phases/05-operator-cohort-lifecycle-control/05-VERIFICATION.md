---
phase: 05-operator-cohort-lifecycle-control
verified: 2026-08-03T22:31:57Z
status: human_needed
score: 7/7 roadmap Success Criteria verified at code level (0 present, behavior-unverified); 16 human-verification items still pending (unchanged in count); round 6's own targets (WR-08, WR-09, WR-10, IN-05, IN-06, IN-07, IN-08, REQUIREMENTS traceability) independently confirmed closed; ONE new critical finding (CR-03, unaddressed) plus two new warnings (WR-11, WR-12) and six new info findings independently confirmed real by direct code read, none of which falsifies a numbered roadmap Success Criterion
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: "7/7 roadmap Success Criteria verified at code level (per 05-VERIFICATION.md pass 6, dated 2026-08-03T19:24:21Z, before the sixth gap-closure round)"
  gaps_closed:
    - "WR-08 (a stale 401 from an ended session signed the NEW session out): all four gated reads (refreshCohorts, pollDetail, loadSettings, saveSettings) now capture the asking session's round before their await and expire the session only when that captured round is defined and still the live one. Confirmed by reading operator.ts directly (refreshCohorts ~963/983-985, pollDetail ~1433/1446-1448, loadSettings ~1312/1318-1320, saveSettings ~1346/1351/1370-1375) and by an independent rerun of operator.spec.ts and settings.spec.ts (91/91 passed in isolation)."
    - "WR-09 (the session-identity rule applied to one of three gated reads): pollDetail now compares the session AND the cohort id before its unreachable branch and its write; loadSettings and saveSettings compare the session on every writing branch, including saveSettings's rejection and transport-fault branches. Confirmed by direct code read."
    - "IN-07 (a round identified a probe EVENT, not a session, for the transition-into-live-session case): probe's transition condition is now captured before its first `set({ auth: 'checking' })`, so a probe that merely reconfirms an already-live session leaves the round unchanged. Confirmed by direct code read at operator.ts:790-821. NOTE: this closes only the transition-INTO-live-session half of the docstring's own bump-site claim; the transition-OUT-of-live-session half (a probe that discovers the session has ended) is the exact gap CR-03 reports below, in the same method."
    - "WR-10 (the refused-seed caption told the operator a restart was the only remedy, which CR-02 made untrue for the same round): RefusedSeed gained a third member (remedy) naming the in-session repair first and the environment edit second, for both SERVICE_NAME and TERMS_TEXT. Confirmed by reading SettingsView.tsx directly and the no-restart-in-cost pin in settings.spec.ts."
    - "IN-05 (a changed refused field still claimed the environment set nothing): a new refusedEnvDefaultText helper supplies the changed caption's parenthetical for a field carrying a refusal (\"changed this session (environment default: refused, see TERMS_TEXT)\"). Confirmed by direct code read."
    - "IN-06 (the derived settings body budget ignored the service-name cap, silently reopening the SC3 wedge past a name cap of roughly 682): settingsBodyLimitBytes is now a pure function of BOTH MAX_SERVICE_NAME_CHARS and MAX_TERMS_CHARS; the shipped constant is one application of it. Confirmed by reading runtime-settings.ts directly and by the cap-pair property rows in runtime-settings.spec.ts (independently re-derived: worst legal body 121369 bytes against a 122224 byte budget, 855 bytes slack)."
    - "IN-08 (the serviceName option's docstring claimed a boot-time constant with no edit surface, which was false since Phase 1/5's runtime settings holder): CreateServiceOptions.serviceName now documents itself as a runtime-editable boot seed. Confirmed by direct code read of packages/service/src/index.ts; git diff on non-comment lines is empty."
    - "REQUIREMENTS.md traceability (SVC-04 and SVC-05 marked Complete while 16 human items, two bearing directly on their own success criteria, remained pending, the third occurrence of a twice-reverted pattern): both rows reverted to Gaps Found, matching PART-05 and PART-06. Confirmed by direct read of REQUIREMENTS.md lines 74-75, 80-81: all four Phase 5 requirements now correctly read Gaps Found."
  gaps_remaining: []
  regressions: []
new_findings_this_pass:
  - "CR-03 (CONFIRMED REAL, NOT a numbered-SC gate, but severity warrants owner attention before the phase is considered closeable): `probe`'s branch that discovers a session has ENDED (packages/web/src/stores/operator.ts:813-817, the `logged-out`/`disabled` else branch, and :818-822, the transport-fault catch branch) neither retires `sessionRound` nor clears the gated slice `expireSession` clears (cohorts, rows, metrics, health, settings, detail, view, operator log, drill-down state: 40+ fields at operator.ts:909-948). `OperatorConsole` is mounted conditionally on the operator tab and probes on every mount (App.tsx:112-117, OperatorConsole.tsx:43-45), so a tab switch after an idle server-side expiry (the ordinary way a cookie stops being accepted) is what discovers the dead session, not a read's 401. Independently confirmed by direct code read: `probe`'s two non-live branches only `set({ auth: state })` or `set({ auth: 'logged-out', error: UNREACHABLE })`, with no call to `expireSession()` and no `sessionRound` bump anywhere in that path. Consequence, reproduced by 05-REVIEW.md and independently verified against the same unmodified source: the next sign-in (same or different operator, same credential, single-operator model) opens on the PREVIOUS session's cohort list, health chip, operator-actions log, drill-down document (member DIDs, pubkeys, signed updates, funding/recovery-key state included) and settings snapshot; the settings snapshot write additionally defeats OperatorConsole's once-per-session re-read latch (`settings === undefined`), so the new session's create form silently runs on the previous session's defaults until the operator happens to reopen Settings. This fails CLOSED against the SERVICE (every route still refuses the dead cookie; not an authentication bypass); it is stale operator-console state crossing a session boundary. No test in operator.spec.ts covers this path (independently confirmed by grep: zero matches for a probe row that seeds a live session and asserts the gated slice is cleared after a logged-out/disabled/unreachable probe answer). Weighed independently against all 7 numbered roadmap Success Criteria: none of them asserts session-boundary data isolation in the operator console as their observable truth (SC1-SC4 concern lifecycle-action mechanics that still function correctly when explicitly invoked; SC3's 'reconfigure without restart' mechanism, the PUT route, is unaffected -- only the pre-populated display can be stale), so this does not FAIL a numbered truth and does not force `gaps_found` on its own. It is recorded as a WARNING rather than a BLOCKER for that reason, despite the code review's own 'BLOCKER' severity label, which grades code quality/correctness rather than roadmap-goal achievement. Round 6's own Task 1 (05-36) explicitly scoped IN-07's fix to 'the round is taken only on a transition INTO a live session' and did not touch the transition OUT via probe, so this is a genuine, not-yet-closed gap in the exact family round 6 was built to close, and a 7th gap round is recommended before /gsd-secure-phase or milestone completion."
  - "WR-11 (confirmed real by direct code read, non-gating): the IN-07 transition condition (`wasLive`, captured before `checking`) is evaluated per-probe-call, so two overlapping probes (React StrictMode double-invokes mount effects in dev per main.tsx:6-10, or a fast tab toggle in production) both read the pre-checking status as non-live and both bump the round on landing, discarding an in-flight list read the IN-07 fix's own spec row claims survives. Self-healing on the next 4000ms poll tick; not a data-correctness defect, but the property the round-6 spec claims does not hold under the app's own StrictMode dev configuration. CR-03's fix would make this load-bearing rather than merely tidy."
  - "WR-12 (confirmed real by direct code read, pre-existing, non-gating): `advertise` and `readvertise` (operator.ts:~1089-1139) are the only two of the store's gated action verbs that do not discriminate a 401 from a generic failure; both collapse into 'Could not advertise the draft. Try again.' on an expired session, and also collapse a 409 (paused-advertising refusal) into the same generic sentence, contrary to every other gated call site's discriminated-result shape. The stale formError also survives expireSession (not one of the five fields signOut clears, per IN-11) and renders on the next session's create form. Transient (corrects within one list-poll tick), and pre-existing code untouched by round 6."
  - "IN-09 through IN-14 (confirmed real by direct code read, all recorded as info/cosmetic, none gating): IN-09 (the nine one-shot action verbs share WR-08's unguarded-401 shape by explicit, owner-approved scope fence, enumerated in 05-36-SUMMARY.md); IN-10 (refreshCohorts alone additionally compares auth status, an asymmetry with the other three reads that the round-6 spec comment acknowledges rather than resolves); IN-11 (expireSession does not clear the five fields signOut clears -- createStatus, formError, advertiseStatus, advertisingId, advertiseMessage -- so stale copy can render on the next session's hidden-then-revealed create form, feeding WR-12); IN-12 (the settings route's own comment in hono-adapter.ts:905-914, read directly, still says the budget is 'computed from MAX_TERMS_CHARS ... alone', stale since 05-38 made it a function of both caps); IN-13 (WR-10's fix corrected only the console caption; the boot-time consequence warnings in runtime-settings.ts and the corresponding DEPLOY.md lines still say a restart is the only repair, read directly and confirmed unchanged); IN-14 (the derived settings body budget charges the TRIMMED string length while applySettings trims after receiving, so a value padded with enough whitespace could theoretically exceed the transmitted-body budget while remaining under the holder's stored-length cap; not reachable from the console UI, recorded as a note)."
gaps: []
deferred:
  - truth: "The transport advert slot is also cleared when a cohort FILLS (keygen-complete), not only on the three settle paths, so a sibling open cohort stays listed but stops being joinable to a freshly connecting participant."
    addressed_in: "Phase 6 (suggested home; not yet claimed by a Phase 6 success criterion)"
    evidence: "Logged in .planning/phases/05-operator-cohort-lifecycle-control/deferred-items.md and documented as upstream limit 1 in docs/UPSTREAM-LIMITS.md. No Phase 5 must-have covers the fill path. Carried forward unchanged across every verification pass, including this one."
behavior_unverified_items: []
human_verification:
  - test: "Judgment 1 (05-UAT test 3): confirm by eye that no Close button exists anywhere on the console, and accept or refuse the deliberate re-reading of roadmap SC 1 (Closed is an automatic nth-seat stage, not an operator-pressed button, per locked CONTEXT D-01)."
    expected: "No Close control exists anywhere on the console; the Closed stage appears automatically the instant the nth seat fills, narrated with its caption, not as an act the operator performs."
    why_human: "A deliberate divergence from the literal wording of the roadmap goal that only the owner can accept or refuse. The stage's existence, trigger, and caption are automated and passing (packages/web/tests/operator-stage.spec.ts); the absence of a Close button anywhere on the rendered console is not asserted by any test."
  - test: "Judgment 2 (05-UAT test 12): read the Ended-group labels (a completed hermetic co-sign now reads 'Signed', not an anchor claim) and decide whether the change reads as newly honest rather than as a regression."
    expected: "Owner accepts the relabeling as an honest disclosure of what MuSig2 completion does and does not prove about an on-chain anchor."
    why_human: "Pure copy/framing judgment; every label and the anchor-wording guard are automated and passing (packages/web/tests/operator-rows.spec.ts)."
  - test: "Eyes (05-UAT test 13, plus narrowed 5/7/8): at a real browser viewport, walk the operator console list, drill-down (with cancel/finalize/seat-reclaim/funding disclosures all visible at once), health strip, a saved long SERVICE_NAME on both the console and the public directory header, and a long TERMS_TEXT body at a narrow viewport height, including the refused-seed source caption's now-split cost/remedy text."
    expected: "Nothing overflows, chips stay on screen, grouping is legible with no horizontal scroll; the long service name renders as plain escaped text on both surfaces without pushing chips off screen; the long terms body scrolls inside its capped container, wraps unbroken tokens, never escapes the card, and join controls stay reachable below it; the refused-seed caption's cost and remedy read as a warning, not as another grey caption."
    why_human: "Layout/legibility at a real screen size. The render harness is a static server render (no layout engine), so it can prove the capping/wrapping/caption-branch classes are present but never that anything actually fits or reads correctly."
  - test: "Copy (05-UAT test 9, plus two follow-on questions): read the four interpolated test-peer confirm strings, the verbatim 'unknown draft' refusal shown to an operator editing a stale draft, and the duplicated 'Operator console' heading shown on both sides of sign-in."
    expected: "All read correctly, in particular that the test-peer confirm states BEFORE the act that peers co-sign for real with throwaway keys; decide whether 'unknown draft' is the right sentence and whether the signed-in console deserves its own heading."
    why_human: "Copy/framing decisions; the four interpolated strings are pinned by nothing today (a named, not-yet-done follow-up), and the other two are deliberate, already-shipped choices awaiting the owner's sign-off."
  - test: "Hands (05-UAT tests 2, 4, 6, 10, 11): exercise 'Cancel edit' on a draft in place; open the finalize and ordinary-cancel confirms and read their in-place wording plus the in-flight disable; load the public directory paused/unpaused and narrow the window; cancel a cohort a participant is seated in and read their terminal card; dismiss an ended cohort row and refresh the page."
    expected: "Cancel edit closes the form with the draft unchanged; finalize/cancel confirms state the real consequence before it happens and disable correctly while in flight; the paused notice and empty states render distinctly and the controls row wraps at narrow widths; the cancel narration and next-step line appear together on the participant's terminal card; a dismissed row stays gone after a refresh."
    why_human: "The render harness is a static server render: no events fire, no effects run, so no state a click produces is reachable from it. Needs a DOM environment or a browser leg that does not exist yet."
  - test: "Environment 1 (05-UAT test 14): follow docs/DEPLOY.md from a clean machine using only the document (sign in, create, advertise, join, rehearse)."
    expected: "Every new control is reachable from the document alone; the retired filler knob appears nowhere; MIN_PARTICIPANTS/AUTO_FALLBACK/SSE_DEBUG and the /v1/config sample match what the service actually reads and serves. This walk should also confirm the corrected participation-terms paragraph (now naming the byte budget honestly per 05-38) and TERMS_TEXT row match observed behavior."
    why_human: "Needs a clean machine and a person following written instructions verbatim; not something a unit or e2e test can stand in for."
  - test: "Environment 2 (05-UAT test 15, narrowed, updated expectation): try a real host that blocks browser requests (CORS) and a real wrong-chain third-party esplora host against this service's actual default network."
    expected: "browser-rejected and mismatch (naming both chains) appear correctly, with the explicit switch-back control offered on failure and no silent fallback in either direction. This confirms SC6 (PART-05) at the code level, closed by 05-29, holds against a real host, which is the one thing the hermetic suite cannot prove."
    why_human: "Real CORS behavior and a real wrong-chain host cannot be produced hermetically."
  - test: "Environment 3 (05-UAT test 16): export the unsigned PSBT, sign it in an actual desktop or hardware wallet, return it, and broadcast; also confirm a large pasted PSBT keeps its field scrolling internally."
    expected: "The wallet accepts the exported .psbt; the returned PSBT validates against the template; the broadcast produces the same txid the local path would; the large-PSBT field does not reflow the step."
    why_human: "No wallet interoperability was verified in this environment; the 05-14 prohibition correctly forbids claiming any specific wallet works without this walk."
  - test: "Environment 4 (05-UAT test 17, narrowed): boot with LIVE=1 BROADCAST=1, engage Disable broadcast, and confirm the real boot behavior plus the disclosed cost of the stand-down."
    expected: "No route turns broadcast back on; the health strip still reports the live boot mode with a separate warn chip; a cohort advertised after the switch shows no Funding card while one advertised before keeps its funding surface; the owner accepts that the esplora-reachability badge stops refreshing once only post-switch cohorts remain."
    why_human: "Needs a real LIVE+BROADCAST boot, which cannot be produced hermetically, plus an owner cost/benefit judgment that only the owner can make."
  - test: "Cancel settle-race backstop (05-01, verification: backstop): attempt to cancel a cohort at the instant its signing round completes."
    expected: "The click is answered 404 with nothing changed, and the console never shows two ended records for one cohort id."
    why_human: "A timing race that cannot be reliably driven from a test harness; classified 'backstop' by design. What is checkable about it (the opaque 404 on an already-settled cohort, and idempotent folding so no duplicate ended record is ever filed) is automated and passing."
---

# Phase 5: Operator Cohort Lifecycle Control Verification Report

**Phase Goal:** The operator runs aggregation and manages a cohort's lifecycle (open, then close, then finalize) and pauses, cancels, or reconfigures advertising from the console, without restarting the process, removing the last hardwired, uncontrollable behavior.
**Verified:** 2026-08-03
**Status:** human_needed
**Re-verification:** Yes. This is the seventh verification pass, following the sixth gap-closure round (05-36, 05-37, 05-38), which was driven by the round-5 re-review's findings (WR-08, WR-09, WR-10, IN-05, IN-06, IN-07, IN-08) rather than by a failed roadmap Success Criterion (pass 6 already scored 7/7 at code level with `gaps: []`). This report independently confirms all eight round-6 targets are genuinely closed, and independently confirms a fresh deep code review (05-REVIEW.md, dated 2026-08-03T18:30:00Z, run after round 6 landed) that found one NEW critical finding (CR-03) plus further warnings and info findings in the exact session-guard method round 6 rewrote.

## Method

I did not trust SUMMARY.md narrative, 05-REVIEW.md's own severity labels, or the prior VERIFICATION.md's frontmatter as complete. For every claim that mattered to a Success Criterion or to a claimed gap closure, I re-derived it from the live tree myself.

- Ran `pnpm test` directly (which chains `tsc -b` then `vitest run` per `package.json`): **68 files, 1304 tests, all passing**, matching the round's own final SUMMARY (05-38) exactly, reproduced independently rather than copied.
- Ran `pnpm lint` directly: clean, zero output beyond the invocation line.
- Ran `pnpm vitest run packages/web/tests/operator.spec.ts packages/web/tests/settings.spec.ts` in isolation: 2 files, 91 tests, all passing (62 + 29), confirming the round-6 touched surface passes standalone.
- Read `packages/web/src/stores/operator.ts` lines 780-950 in full (the whole `probe`, `signIn`, `signOut`, `expireSession` block) directly, independent of the review's own line citations, and confirmed: (a) `probe`'s transition-into-live-session guard (`wasLive`, captured before `checking`) is real and matches the IN-07 fix as described; (b) `probe`'s `logged-out`/`disabled` branch (`:813-817`) and its `catch` branch (`:818-822`) call neither `expireSession()` nor bump `sessionRound`, confirming CR-03 exactly as the review describes it; (c) `expireSession` (`:909-948`) clears more than 25 named fields (cohorts, rows, metrics, health, settings, detail, view, operator log, editing/dismissal/cancel/finalize state), none of which CR-03's two branches touch.
- Ran `grep -n "CR-03\|probe.*logged-out\|probe.*disabled" packages/web/tests/operator.spec.ts`: zero matches, confirming no regression row exists for the probe-discovers-dead-session path, independent evidence that the gap is real and unguarded by the test suite.
- Read `packages/web/src/stores/operator.ts` lines 1348-1387 (`pollDetail`), 1275-1320 (`loadSettings`), 928-1003 (`refreshCohorts`) directly and confirmed the WR-08/WR-09 fixes: all four gated reads now capture the asking session's round before their await and compare it before both their unauthorized branch's expiry and their success/rejection branch's write, with `pollDetail` additionally retaining its cohort-id comparison alongside the new session comparison.
- Read `packages/web/src/components/operator/SettingsView.tsx` lines 60-110 directly and confirmed WR-10 (the `RefusedSeed` record's third `remedy` member, naming the in-session repair first) and IN-05 (`refusedEnvDefaultText`, supplying the changed caption's parenthetical for a refused field) are both landed as described.
- Read `packages/service/src/runtime-settings.ts` directly and confirmed IN-06: `settingsBodyLimitBytes` is exported as a function of both `MAX_SERVICE_NAME_CHARS` and `MAX_TERMS_CHARS`; independently re-derived the arithmetic `(200 + 20000) * 6 + 1024 = 122224` and confirmed it matches the shipped constant and the docstring's own figure.
- Read `packages/service/src/index.ts`'s `CreateServiceOptions.serviceName` docstring and confirmed IN-08's wording correction; confirmed via `hono-adapter.ts:635` that the served config still reads the name from the runtime holder per request (unchanged wiring, corrected documentation only).
- Read `docs/DEPLOY.md`'s participation-terms paragraph and the `TERMS_TEXT` row directly and confirmed they now name the byte budget rather than claiming the character cap is the only limit, matching the landed IN-06 fix; independently confirmed the two boot-warning `consequence` strings in `runtime-settings.ts` and the corresponding DEPLOY.md lines were NOT carried into this correction and still overclaim a restart is the only remedy (IN-13, unaddressed, recorded as info).
- Cross-referenced `REQUIREMENTS.md` directly: `SVC-04` and `SVC-05` now read `Gaps Found` (lines 74-75), matching `PART-05` and `PART-06` (lines 80-81, unchanged). All four requirements mapped to Phase 5 appear in PLAN frontmatter across the 38 plans; no orphaned requirements.
- Read `05-UAT.md` directly: `status: testing`, 16 items, all `result: [pending]`, byte-identical in count to the prior pass; round 6 touched no UAT item (confirmed by 05-38-SUMMARY.md's own statement, independently verified against the file's frontmatter `pending: 16`).
- Read `05-REVIEW.md` in full (the fresh round-6 re-review: 1 critical, 5 warnings, 8 info, `status: issues_found`) and independently weighed CR-03, WR-11, WR-12 and the six new info findings against the 7 roadmap Success Criteria rather than accepting the review's own severity framing. Independently reproduced CR-03's core claim by direct code read of the unmodified store (not by re-running the review's own repro scripts, which are not part of the committed tree).
- Independently confirmed via direct code read that round 6's own Task 1 (05-36, IN-07) explicitly scoped its fix to "the round is taken only on a transition INTO a live session" (05-36-PLAN.md's own action text) and did not address the transition-out-via-probe case, which is exactly what CR-03 reports; this is a genuine gap in round 6's own stated objective, not a claim contradicted by round 6's own SUMMARY (05-36-SUMMARY.md enumerates the nine action-verb exemptions but does not mention the probe's own non-live branches as a further exemption, so it is an omission rather than a documented scope fence).

## Goal Achievement

### Observable Truths (roadmap Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Operator moves a cohort through open, close and finalize from the console and the directory reflects each state change | VERIFIED | Unaffected by round 6 (no round-6 file touches `LifecycleActions.tsx` or the cohort-keyed guard 05-28 added). Confirmed by the full `pnpm test` pass (`operator.spec.ts`, `lifecycle.spec.ts`, both green). CR-03 does not affect this mechanism: a stale drill-down view can open onto a wrong cohort id, but the 05-28 mismatch guard (unedited, still green) prevents any action from being armed against it. Human judgment item (Judgment 1) remains for the "no Close button" reading. |
| 2 | Operator pauses or cancels advertising so new cohorts stop being offered, without killing the running service | VERIFIED | No round-6 file touches the pause/advertise-gate mechanism. Confirmed by the full `pnpm test` pass and by the unedited `runAdvertisingToggle` path (still uses the unguarded-401 shape by explicit round-6 scope fence, WR-12/IN-09, non-gating). |
| 3 | Operator reconfigures cohort shape (capacity, threshold, beacon type for the next cohort) without editing env vars or restarting | VERIFIED | The IN-06 gap that could have silently reopened the SC3 wedge past a service-name cap of ~682 is closed: `settingsBodyLimitBytes` now derives from both caps, independently re-derived (122224 byte budget, 855 bytes slack over the worst legal body). The save mechanism itself (`PUT /v1/operator/settings`) is unaffected by CR-03; CR-03 affects only the pre-populated DISPLAY the operator sees before editing, not the route's ability to persist a genuinely reconfigured shape once submitted. |
| 4 | A canceled or closed cohort no longer appears as joinable in the participant directory | VERIFIED | Unaffected by round 6. Confirmed by the full `pnpm test` pass; no round-6 file touches the directory-visibility path. |
| 5 | Participation terms accepted at join and recorded as a DID-signed, server-verified, terms-hash-bound artifact, with the app-level enforcement boundary disclosed honestly (SVC-05) | VERIFIED (code level) | `packages/service/tests/tos.spec.ts` (28 tests, part of the full 1304-test pass, all green), unaffected by round 6's service-side changes (05-38 touched only the settings body budget, not the acceptance/verification mechanism). 05-37 additionally closed WR-10 and IN-05 on the disclosure surface. Human judgment items remain (Judgment 2, Eyes). |
| 6 | Participant can point browser chain reads at their own esplora endpoint, with a network-mismatch guard, four distinguishable failure messages, no silent fallback, real-funds guard rails unweakened (PART-05) | VERIFIED (code level) | No round-6 file touches `esplora.ts` or `tx-client.ts`. Confirmed by the full `pnpm test` pass (`tx-client.spec.ts`, 55/55). The live leg still needs a person; see Human Verification. |
| 7 | Participant can sign the registration transaction in their own wallet through a PSBT round trip validated against the exact template before anything is broadcast (PART-06) | VERIFIED (code level) | No round-6 file touches this surface. Confirmed via the full `pnpm test` pass (`packages/shared/tests/psbt.spec.ts`, `packages/web/tests/psbt.spec.ts`, both green). Real wallet interoperability: human item (Environment). |

**Score:** 7/7 truths verified (0 present, behavior-unverified)

**Independent note on CR-03 and SC1/SC3:** CR-03 is real (confirmed above by direct code read and reproduction of its mechanics against the unmodified store), but weighed independently against the literal text of each of the 7 numbered Success Criteria, it does not falsify any of them: the actual lifecycle-control, pause/cancel, reconfiguration, and terms-acceptance MECHANISMS all function correctly when explicitly invoked with the operator's own current input. What CR-03 breaks is the console's promise that what it *displays* after a session boundary belongs to the session viewing it, which none of the 7 SCs states as their observable truth. This is the same reasoning the prior pass applied to WR-08/WR-09/WR-10 before they were fixed. Given CR-03's severity (a confirmed, reproducible cross-session data leak of operator-sensitive state including member DIDs, pubkeys, funding and recovery-key material, and a defect in the exact method round 6 was built to fix), it is recorded as a prominent WARNING below rather than silently carried, and a 7th gap round is recommended before the phase is considered ready for `/gsd-secure-phase` or milestone completion.

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | The transport advert slot is also cleared when a cohort FILLS (keygen-complete), not only on the three settle paths, so a sibling open cohort stays listed but stops being joinable to a freshly connecting participant | Phase 6 (suggested, not yet claimed) | `deferred-items.md` + `docs/UPSTREAM-LIMITS.md` limit 1. Not a Phase 5 must-have. Carried forward unchanged across every verification pass, including this one. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/web/src/stores/operator.ts` (`sessionRound`, `probe` transition guard) | The round bumps only on a transition into a live session (IN-07) | VERIFIED (partial per docstring's full claim, see CR-03) | Read directly at lines 790-812; `wasLive` captured before `checking`, the confirming-probe branch leaves the round unchanged. The docstring's paired claim (a round also identifies the END of a session) is NOT upheld by the probe's own discovery-of-a-dead-session branch, which is CR-03. |
| `packages/web/src/stores/operator.ts` (`refreshCohorts`, `pollDetail`, `loadSettings`, `saveSettings` unauthorized + writing branches) | Session-identity discipline applied uniformly to every gated read's 401 handling and success/rejection handling (WR-08, WR-09) | VERIFIED | Read directly; all four reads capture and compare the asking session before expiring or writing. Confirmed by rerunning the touched spec files (91/91) independently. |
| `packages/web/src/components/operator/SettingsView.tsx` (`RefusedSeed`, `refusedEnvDefaultText`) | Accurate cost/remedy split and a changed caption that names the refusal rather than claiming the environment set nothing (WR-10, IN-05) | VERIFIED | Read directly at lines 60-110. |
| `packages/service/src/runtime-settings.ts` (`settingsBodyLimitBytes`, `MAX_SERVICE_NAME_CHARS` exported) | The settings body budget derived from BOTH string caps, not just the terms cap (IN-06) | VERIFIED | Read in full; the exported function and the re-derived arithmetic match the docstring exactly. |
| `packages/service/src/index.ts` (`CreateServiceOptions.serviceName` docstring) | Accurate description of a runtime-editable boot seed, not a boot-time constant (IN-08) | VERIFIED | Read directly; git diff on non-comment lines confirmed empty by the SUMMARY, consistent with wiring being unchanged. |
| `.planning/REQUIREMENTS.md` (SVC-04, SVC-05 traceability rows) | Reverted to `Gaps Found`, matching PART-05/PART-06, pending 05-UAT.md closure | VERIFIED | Read directly at lines 74-75, 80-81: all four Phase 5 requirements now correctly read `Gaps Found`. |
| `packages/web/src/stores/operator.ts` (`probe`'s non-live and transport-fault branches) | Ending a session discovered by a probe should be the same operation as ending one discovered by a read's 401: round retired, gated slice cleared | **MISSING (CR-03, new this pass)** | Read directly at lines 813-822: neither branch calls `expireSession()` nor bumps `sessionRound`. A tab switch after an idle server-side expiry is the ordinary way this path fires, and the next sign-in inherits the previous session's cohort list, health, drill-down document and settings snapshot (the last of which also defeats the console's once-per-session settings re-read latch). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `runtime-settings.ts` (`settingsBodyLimitBytes`) | `hono-adapter.ts` (`PUT /v1/operator/settings` `bodyLimit`) | The route's byte budget derived from both string caps | WIRED | Confirmed by direct read; the route consumes `SETTINGS_BODY_LIMIT_BYTES` by name, unedited by 05-38 (a one-file fix). |
| `OperatorConsole.tsx` (list poll) | `operator.ts` (`refreshCohorts`) | Session-identity round guard, on both the ok path and the 401 path | WIRED (both paths, closing WR-08's ok-path-only gap from the prior pass) | Confirmed by direct code read. |
| `CohortDetail.tsx` (detail poll) | `operator.ts` (`pollDetail`) | Session-identity guard alongside the cohort-identity guard | WIRED (closing WR-09) | Confirmed by direct code read; both comparisons present, in the order the review specified. |
| `SettingsView.tsx` (settings read/save) | `operator.ts` (`loadSettings`, `saveSettings`) | Session-identity guard on every writing branch | WIRED (closing WR-09) | Confirmed by direct code read. |
| `OperatorConsole.tsx` / `App.tsx` (operator-tab mount) | `operator.ts` (`probe`) | Session-ending discovered by a probe should retire the round and clear the gated slice, the same as a read's 401 discovering it | **NOT WIRED (CR-03, new this pass)** | Confirmed by direct code read: the probe's non-live and transport-fault branches perform neither action. The WR-08/WR-09 guards this round landed correctly refuse to act on a STALE answer once a session has ended, but nothing marks the session as having ended when a probe (rather than a read) is what discovers it. |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| SVC-04 | 05-01..05-38 (core wave + all six gap rounds) | Lifecycle control, pause/cancel/reconfigure advertising without restart | SATISFIED at code level (all 4 of its numbered Success Criteria VERIFIED); traceability table correctly reads `Gaps Found` pending `05-UAT.md` and the CR-03 finding | SC1-SC4 all VERIFIED. `REQUIREMENTS.md` now correctly reads `SVC-04 \| Phase 5 \| Gaps Found`, matching this phase's own pending human items and the new CR-03 finding in the same subsystem this requirement covers. |
| SVC-05 | 05-13, 05-35, 05-37 | Participation terms, DID-signed, server-verified, hash-bound, honest boundary disclosure | SATISFIED at code level; traceability table correctly reads `Gaps Found` | `tos.spec.ts` 28/28 green; WR-10 and IN-05 closed this round on the disclosure surface. `REQUIREMENTS.md` now correctly reads `SVC-05 \| Phase 5 \| Gaps Found`. |
| PART-05 | 05-11, 05-29 | Participant-supplied esplora endpoint, mismatch guard, four failure messages, no silent fallback | SATISFIED (code level) | `tx-client.spec.ts` 55/55 green, unaffected by round 6. Human Environment item remains. Traceability table already correctly reads `Gaps Found`. |
| PART-06 | 05-14 | PSBT wallet round trip, template-validated, guarded broadcast | SATISFIED (code level) | `psbt.spec.ts` suites green, unaffected by round 6. Human Environment item remains. Traceability table already correctly reads `Gaps Found`. |

No orphaned requirements: `SVC-04`, `SVC-05`, `PART-05`, `PART-06` are exactly the four requirements mapped to Phase 5 in `REQUIREMENTS.md`, and all four appear in PLAN frontmatter across the 38 plans.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `packages/web/src/stores/operator.ts` | 813-822 | `probe`'s branches for a session it discovers has ended (`logged-out`/`disabled`) or cannot reach (transport-fault `catch`) neither retire `sessionRound` nor clear the gated slice `expireSession` clears (CR-03) | **Warning (does not gate a numbered roadmap Success Criterion; independently re-weighed, not accepted from the review's own "BLOCKER" label without re-derivation)** | Confirmed real by direct code read and reproduction of its mechanics. The console fails closed against the SERVICE (every route still refuses the dead cookie) but presents stale, potentially sensitive cross-session state (member DIDs, pubkeys, funding/recovery-key material, settings defaults) to the next signed-in session, on the operator tab's ordinary mount path (a tab switch after an idle expiry), not merely a race. This is the exact method round 6 (05-36) rewrote for the closely related WR-08/WR-09/IN-07 findings, and round 6's own scope statement ("the round is taken only on a transition INTO a live session") did not extend to the transition out. Recommend a 7th gap round before `/gsd-secure-phase` or milestone completion. |
| `packages/web/src/stores/operator.ts` | 795 (`wasLive`), consumed 799-812 | The IN-07 transition condition is decided from a snapshot that a second, overlapping probe (React StrictMode double-invokes mount effects in dev; a fast tab toggle in production) also reads as non-live, so two overlapping probes still bump the round and still discard an in-flight read the round-6 spec's own row claims survives (WR-11) | Warning | Confirmed real by direct code read against `main.tsx`'s StrictMode configuration. Self-healing within one poll tick; not a data-correctness defect, but the round-6 spec's own claim does not hold under the app's own dev configuration. |
| `packages/web/src/stores/operator.ts` | ~1089-1139 | `advertise` and `readvertise` are the only two gated action verbs that collapse a 401 and a 409 into one generic "Could not advertise the draft. Try again." message, instead of the discriminated-result shape every other gated verb uses (WR-12, pre-existing, not touched by round 6) | Warning | Confirmed real by direct code read. Transient (self-corrects within one list-poll tick) but on the console's two most-used buttons. |
| `packages/service/src/hono-adapter.ts` | 905-914 | The settings route's own comment still says the byte budget is "computed from `MAX_TERMS_CHARS` ... alone", stale since 05-38 made it a function of both string caps (IN-12) | Info | Confirmed real by direct code read. Documentation staleness only; the route's actual behavior (consuming the exported `SETTINGS_BODY_LIMIT_BYTES` constant) is correct. |
| `packages/service/src/runtime-settings.ts`, `docs/DEPLOY.md` | consequence clauses; lines 510, 516 | WR-10's fix corrected only the console caption; the boot-time warning strings and the corresponding DEPLOY.md lines still state a restart is the only remedy for a refused seed, which CR-02 (an earlier round) made untrue (IN-13) | Info | Confirmed real by direct read of both files. Docs are incomplete rather than false (DEPLOY.md does separately note the value is runtime-editable), but the specific sentence WR-10 corrected elsewhere was not carried here. |

No `TBD`, `FIXME`, or `XXX` markers found in any round-6-touched file (independently grepped). No em-dash found in any round-6-touched file (independently grepped over the files 05-36/05-37/05-38 modified).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full test gate is green after round 6, including typecheck (`tsc -b` chained before `vitest run`) | `pnpm test` | 68 files, 1304 tests, all passing | PASS |
| The round-6-touched surface passes in isolation | `pnpm vitest run packages/web/tests/operator.spec.ts packages/web/tests/settings.spec.ts` | 2 files, 91 tests, all passing | PASS |
| Lint is clean | `pnpm lint` | No output beyond the invocation | PASS |
| WR-08/WR-09 fixes hold (session-identity guard on all four gated reads) | Read `refreshCohorts`, `pollDetail`, `loadSettings`, `saveSettings` directly and confirm each captures and compares the asking session before expiring or writing | All four confirmed | PASS |
| CR-03 (a probe discovering a dead session neither retires the round nor clears the gated slice) | Read `probe`'s non-live and transport-fault branches directly; grep `operator.spec.ts` for coverage of this path | Confirmed real: no `expireSession()` call, no round bump, zero test coverage for this exact path | CONFIRMED (new warning finding, not a test failure in the shipped suite, which simply does not test this path) |
| REQUIREMENTS.md traceability correction | Read lines 74-75, 80-81 directly | `SVC-04`, `SVC-05`, `PART-05`, `PART-06` all read `Gaps Found` | PASS |

### Probe Execution

Not applicable. This phase has no `scripts/*/tests/probe-*.sh` convention; verification relies on the vitest suite, the `e2e:*` harness scripts, and direct code reads.

## Human Verification Required

16 items carried forward, unchanged in count from the prior pass (see frontmatter `human_verification` and `05-UAT.md`, `status: testing`, all `result: [pending]`). Round 6 touched no UAT item (confirmed directly against `05-UAT.md`'s frontmatter `pending: 16`, and by 05-38-SUMMARY.md's own explicit statement). None of round 6's changes altered any existing item's preconditions in a way that would change its expected outcome, except that Environment 1 (the DEPLOY.md walk-through) should now find the participation-terms guidance naming the byte budget honestly rather than only the character cap.

CR-03, WR-11, WR-12, and the six new info findings are NOT human-verification items: each is a confirmed, reproducible code defect established by direct code read and (for CR-03) by tracing the unmodified store's control flow, not something requiring a person, a browser, or a clean machine to establish. They belong in a future gap-closure round's scope, not in `05-UAT.md`.

## Gaps Summary

Round 6 (05-36, 05-37, 05-38) closed all eight of its owner-approved targets: WR-08 (a stale 401 from an ended session no longer signs the next session out, on all four gated reads), WR-09 (the session-identity rule now applies to every gated read's writing branches, not one of three), WR-10 (the refused-seed caption now names the in-session repair first, not only a restart), IN-05 (a changed refused field no longer claims the environment set nothing), IN-06 (the settings body budget is now derived from both string caps, closing a silent reopening of the SC3 wedge past a service-name cap of ~682), IN-07 (a probe that merely reconfirms an already-live session no longer retires its round), IN-08 (the `serviceName` option's docstring now matches its runtime-editable shipped behavior), and the `REQUIREMENTS.md` traceability correction (SVC-04 and SVC-05 reverted from the premature `Complete` mark to `Gaps Found`, matching PART-05/PART-06 and closing the third occurrence of this phase's twice-reverted pattern). All eight were independently reconfirmed in this pass by direct code read and by an independent rerun of `pnpm test` (68 files, 1304 tests, matching the round's own final figure) and `pnpm lint`, not by trusting the SUMMARYs' own reported numbers.

All 7 numbered roadmap Success Criteria continue to verify at the code level; none is FAILED. This pass's overall status remains `human_needed` rather than `gaps_found`: the 16 pending items in `05-UAT.md` (unchanged in count) are what stands between this phase and a `passed` verification, not any remaining code defect that falsifies a numbered truth.

A fresh deep code review conducted after round 6 landed (`05-REVIEW.md`) found, and this pass independently confirmed by direct code read rather than by citation, ONE new critical finding: CR-03, the exact same session-ending path (a `probe` discovering the session has died, rather than a read's 401 discovering it) that round 6's own IN-07 task touched but did not close for the "session ends" half of its docstring's claim. `probe`'s `logged-out`/`disabled` branch and its transport-fault `catch` branch neither retire `sessionRound` nor clear the more-than-25-field gated slice `expireSession` clears, so the next sign-in (on the operator tab's ordinary conditional-mount path, which is a tab switch after an idle server-side expiry, not a rare race) inherits the previous session's cohort list, health chip, operator-actions log, drill-down document (member DIDs, pubkeys, signed updates, funding and recovery-key state included), and settings snapshot, the last of which also silently defeats the console's once-per-session settings re-read latch. This fails CLOSED against the service itself (no route accepts the dead cookie), so it is not an authentication bypass; it is stale, potentially sensitive operator-console state crossing a session boundary. Weighed independently against the literal text of all 7 numbered roadmap Success Criteria, CR-03 does not falsify any of them (the underlying lifecycle-control, pause/cancel, reconfiguration, and terms-acceptance mechanisms all function correctly on explicit, current operator input; what CR-03 corrupts is only what the console pre-populates and displays across a session boundary), so it does not force `gaps_found` on its own, applying the same reasoning the prior pass applied to WR-08/WR-09/WR-10 before they were fixed. Given CR-03's severity, however, and given that it sits in the exact family and the exact method round 6 was built to close, it is recorded here as a prominent warning rather than folded quietly into the info list, and a 7th gap round targeting it (plus the newly confirmed WR-11 and WR-12) is recommended before `/gsd-secure-phase 5` or milestone completion.

Two further new warnings (WR-11, a StrictMode double-probe overlap that defeats the round-6 IN-07 spec row's own claim in dev; WR-12, pre-existing generic-error copy on the `advertise`/`readvertise` verbs) and six new info findings (IN-09 through IN-14, an enumerated scope fence, an asymmetric guard clause, an incomplete clear-on-expiry field list, a stale route comment, an incompletely-carried caption correction, and a trimmed-vs-transmitted-length budget nuance) were also independently confirmed real by direct code read. None of these falsifies a numbered roadmap Success Criterion either; all are recorded for a future round.

---

_Verified: 2026-08-03_
_Verifier: Claude (gsd-verifier)_
