---
phase: 03-participant-submit-co-sign-track-and-resolve
plan: 06
type: execute
wave: 4
depends_on: [03-05]
files_modified:
  - packages/web/src/components/cohort/CompletionSummary.tsx
  - packages/web/src/components/cohort/StageTimeline.tsx
  - packages/web/src/components/cohort/CohortPage.tsx
  - packages/web/src/components/participant/RegisterPanel.tsx
  - packages/web/src/components/participant/PublishPanel.tsx
  - packages/web/src/components/participant/ResolvePanel.tsx
  - packages/web/src/components/participant/ResultCard.tsx
  - e2e/browser-participant-cohort.ts
  - package.json
autonomous: true
requirements: [PART-04]
user_setup: []

must_haves:
  truths:
    - "populated (UI E5): once the anchor stage completes, auto-resolve (D-28) runs and the completion summary shows the plain-language reflected outcome ('Your update is reflected. The resolved DID document now lists this cohort's beacon service (version {N}).') on a live path where findAppendedBeacon is present."
    - "empty (UI E5): on the hermetic (anchor.enabled:false) path the round-trip is the honest 'Resolved to the genesis document. This no-broadcast service has no on-chain signal to discover...' outcome - expected, NOT a mismatch warning (D-29, Finding 7)."
    - "error / partial (UI E5): on a live path with the update not yet in the resolved document, the mismatch outcome renders the indexing explanation + a 'Resolve again' retry (D-29); the brief 'resolving...' retry is gated on anchor.enabled."
    - "loading (UI E5): resolve-in-flight shows the brief 'resolving...' state gated on the anchor enabled bit (D-28)."
    - "loading / backstop-adjacent (UI E6): the anchor sub-steps render Signed -> Broadcast (txid + 'View on explorer') -> Confirmed on the ~5s poll cadence and FREEZE at first confirmation (no live conf count, no reorg UX, D-22)."
    - "partial (UI E1): an explicit k-of-n fallback outcome states the cohort anchored via the fallback path with {k} of {n} signatures and whether this participant's update was included (D-23), extending the Phase-2 G-02-1 honesty."
    - "error (UI E1/E7): service-unreachable raises the distinct 'Can't reach this service' banner with quiet auto-retry; stages freeze honestly and it is never a terminal by itself (D-24, closes 02-09 WR-02)."
    - "error (UI E1/E7): a terminal failure lands on the page with a best-effort specific reason (phase timeout / cohort vanished / runner error / seat lost) and the honest 'The cohort ended and this service didn't say why.' fallback + 'Back to cohorts' (D-25, Finding 2 stall copy)."
    - "error (UI E2): submit-path failures surface the D-24 banner (transient) or the D-25 terminal states; a stalled cohort uses the dedicated 'The cohort ended. It stalled waiting for all members to submit their updates.' copy (Finding 2)."
    - "error (UI E3): seated-state directory-unreachability routes to the cohort page's D-24/D-25 states rather than the browse-directory error card."
    - "error (UI E10): the 'Your cohort' chip freezes with the stages under D-24 and reflects the terminal stage label under D-25; it never invents a state the page does not show."
    - "partial / non-error (UI E1): cooperative non-inclusion is a distinct NON-error outcome ('The cohort proceeded without your update. {reason}...') that KEEPS reporting the cohort's anchor result (D-10)."
    - "The completion summary persists on the page (D-10): resolved DID document + resolution metadata behind a raw-detail expander, a plain-language result (beacon service added, version N), a 'Download sidecar (resolver artifacts)' export (existing lib/sidecar logic), and a 'Browse cohorts' CTA; it is replaced on the next join (no history, v2 PMG-01)."
    - "error confirm-dialog (UI E8): 'Start over' from any terminal state clears the record AND erases the in-memory DID key behind the danger-variant, explicit key-custody confirmation (D-10)."
    - "Conditional post-completion stages (D-17, Finding 8): on a live+broadcast+KEY+included path the first-update registration stage appears (reusing RegisterPanel logic), and when GET /v1/ipfs enabled the opt-in IPFS publish appears - both AFTER Signed/Anchored, never on the hermetic path (the publishable artifacts exist only at cohort-complete)."
    - "The surviving Register/Publish/Resolve/Result LOGIC is absorbed as cohort-page stage internals (CompletionSummary) and the four standalone tail panel files are deleted (D-31); no BrowseView/cohort import references them afterward."
    - "The browser capstone (D-32) drives ONE real Chromium page (the participant) while headless in-process peers fill the remaining seats and proves: browse -> pick -> join -> explicit submit CLICK -> co-sign -> mode-honest SIGNED (not 'anchored') -> auto-resolve -> round-trip check, plus criterion 4 (the directory is the landing; there is no KeyGen-first stepper affordance)."
    - statement: "backstop (UI E6): the anchor sub-step chevron renders each state (Signed/Broadcast/Confirmed) on the ~5s poll cadence and polling stops at confirmed/failed (D-22)."
      verification: backstop
    - statement: "backstop (UI E5): the full resolved DID document renders inside the expander treatment without card overflow."
      verification: backstop
  artifacts:
    - packages/web/src/components/cohort/CompletionSummary.tsx
    - e2e/browser-participant-cohort.ts
    - "package.json script: e2e:browser:participant"
  key_links:
    - "anchor DTO (03-04 poll) -> StageTimeline anchor sub-steps (D-22) + CompletionSummary mode-honest copy"
    - "auto-resolve (03-04) -> resolution + roundTripOutcome -> CompletionSummary presentation + export"
    - "browser-harness.ts + browse-join-cohort.ts boot pattern -> e2e/browser-participant-cohort.ts capstone (headless peers + one real page)"
  prohibitions:
    - statement: "MUST NOT claim 'your update is reflected' or render a txid/on-chain anchor when the resolve returned the genesis document on a hermetic (enabled:false) service; that is the expected outcome, not a mismatch (D-07/D-29)."
      status: flagged-unverified
      verification: flagged
    - statement: "MUST NOT treat a transient poll/SSE failure as a terminal failure; the unreachable banner freezes stages with quiet retry (D-24)."
      status: flagged-unverified
      verification: flagged
    - statement: "MUST NOT invent a failure reason when the cohort ends without one; use the honest 'didn't say why' fallback (D-25)."
      status: flagged-unverified
      verification: flagged
    - statement: "MUST NOT render the conditional registration or IPFS stage on a hermetic service; they are live/enabled-gated post-completion stages only (D-17, Finding 8)."
      status: flagged-unverified
      verification: flagged
    - statement: "MUST NOT wire the browser capstone into CI in this phase; CI wiring + the red e2e:browser rewrite is Phase-6 debt (D-32)."
      status: flagged-unverified
      verification: flagged
    - statement: "MUST NOT use the em-dash character in any new copy, comment, or code; no new demo-crowd framing terms."
      status: flagged-unverified
      verification: flagged
---

<objective>
Complete the PART-04 tracking + resolve slice on the cohort page: the mode-honest anchor sub-steps (D-22), the auto-resolve completion summary with the three-way round-trip check and sidecar export (D-28/D-29/D-30), the conditional registration/IPFS post-completion stages (D-17), the honest degraded states (unreachable / terminal / cooperative non-inclusion / k-of-n fallback, D-23/D-24/D-25/D-10), and "Start over" - then delete the four tail panels whose logic is absorbed, and prove the whole loop with a browser-level hermetic capstone (D-32, criterion 4).

Purpose: After this plan a participant can track the anchor, resolve, and see an honest round-trip and honest failures, and the phase's end-to-end proof exists.
Output: `CompletionSummary.tsx`, anchor sub-steps + degraded states on the cohort page, four deleted tail panels, and `e2e/browser-participant-cohort.ts` + `e2e:browser:participant`.
</objective>

## Phase Goal (user story, derived from the ROADMAP Phase 3 goal)

**As a** participant who joined a cohort by choice, **I want to** submit my DID update, take part in the cohort's n-of-n MuSig2 co-signing round, track the anchor, and resolve my updated DID, **so that** I complete the real aggregation lifecycle from the cohort I discovered, not a standalone demo stepper.

> This is the final Wave-4 slice: after it a participant can track anchor status, resolve, and see honest round-trip + honest failures, and the browser capstone proves the whole two-sided loop end to end (criterion 4).

## Flagged assumption (specless-probe fallback, PART-04 unclassified edge)

The edge-coverage probe surfaced one PART-04 item with category `unclassified` ("review manually") and no descriptor. It is NOT auto-resolved. Interpreted here as the general PART-04 tracking/resolve reliability surface; its concrete edges are covered by the truths above - anchor freeze at first confirmation (D-22), resolver-lag retry gated on `enabled` (Finding 7), the hermetic-genesis honest round-trip outcome (D-29), and the unreachable/terminal degraded states (D-24/D-25). Surfaced as an explicit planner assumption per the specless-probe fallback (never silently dropped); if end-of-phase human verify reveals a distinct uncovered PART-04 reliability edge, fold it into a gap-closure plan.

## Artifacts this phase produces (this plan)

- `packages/web/src/components/cohort/CompletionSummary.tsx` (NEW): absorbs ResultCard/ResolvePanel/PublishPanel/RegisterPanel logic as stage internals.
- Anchor sub-steps (Signed/Broadcast/Confirmed) in `StageTimeline.tsx`; degraded-state banners + Start-over in `CohortPage.tsx`.
- Deletions: `components/participant/{RegisterPanel,PublishPanel,ResolvePanel,ResultCard}.tsx`.
- `e2e/browser-participant-cohort.ts` (NEW) + `e2e:browser:participant` script in `package.json`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-participant-submit-co-sign-track-and-resolve/03-CONTEXT.md
@.planning/phases/03-participant-submit-co-sign-track-and-resolve/03-UI-SPEC.md
@.planning/phases/03-participant-submit-co-sign-track-and-resolve/03-RESEARCH.md
@.planning/phases/03-participant-submit-co-sign-track-and-resolve/03-PATTERNS.md
@packages/web/src/components/cohort/CohortPage.tsx
@packages/web/src/components/cohort/StageTimeline.tsx
@packages/web/src/components/participant/ResultCard.tsx
@packages/web/src/components/participant/ResolvePanel.tsx
@packages/web/src/components/participant/RegisterPanel.tsx
@packages/web/src/components/participant/PublishPanel.tsx
@packages/web/src/lib/resolve.ts
@packages/web/src/lib/sidecar.ts
@packages/web/src/stores/participant.ts
@e2e/browse-join-cohort.ts
@e2e/lib/browser-harness.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Anchor sub-steps + CompletionSummary (resolve/round-trip/export/Register/IPFS); absorb + delete the tail panels</name>
  <files>packages/web/src/components/cohort/StageTimeline.tsx, packages/web/src/components/cohort/CompletionSummary.tsx, packages/web/src/components/cohort/CohortPage.tsx, packages/web/src/components/participant/RegisterPanel.tsx, packages/web/src/components/participant/PublishPanel.tsx, packages/web/src/components/participant/ResolvePanel.tsx, packages/web/src/components/participant/ResultCard.tsx</files>
  <read_first>
    - packages/web/src/components/participant/ResultCard.tsx (the result presentation + sidecar download to absorb)
    - packages/web/src/components/participant/ResolvePanel.tsx (resolve trigger + resolved-document rendering to absorb)
    - packages/web/src/components/participant/RegisterPanel.tsx + PublishPanel.tsx (the live-only registration + IPFS opt-in to absorb as conditional post-completion stages)
    - packages/web/src/stores/participant.ts (anchor DTO, resolution, roundTripOutcome, resolve/register/publishIpfs/downloadSidecar/startOver actions)
    - .planning/phases/03-participant-submit-co-sign-track-and-resolve/03-RESEARCH.md Finding 7 (three round-trip outcomes) + Finding 8 (both fold-ins post-completion)
    - .planning/phases/03-participant-submit-co-sign-track-and-resolve/03-UI-SPEC.md (Copywriting Contract: round-trip reflected/hermetic-genesis/mismatch, Anchored live sub-steps, k-of-n fallback, completion export offer, resolve rerun)
  </read_first>
  <action>
    Extend `StageTimeline.tsx` with the live anchor sub-steps (Signed -> Broadcast with txid + a 'View on explorer' link from anchor.explorerUrl -> Confirmed) rendered ONLY when anchor.enabled, freezing at first confirmation (D-22); the hermetic path keeps the single 'Signed' line. Create `CompletionSummary.tsx` absorbing the ResultCard/ResolvePanel/PublishPanel/RegisterPanel LOGIC as stage internals: the plain-language result (beacon service added, version N), the resolved DID document + resolution metadata behind a raw-detail expander, the three-way round-trip outcome from roundTripOutcome (reflected / hermetic-genesis-expected / mismatch-with-retry, D-29), a 'Resolve again' action, the 'Download sidecar (resolver artifacts)' export (reuse lib/sidecar + downloadSidecar), and the CONDITIONAL post-completion stages (D-17/Finding 8): the KEY first-update registration (reuse RegisterPanel logic, live+included only, gated on anchor.enabled) and the opt-in IPFS publish (gated on GET /v1/ipfs enabled), both rendered in the completion region, never on the hermetic path. Mount CompletionSummary in CohortPage at the resolved/complete stage. Then DELETE `RegisterPanel.tsx`, `PublishPanel.tsx`, `ResolvePanel.tsx`, `ResultCard.tsx` and confirm nothing imports them. No em-dash characters; no new demo-crowd framing terms.
  </action>
  <verify>
    <automated>pnpm --filter @btcr2-aggregation/web build</automated>
  </verify>
  <acceptance_criteria>
    - StageTimeline renders anchor sub-steps only when anchor.enabled and freezes at confirmed (source assertion).
    - CompletionSummary renders the three-way round-trip (incl. hermetic-genesis as expected, not a warning), the resolved-doc expander, the sidecar export, and the conditional Register/IPFS stages gated on enabled/live (source assertion).
    - `RegisterPanel.tsx`, `PublishPanel.tsx`, `ResolvePanel.tsx`, `ResultCard.tsx` are deleted and unimported (grep returns no matches).
    - `pnpm --filter @btcr2-aggregation/web build` clean.
  </acceptance_criteria>
  <done>The cohort page tracks the anchor mode-honestly, auto-resolves with an honest round-trip and export, folds in the conditional live stages, and the four tail panels are absorbed and deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Degraded-state banners + k-of-n fallback outcome + cooperative non-inclusion + Start over</name>
  <files>packages/web/src/components/cohort/CohortPage.tsx</files>
  <read_first>
    - packages/web/src/components/cohort/CohortPage.tsx (from 03-05; where the stage/summary render)
    - packages/web/src/stores/participant.ts (unreachable, error/reason, status, startOver, the fallback-observed state)
    - .planning/phases/03-participant-submit-co-sign-track-and-resolve/03-UI-SPEC.md (Copywriting Contract: service unreachable, cohort stalled, terminal specific/unknown, cooperative non-inclusion, Start over destructive confirmation, k-of-n fallback outcome)
  </read_first>
  <action>
    Add the degraded-state rendering to CohortPage, each mapping to the UI-SPEC copy: the D-24 'Can't reach this service' banner when the store's unreachable is set (quiet auto-retry, stages frozen, never terminal by itself); the D-25 terminal states with best-effort specific reasons and the honest 'The cohort ended and this service didn't say why.' fallback + 'Back to cohorts'; the Finding-2 stalled-cohort copy for a CollectingUpdates stall; the D-23 explicit k-of-n fallback outcome ('The cohort anchored via the k-of-n fallback path with {k} of {n} signatures. Your update was {included / not included}.') when the runner observed fallback-requested; and the D-10 cooperative non-inclusion as a distinct NON-error outcome that still reports the cohort's anchor result. Add the 'Start over' action from any terminal state as a danger-variant button behind the explicit key-custody confirmation dialog ('This clears this cohort's result and erases the DID key held in your browser...'), calling the store's startOver(). Ensure the 'Your cohort' chip reflects these states (freeze under D-24, terminal label under D-25) without inventing a state the page does not show. No em-dash characters.
  </action>
  <verify>
    <automated>pnpm --filter @btcr2-aggregation/web build</automated>
  </verify>
  <acceptance_criteria>
    - The unreachable banner renders off the store's unreachable flag and is not a terminal state (source/behavior assertion).
    - Terminal reasons use the best-effort-plus-fallback copy; the CollectingUpdates stall uses the dedicated stall copy (source assertion).
    - The k-of-n fallback outcome and cooperative non-inclusion each render as documented (fallback states k of n + inclusion; non-inclusion is a non-error that still shows the anchor result) (source assertion).
    - 'Start over' is gated behind the danger-variant key-custody confirmation and calls startOver() (source/behavior assertion).
    - `pnpm --filter @btcr2-aggregation/web build` clean.
  </acceptance_criteria>
  <done>Every honest degraded/terminal/fallback/non-inclusion state renders with the UI-SPEC copy, and Start over wipes the identity behind an explicit warning.</done>
</task>

<task type="auto">
  <name>Task 3: Browser-level hermetic capstone e2e (criterion 4) + script</name>
  <files>e2e/browser-participant-cohort.ts, package.json</files>
  <read_first>
    - e2e/browse-join-cohort.ts (service boot, operator login + advertise, cookie handling, advert-ordering discipline in the header, headless in-process peers)
    - e2e/lib/browser-harness.ts (launchBrowser, waitForApp, trackPageErrors, resolveChromium, getFreePort)
    - .planning/phases/03-participant-submit-co-sign-track-and-resolve/03-PATTERNS.md (capstone section) + 03-RESEARCH.md Pitfall 7 (single-advert-slot ordering) + D-32/D-33
  </read_first>
  <action>
    Create `e2e/browser-participant-cohort.ts` (script `e2e:browser:participant`, local like e2e:kofn; NOT wired into CI - Phase 6 debt, D-32). Boot one hermetic operator-enabled service and advertise a single cohort of size n (advertise the picked cohort LAST / advertise one cohort to respect the single-advert-slot transport, Pitfall 7). Launch headless Chromium (launchBrowser + waitForApp) and drive ONE real page as the participant: land on the directory (assert the browse landing is the entry path and there is NO KeyGen-first stepper affordance - criterion 4), pick the cohort, choose/generate an identity inline, join, then explicitly CLICK 'Submit my DID update' (proving the explicit submit gate). Start (n-1) headless in-process peers (createParticipant with the picked cohortId, auto-submit opt-out - D-33 notes FILLERS/headless peers cover self-testing) that fill the remaining seats and co-sign. Assert the page reaches the mode-honest SIGNED state (the hermetic signed copy, NOT 'anchored' and no txid), auto-resolve runs, and the round-trip renders the hermetic-genesis honest outcome. Synchronize on hard completion events, never bare timers. Reuse the cookie-echo + advert-ordering discipline from browse-join-cohort.ts. Add the `e2e:browser:participant` script to package.json ("pnpm -r build && tsx e2e/browser-participant-cohort.ts"). No em-dash characters; no new demo-crowd framing terms.
  </action>
  <verify>
    <automated>pnpm e2e:browser:participant</automated>
  </verify>
  <acceptance_criteria>
    - `e2e:browser:participant` exists in package.json and runs the new harness (source assertion).
    - The capstone asserts the directory landing / absence of a KeyGen-first stepper affordance (criterion 4) (test assertion).
    - The capstone drives an explicit submit CLICK and asserts the mode-honest SIGNED wording (not 'anchored', no txid) + the hermetic-genesis round-trip outcome (test assertion).
    - `pnpm e2e:browser:participant` passes (headless peers fill the remaining seats; synchronized on completion events).
  </acceptance_criteria>
  <done>A browser-level hermetic capstone proves browse -> pick -> join -> explicit submit click -> co-sign -> mode-honest signed -> auto-resolve -> honest round-trip, and that the directory (not a stepper) is the entry path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anchor read / resolve -> UI copy | Tracking + resolve copy must reflect the true mode (enabled) and the true round-trip |
| terminal/degraded detection -> UI | Transient failures must not be shown as terminal; unknowns must not be invented |
| in-browser key -> Start over | Identity wipe is irreversible and must be gated behind an explicit warning |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06-01 | Repudiation / misrepresentation | round-trip + anchor copy | high | mitigate | Every anchor/resolve string branches on anchor.enabled; hermetic-genesis is rendered as expected, never as 'reflected' or a mismatch (D-07/D-29); the capstone asserts the SIGNED (not anchored) wording. |
| T-03-06-02 | Denial of Service / honesty | degraded-state detection | medium | mitigate | Transient unreachable freezes with quiet retry and never becomes terminal (D-24); terminal reasons are best-effort with an honest 'didn't say why' fallback (D-25). |
| T-03-06-03 | Tampering | Start over identity wipe | high | mitigate | The wipe is behind a danger-variant, explicit key-custody confirmation; the sidecar-export reminder precedes it (D-10). |
| T-03-06-04 | Information disclosure | conditional live stages | medium | mitigate | Registration/IPFS stages render only when live/enabled; the hermetic path never exposes a funding/registration surface (D-17). |

No new packages are installed in this plan (zero-install phase); supply-chain checkpoint not applicable.
</threat_model>

<verification>
- `pnpm --filter @btcr2-aggregation/web build` clean (no dangling imports after the four deletions).
- `pnpm test` green.
- `pnpm e2e:browser:participant` passes (the phase-level proof).
- Regression: `pnpm e2e && pnpm e2e:browse && pnpm e2e:operator && pnpm e2e:kofn && pnpm e2e:fallback` all pass.
- The two booth-topology browser jobs (`e2e:browser`, `e2e:browser:prod`) remain red by prior operator decision (Phase-6 debt); do not count them.
- End-of-phase human verify (config: human_verify_mode=end-of-phase) covers the visual/interaction states; no checkpoint task here.
</verification>

<success_criteria>
A participant can track the anchor mode-honestly, resolve with an honest three-way round-trip and export, and see honest degraded/fallback/non-inclusion states; the four tail panels are absorbed and deleted; and the browser capstone proves the whole discover -> submit -> co-sign -> signed -> resolve loop with the stepper retired (criterion 4).
</success_criteria>

<output>
Create `.planning/phases/03-participant-submit-co-sign-track-and-resolve/03-06-SUMMARY.md` when done.
</output>
