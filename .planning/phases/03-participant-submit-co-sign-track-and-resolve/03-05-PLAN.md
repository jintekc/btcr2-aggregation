---
phase: 03-participant-submit-co-sign-track-and-resolve
plan: 05
type: execute
wave: 3
depends_on: [03-04]
files_modified:
  - packages/web/src/components/cohort/CohortPage.tsx
  - packages/web/src/components/cohort/StageTimeline.tsx
  - packages/web/src/components/cohort/SubmitPanel.tsx
  - packages/web/src/components/browse/BrowseView.tsx
  - packages/web/src/components/browse/CohortRow.tsx
  - packages/web/src/components/browse/JoinIdentityStep.tsx
  - packages/web/src/App.tsx
  - packages/web/src/components/participant/FlowStepper.tsx
  - packages/web/src/components/participant/ParticipantView.tsx
  - packages/web/src/components/participant/KeyGenPanel.tsx
autonomous: true
requirements: [PART-03]
user_setup: []

must_haves:
  truths:
    - "populated (UI E1): the one live cohort page shows the FULL stage timeline upfront (Waiting for seats -> Seated -> Submit update -> Co-signing -> Signed) with the active stage highlighted (pulsing accent StatusDot), completed stages good-tone, and future stages dimmed as pending (D-01/D-05), rendered from deriveStage."
    - "empty (UI E1/E9): with no cohort joined, the surface renders the documented 'No cohort joined yet' empty state with a 'Browse cohorts' CTA back to the directory landing; the stage timeline renders only for a joined cohort."
    - "loading (UI E1): the active stage shows the quiet 'Active for {mm:ss}' elapsed indicator + a pulsing StatusDot; no countdowns, no duration promises (D-05)."
    - "empty (UI E2): the Submit update stage renders dimmed/pending until the submit window opens; the preview body is the exact locally-built update (D-01/D-12)."
    - "loading (UI E2): clicking Submit disables the CTA and advances the timeline to the co-signing stage's live indicator (submitUpdate resolves the deferred)."
    - "The submit moment (D-12/D-14): SubmitPanel shows the plain-language preview lead + a raw signed-update JSON expander + one beacon-commitment consent line branched on anchor.enabled (hermetic vs live copy), with NO second mid-round approval gate (D-14)."
    - "Submit-window urgency (D-13, the one exception to D-03's quiet indicator): when the window opens, the stage heading escalates ('Your update is needed') AND the tab title changes ('(!) Submit your update'); the title is restored on submit/leave/terminal."
    - "Mode-honest signed copy (D-07): on a hermetic (anchor.enabled:false) service the stage reads 'Signed. This no-broadcast service does not publish to Bitcoin, so there is no on-chain anchor to show.'; it never renders 'Anchored' or a txid when enabled is false."
    - "compact identity section (D-06): shows the participant's own DID (Mono/CopyField), onboarding model (KEY/EXTERNAL), and the key-custody note ('You hold this key in your browser only. It is never sent to the service.'); the secret is shown only via the existing CopyField pattern."
    - "Plain-language stage labels + a technical-detail expander (D-06): raw protocol phase, cohort id, beacon address, txid as Mono/CopyField, and a timestamped client-observed activity log (D-27) accumulate inside the expander; clean stages for strangers."
    - "populated (UI E3): while seated, the joined cohort's directory row shows 'You're in this cohort' + a 'View cohort' action (accent primary) instead of Join, and Join is disabled on every other row (one-cohort-at-a-time, D-03/D-04); in-flight rows read 'In progress' (D-26)."
    - "loading (UI E3): directory row state flips live on the ~5s poll; the joined-row affordance updates without a reload (D-04/D-26)."
    - "loading (UI E10): a persistent 'Your cohort · {stage label}' link with a live StatusDot returns to the cohort page and updates from the same feed as the timeline; no browser notifications beyond the D-13 submit-window title exception (D-03)."
    - "error confirm-dialog (UI E8): Leave shows the explicit commitment-point confirmation ('Once co-signing starts you are committed through anchoring') and is available ONLY while waiting for seats; it hides once the cohort locks into signing (D-09)."
    - "overflow / long-text (UI E3): long DID / cohort id values in rows use Mono truncate + CopyField so the full value stays copyable without breaking layout (shipped primitives)."
    - "zero-one-many (UI E4/E1): the timestamped activity log accumulates in the technical-detail expander (D-27) and seat / co-sign figures use tabular-nums; counts are about other members only, never member identities (D-08)."
    - "D-18 identity reuse: when an identity is already in memory, the join identity step offers 'use current identity' as the DEFAULT alongside generate/import, so the same DID can accumulate a version N+1 update across cohorts."
    - "D-19 fit warning: on import/pick the join identity step renders preSeatFitWarning(pickedRow) as an informed warning with a 'join anyway' choice - never a block; late cooperative non-inclusion stays the backstop."
    - "The FlowStepper shell, ParticipantView, and KeyGenPanel are deleted; generate/import lives ONLY in the join identity step (one identity moment in the app), and the directory landing - not a KeyGen-first stepper - is the entry path (D-31, criterion 4)."
    - statement: "long-text (UI E4): a large raw signed-update JSON in the collapsed-by-default expander does not overflow the card; the expander scrolls."
      verification: backstop
    - statement: "overflow (UI E4): the activity log at many entries is contained and scrolled by the expander without breaking page layout."
      verification: backstop
    - statement: "overflow (UI E2): an update-preview JSON at unusual size uses the same expander/scroll treatment; no clipped-content silent loss."
      verification: backstop
  artifacts:
    - packages/web/src/components/cohort/CohortPage.tsx
    - packages/web/src/components/cohort/StageTimeline.tsx
    - packages/web/src/components/cohort/SubmitPanel.tsx
  key_links:
    - "deriveStage (03-04) -> StageTimeline render + 'Your cohort · {stage}' link"
    - "pendingSubmit/submitUpdate (03-04) -> SubmitPanel preview + submit CTA + submit-window title"
    - "BrowseView mounts CohortPage on joined-cohort store state (internal SPA view, D-11); directory rows read seated state for the 'You're in this cohort' affordance"
  prohibitions:
    - statement: "MUST NOT render 'Anchored', a txid, or any on-chain claim when the anchor read's enabled is false; hermetic copy reads signed/complete only (D-07)."
      status: flagged-unverified
      verification: flagged
    - statement: "MUST NOT send or display the participant's private key to the service; the secret is shown only via the existing in-browser CopyField (key custody)."
      status: flagged-unverified
      verification: flagged
    - statement: "MUST NOT add a second mid-round signing-approval gate; one consent at submit covers the round (D-14)."
      status: flagged-unverified
      verification: flagged
    - statement: "MUST NOT block a join on the D-19 fit check; warn with an informed 'join anyway' only (never blocks)."
      status: flagged-unverified
      verification: flagged
    - statement: "MUST NOT introduce new demo-crowd framing wording in the new cohort-page copy or comments; use operator/participant/service framing (the full legacy sweep is Phase 6)."
      status: flagged-unverified
      verification: flagged
    - statement: "MUST NOT use the em-dash character anywhere in the new copy, comments, or code (user hard rule; blocked a Phase 2 plan-check iteration)."
      status: flagged-unverified
      verification: flagged
---

<objective>
Build the one live cohort page (D-01/D-02) through the mode-honest "Signed" stage: the stage timeline absorbing the Phase-2 waiting/seated states, the explicit submit moment (preview + consent + submit-window urgency), the compact identity section + technical-detail/activity-log expander, the directory row state + persistent "Your cohort" link, plus the D-18/D-19 identity-step upgrades - and delete the standalone stepper so the directory is the only entry path (criterion 4). This is the PART-03 user-visible slice.

Purpose: After this plan a real participant can browse, pick, join, explicitly submit, co-sign, and see the honest Signed state on one continuous surface, with the linear stepper gone.
Output: `components/cohort/{CohortPage,StageTimeline,SubmitPanel}.tsx`, the rewired BrowseView/App/CohortRow/JoinIdentityStep, and three deleted stepper files.
</objective>

## Phase Goal (user story, derived from the ROADMAP Phase 3 goal)

**As a** participant who joined a cohort by choice, **I want to** submit my DID update, take part in the cohort's n-of-n MuSig2 co-signing round, track the anchor, and resolve my updated DID, **so that** I complete the real aggregation lifecycle from the cohort I discovered, not a standalone demo stepper.

> This is the first user-visible Wave-3 slice: after Task 3 a user can do the full browse -> pick -> join -> explicit submit -> co-sign -> Signed loop that they could not do before. Anchor tracking + resolve + degraded states land in 03-06.

## Artifacts this phase produces (this plan)

- `packages/web/src/components/cohort/CohortPage.tsx`, `StageTimeline.tsx`, `SubmitPanel.tsx` (NEW).
- BrowseView renders CohortPage on joined-cohort store state (internal SPA view, no route); a "Your cohort · {stage}" persistent link.
- CohortRow "You're in this cohort" + View affordance; JoinIdentityStep "use current identity" default + fit warning.
- Deletions: `components/participant/{FlowStepper,ParticipantView,KeyGenPanel}.tsx`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-participant-submit-co-sign-track-and-resolve/03-CONTEXT.md
@.planning/phases/03-participant-submit-co-sign-track-and-resolve/03-UI-SPEC.md
@.planning/phases/03-participant-submit-co-sign-track-and-resolve/03-PATTERNS.md
@packages/web/src/App.tsx
@packages/web/src/components/browse/BrowseView.tsx
@packages/web/src/components/browse/CohortRow.tsx
@packages/web/src/components/browse/JoinIdentityStep.tsx
@packages/web/src/components/browse/DirectoryList.tsx
@packages/web/src/ui/primitives.tsx
@packages/web/src/stores/participant.ts
@packages/web/src/lib/directory.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: StageTimeline + CohortPage shell (waiting -> seated -> submit -> co-signing -> signed) with identity section + expander</name>
  <files>packages/web/src/components/cohort/StageTimeline.tsx, packages/web/src/components/cohort/CohortPage.tsx</files>
  <read_first>
    - packages/web/src/ui/primitives.tsx (Card/SectionTitle/Badge/Button/StatusDot at 188-205/Mono at 208/CopyField at 242 - build from these)
    - packages/web/src/components/browse/BrowseView.tsx (the seated/waiting states this absorbs, D-02)
    - packages/web/src/stores/participant.ts (deriveStage, Stage, the state slices to subscribe)
    - packages/web/src/lib/clock.ts (elapsed offset for the quiet indicator)
    - .planning/phases/03-participant-submit-co-sign-track-and-resolve/03-UI-SPEC.md (Color: primary visual anchor = the timeline; Typography; Copywriting Contract stage labels + elapsed + signed-complete-hermetic + keep-tab-open + identity note)
  </read_first>
  <action>
    Create `StageTimeline.tsx` rendering the full D-01 stage list upfront from `deriveStage(state)`: completed stages carry a good-tone StatusDot, the active stage a pulsing accent StatusDot + the quiet 'Active for {mm:ss}' elapsed indicator (D-05), future stages dimmed. Labels are the UI-SPEC copy ('Waiting for seats' / 'Seated' / 'Submit update' / 'Co-signing' / 'Signed'). Create `CohortPage.tsx` as the one continuous surface (D-01/D-02): the empty state ('No cohort joined yet' + 'Browse cohorts' CTA) when no cohort is joined; otherwise the StageTimeline, a compact identity section (own DID via Mono/CopyField, KEY/EXTERNAL model, the key-custody note), a keep-tab-open note (D-08), and a collapsible technical-detail expander (raw protocol phase, cohort id, beacon address as CopyField, and the timestamped activity log from the store's log, D-06/D-27). Signed copy is mode-honest off `anchor.enabled` (D-07): hermetic reads the documented 'Signed. This no-broadcast service...' line, never a txid. Subscribe to store slices via `useParticipant`; the accent budget is one CTA per view (UI-SPEC Color). Build only from `ui/primitives.tsx`. No em-dash characters; no new demo-crowd framing terms.
  </action>
  <verify>
    <automated>pnpm --filter @btcr2-aggregation/web build</automated>
  </verify>
  <acceptance_criteria>
    - StageTimeline renders all D-01 stages upfront with active/completed/pending treatment derived from deriveStage (source assertion).
    - CohortPage renders the 'No cohort joined yet' empty state with a 'Browse cohorts' CTA when unjoined (source/behavior assertion).
    - No 'Anchored'/txid string is rendered when anchor.enabled is false (grep the hermetic branch).
    - `pnpm --filter @btcr2-aggregation/web build` (tsc --noEmit + vite build) is clean.
  </acceptance_criteria>
  <done>The one live cohort page renders the full timeline, the identity section, the keep-tab-open note, and the technical/activity expander, mode-honest at Signed.</done>
</task>

<task type="auto">
  <name>Task 2: SubmitPanel (preview + consent + urgency) + JoinIdentityStep D-18/D-19</name>
  <files>packages/web/src/components/cohort/SubmitPanel.tsx, packages/web/src/components/browse/JoinIdentityStep.tsx</files>
  <read_first>
    - packages/web/src/stores/participant.ts (pendingSubmit state, submitUpdate action, preSeatFitWarning; the SubmittedUpdate shape)
    - packages/web/src/components/browse/JoinIdentityStep.tsx (the existing generate/import inline step; where the identity choice renders)
    - .planning/phases/03-participant-submit-co-sign-track-and-resolve/03-UI-SPEC.md (Copywriting Contract: Submit preview lead, both beacon-commitment consent lines, submit-window urgency heading + tab title)
  </read_first>
  <action>
    Create `SubmitPanel.tsx` shown when the submit window is open (store `pendingSubmit`): render the plain-language preview lead, the exact signed-update body in a collapsed-by-default raw-JSON expander (the same expander/scroll treatment as the technical expander), one beacon-commitment consent line branched on `anchor.enabled` (the hermetic vs live UI-SPEC copy, D-14), and the single accent 'Submit my DID update' CTA calling `submitUpdate()`; disable it after click. Apply the D-13 submit-window urgency: the stage heading escalates ('Your update is needed') and the panel sets `document.title` to '(!) Submit your update' while open, restoring it on submit/leave/terminal (a small effect). NO second approval gate (D-14). In `JoinIdentityStep.tsx`, add the D-18 'use current identity' option as the DEFAULT when an identity is already in memory (alongside generate/import), and render `preSeatFitWarning(pickedRow)` as an informed warning with an explicit 'join anyway' choice (never blocks, D-19). No em-dash characters.
  </action>
  <verify>
    <automated>pnpm --filter @btcr2-aggregation/web build</automated>
  </verify>
  <acceptance_criteria>
    - SubmitPanel renders the preview + a collapsed raw-JSON expander + exactly one consent line chosen by anchor.enabled + one accent submit CTA calling submitUpdate() (source assertion).
    - The submit-window effect sets and restores document.title (source assertion for set + cleanup).
    - JoinIdentityStep offers 'use current identity' as default when an identity exists and shows preSeatFitWarning as a non-blocking warning (source/behavior assertion).
    - No second signing-approval control exists (grep: no onApproveSigning wired to UI).
    - `pnpm --filter @btcr2-aggregation/web build` clean.
  </acceptance_criteria>
  <done>The explicit submit moment renders the honest preview + one consent + urgency, and the identity step supports reuse and non-blocking fit warnings.</done>
</task>

<task type="auto">
  <name>Task 3: Mount CohortPage in BrowseView + directory row state + "Your cohort" link + delete the stepper</name>
  <files>packages/web/src/components/browse/BrowseView.tsx, packages/web/src/components/browse/CohortRow.tsx, packages/web/src/App.tsx, packages/web/src/components/participant/FlowStepper.tsx, packages/web/src/components/participant/ParticipantView.tsx, packages/web/src/components/participant/KeyGenPanel.tsx</files>
  <read_first>
    - packages/web/src/components/browse/BrowseView.tsx (the seated/joinClosed/failed/pickedRow branches this replaces with CohortPage mount, D-02/D-11)
    - packages/web/src/components/browse/CohortRow.tsx (the Join button + row layout to extend with the seated affordance)
    - packages/web/src/App.tsx (the shell; where a persistent 'Your cohort' link mounts)
    - packages/web/src/stores/participant.ts (seated/optedIn/status/deriveStage slices for row + link state)
  </read_first>
  <action>
    Rewire `BrowseView.tsx` so that when the store is in a joined-cohort lifecycle (optedIn/live/seated/complete/terminal for the picked cohort), it renders `CohortPage` (internal SPA view state, no route, D-11) instead of the old seated card + the Register/Publish/Resolve/ResultCard tail; keep the directory landing for the browse view and add an internal toggle so a persistent 'Your cohort . {stage label}' link (with a live StatusDot, D-03; use a plain separator, never an em-dash) returns to the cohort view while browsing. Remove BrowseView's imports of the four tail panels (their logic is absorbed into 03-06's CompletionSummary; the files are deleted there). In `CohortRow.tsx`, when the store is seated in THIS row's cohort, render a 'You're in this cohort' state with an accent 'View cohort' action (routes to the cohort view) instead of Join, and disable Join on every OTHER row while seated (one-cohort-at-a-time, D-04); in-flight rows already carry their raw phase so the 'In progress' label comes from statusLabel (D-26). In `App.tsx`, surface the persistent 'Your cohort' link in the shell when a cohort lifecycle is active. DELETE `FlowStepper.tsx`, `ParticipantView.tsx`, and `KeyGenPanel.tsx` (the standalone stepper shell + KeyGen-first entry; generate/import lives only in JoinIdentityStep, D-31). Confirm no remaining import references them. No em-dash characters; no new demo-crowd framing terms.
  </action>
  <verify>
    <automated>pnpm --filter @btcr2-aggregation/web build</automated>
  </verify>
  <acceptance_criteria>
    - BrowseView renders CohortPage for the joined-cohort states and no longer imports FlowStepper/ParticipantView/KeyGenPanel or the four tail panels (grep).
    - CohortRow shows 'You're in this cohort' + 'View cohort' on the seated row and disables Join on other rows while seated (source/behavior assertion).
    - A persistent 'Your cohort . {stage}' link exists and carries the live stage (source assertion).
    - `FlowStepper.tsx`, `ParticipantView.tsx`, `KeyGenPanel.tsx` are deleted and nothing imports them (grep returns no matches).
    - `pnpm --filter @btcr2-aggregation/web build` clean (no dangling imports).
  </acceptance_criteria>
  <done>The directory is the only entry path: the cohort page mounts on join, the seated row and 'Your cohort' link keep the directory reachable, and the standalone stepper is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-browser key -> UI display | The participant's secret is shown for backup but must never leave the browser |
| render copy vs service mode | Anchor/signed copy must reflect the true broadcast mode (enabled bit) |
| submit consent -> signing round | One consent must cover the whole round without a second gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05-01 | Information disclosure | key custody | high | mitigate | The secret renders only via the existing in-browser CopyField; it is never sent to the service; the custody note is shown in the identity section. |
| T-03-05-02 | Repudiation / misrepresentation | signed/anchor copy | high | mitigate | Every anchor/signed string branches on anchor.enabled; hermetic never claims a txid or on-chain anchor (D-07). |
| T-03-05-03 | Denial of Service (protocol) | submit consent | medium | mitigate | One consent at submit; no second mid-round approval gate that could stall the round (D-14). |
| T-03-05-04 | Tampering (UX integrity) | join fit gate | low | accept | preSeatFitWarning warns but never blocks; late cooperative non-inclusion is the honest backstop (D-19) - accepted as the intended non-blocking design. |

No new packages are installed in this plan (zero-install phase); supply-chain checkpoint not applicable.
</threat_model>

<verification>
- `pnpm --filter @btcr2-aggregation/web build` (tsc --noEmit + vite build) clean, no dangling imports after the deletions.
- `pnpm test` green (the store specs from 03-04 unaffected).
- `pnpm e2e:browse && pnpm e2e:operator` still pass (headless paths do not exercise the cohort page).
- Manual/visual is deferred to end-of-phase human verify (config: human_verify_mode=end-of-phase); no checkpoint task here.
</verification>

<success_criteria>
A real participant can browse, pick, join, explicitly submit, co-sign, and see the mode-honest Signed state on one continuous cohort page reached only from the directory; the standalone stepper is deleted.
</success_criteria>

<output>
Create `.planning/phases/03-participant-submit-co-sign-track-and-resolve/03-05-SUMMARY.md` when done.
</output>
